import ShielddGnarkFormal.ChoiceFreeZMod
import ShielddGnarkFormal.Deployed.TransferSemanticTranslator

set_option maxRecDepth 1000000
set_option maxHeartbeats 8000000

/-! Predicate-specific Transfer consequences.
GENERATED by gen_deployed_family.py — do not edit by hand. -/

namespace Shieldd.GnarkFormal.Deployed.Contracts.Transfer

open Shieldd.GnarkFormal
open Protocol
open scoped Shieldd.GnarkFormal.ChoiceFreeZMod

attribute [-instance] ZMod.instField
private local instance choiceFreeTransferSpecificationCommRing :
    CommRing SemanticF :=
  ZMod.commRing _

private abbrev action (rho : Nat → SemanticF) :=
  Deployed.TransferRefinement.C.action rho

/-- Exact relation to the complete handwritten Transfer circuit facts. -/
theorem circuitFacts_of_relationAll
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Transfer.CircuitFacts
      Protocol.Transfer.Concrete.circuitPrimitives
      (action rho) := by
  let facts := transfer_circuitFacts rho h
  let exactProviders := transferExactProviders rho facts
  let semanticProviders :=
    transferSemanticProviders_of_exact rho exactProviders
  exact
    Deployed.TransferSemanticTranslator.C.circuitFacts_of_semanticProviders
      rho
      (authAkNonIdentity_of_exact rho facts)
      (senderDivGenNonIdentity_of_exact rho facts)
      (authIvkReducedNonzero_of_exact rho facts)
      (Deployed.TransferSemanticTranslator.C.senderTransmissionComputedNonIdentity_of_semantic
          rho semanticProviders
          (senderTransmissionNonIdentity_of_exact rho facts))
      (output0RecipientDivGenNonIdentity_of_exact rho facts)
      (output0RecipientTransmissionNonIdentity_of_exact rho facts)
      (isRegulatedBoolean_of_exact rho facts)
      (assetIdNonzero_of_exact rho facts)
      semanticProviders

private def semanticProvidersOfRelation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    TransferSemanticProviders rho :=
  transferSemanticProviders_of_exact rho
    (transferExactProviders rho (transfer_circuitFacts rho h))

private theorem relationCanonicalSender
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.canonicalSender (action rho) :=
  (circuitFacts_of_relationAll rho h).canonicalSender

private theorem relationRequiredSpend
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.requiredSpend (action rho) :=
  (circuitFacts_of_relationAll rho h).requiredSpend

private theorem relationOptionalSpend
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.optionalSpend (action rho) :=
  (circuitFacts_of_relationAll rho h).optionalSpend

private theorem relationReceiverOutput
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.receiverOutput (action rho) :=
  (circuitFacts_of_relationAll rho h).receiverOutput

private theorem relationChangeOutput
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.changeOutput (action rho) :=
  (circuitFacts_of_relationAll rho h).changeOutput

private theorem relationAssetRegistry
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.assetRegistry (action rho) :=
  (circuitFacts_of_relationAll rho h).assetRegistry

private theorem relationSenderCompliance
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.senderCompliance (action rho) :=
  (circuitFacts_of_relationAll rho h).senderCompliance

private theorem relationReceiverCompliance
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.receiverCompliance (action rho) :=
  (circuitFacts_of_relationAll rho h).receiverCompliance

private theorem relationComplianceTranscript
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.complianceTranscript (action rho) :=
  (circuitFacts_of_relationAll rho h).complianceTranscript

private theorem relationBalance
    (rho : Nat → SemanticF) (h : relationAll rho) :
    Protocol.Transfer.Concrete.balanceComputedAndCompressed (action rho) :=
  (circuitFacts_of_relationAll rho h).balanceComputedAndCompressed

/-- `ASSET-ID-NONZERO` for the exact deployed relation. -/
theorem specification_asset_id_nonzero
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).assetId ≠ 0 := by
  exact (relationAssetRegistry rho h).1

/-- `ASSET-LEAF-HASH` for the exact deployed relation. -/
theorem specification_asset_leaf_hash
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ dkPubFq ringPkFq,
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.dkPub dkPubFq ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.ringPk ringPkFq ∧
      Protocol.Common.quadMember (action rho).assetAnchor
        (Protocol.Transfer.Concrete.assetLeafHash
          (action rho).assetProof.leaf dkPubFq ringPkFq)
        (action rho).assetProof.path (action rho).assetProof.position := by
  rcases (relationAssetRegistry rho h).2.2 with
    ⟨dkPubFq, ringPkFq, hdk, hring, hmember, _⟩
  exact ⟨dkPubFq, ringPkFq, hdk, hring, hmember⟩

/-- `ASSET-PARAMETERS-HASH` for the exact deployed relation. -/
theorem specification_asset_parameters_hash
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ dkPubFq,
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.dkPub dkPubFq ∧
      Protocol.Transfer.Concrete.assetParamsHash
          (action rho).assetProof.leaf dkPubFq =
        Poseidon377.hash3
          Protocol.Transfer.Concrete.assetParamsDomain dkPubFq
          (action rho).assetProof.leaf.threshold
          (action rho).assetProof.leaf.routePolicyHash := by
  rcases (relationAssetRegistry rho h).2.2 with
    ⟨dkPubFq, _, hdk, _, _, _⟩
  exact ⟨dkPubFq, hdk, rfl⟩

/-- `ASSET-POLICY-KEY-ENCODING` for the exact deployed relation. -/
theorem specification_asset_policy_key_encoding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ dkPubFq ringPkFq,
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.dkPub dkPubFq ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.ringPk ringPkFq := by
  rcases (relationAssetRegistry rho h).2.2 with
    ⟨dkPubFq, ringPkFq, hdk, hring, _, _⟩
  exact ⟨dkPubFq, ringPkFq, hdk, hring⟩

/-- `ASSET-REGISTRY-GAP-ORDERING` for the exact deployed relation. -/
theorem specification_asset_registry_gap_ordering
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Transfer.Concrete.registryGap
      (action rho).assetId (action rho).assetProof.isRegulated
      (action rho).assetProof.leaf.value
      (action rho).assetProof.leaf.nextValue := by
  rcases (relationAssetRegistry rho h).2.2 with
    ⟨_, _, _, _, _, hgap⟩
  exact hgap

/-- `ASSET-REGISTRY-MEMBERSHIP` for the exact deployed relation. -/
theorem specification_asset_registry_membership
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ dkPubFq ringPkFq,
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.dkPub dkPubFq ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.ringPk ringPkFq ∧
      Protocol.Common.quadMember (action rho).assetAnchor
        (Protocol.Transfer.Concrete.assetLeafHash
          (action rho).assetProof.leaf dkPubFq ringPkFq)
        (action rho).assetProof.path (action rho).assetProof.position := by
  rcases (relationAssetRegistry rho h).2.2 with
    ⟨dkPubFq, ringPkFq, hdk, hring, hmember, _⟩
  exact ⟨dkPubFq, ringPkFq, hdk, hring, hmember⟩

/-- `ASSET-REGULATED-BOOLEAN` for the exact deployed relation. -/
theorem specification_asset_regulated_boolean
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.boolean (action rho).assetProof.isRegulated := by
  exact (relationAssetRegistry rho h).2.1

/-- `ASSET-RING-HASH` for the exact deployed relation. -/
theorem specification_asset_ring_hash
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ ringPkFq,
      Protocol.Common.Decaf.compressesTo
        (action rho).assetProof.leaf.ringPk ringPkFq ∧
      Protocol.Transfer.Concrete.assetRingHash
          (action rho).assetProof.leaf ringPkFq =
        Poseidon377.hash5
          Protocol.Transfer.Concrete.assetRingDomain ringPkFq
          (action rho).assetProof.leaf.ringIdHash
          (action rho).assetProof.leaf.policyIdHash
          (action rho).assetProof.leaf.permissionHash
          (action rho).assetProof.leaf.resourceHash := by
  rcases (relationAssetRegistry rho h).2.2 with
    ⟨_, ringPkFq, _, hring, _, _⟩
  exact ⟨ringPkFq, hring, rfl⟩

/-- `CIR-SELECTOR-BOOLEAN` for the exact deployed relation. -/
theorem specification_cir_selector_boolean
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    spend1IsDummy rho = 0 ∨ spend1IsDummy rho = 1 := by
  exact
    Deployed.TransferSemanticTranslator.C.optionalIsDummyBoolean
      rho (semanticProvidersOfRelation rho h)

/-- `CIR-SHAPE-FIXED` for the exact deployed relation. -/
theorem specification_cir_shape_fixed
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (Protocol.Transfer.Concrete.statementFields
      (action rho)).length = 47 := by
  exact Protocol.Transfer.Concrete.statementFields_length
    (action rho)

/-- `COMPLIANCE-ADDRESS-ENCRYPTION` for the exact deployed relation. -/
theorem specification_compliance_address_encryption
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderExtSelected outputExtSelected
      senderExtSelectedFq outputExtSelectedFq,
      Protocol.Common.Decaf.compressesTo
        senderExtSelected senderExtSelectedFq ∧
      Protocol.Common.Decaf.compressesTo
        outputExtSelected outputExtSelectedFq ∧
      Protocol.Transfer.Concrete.addressEncryption
        senderExtSelectedFq (action rho).transcript.senderExt.c2
        (action rho).receiver.note.owner
        (action rho).transcript.senderExt.ciphertext ∧
      Protocol.Transfer.Concrete.addressEncryption
        outputExtSelectedFq (action rho).transcript.outputExt.c2
        (action rho).sender
        (action rho).transcript.outputExt.ciphertext := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      _, _, _, _, _, senderExtSelected,
      _, _, _, _, _, outputExtSelected,
      _, _, _, _, hcrypto, _⟩
  rcases hcrypto with
    ⟨_, _, senderExtSelectedFq, _, outputExtSelectedFq,
      _, _, hsenderCompress, _, houtputCompress,
      _, _, hsenderAddress, _, houtputAddress⟩
  exact
    ⟨senderExtSelected, outputExtSelected,
      senderExtSelectedFq, outputExtSelectedFq,
      hsenderCompress, houtputCompress,
      hsenderAddress, houtputAddress⟩

/-- `COMPLIANCE-AMOUNT-ENCRYPTION` for the exact deployed relation. -/
theorem specification_compliance_amount_encryption
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderCoreSelected outputCoreSelected
      senderCoreSelectedFq outputCoreSelectedFq,
      Protocol.Common.Decaf.compressesTo
        senderCoreSelected senderCoreSelectedFq ∧
      Protocol.Common.Decaf.compressesTo
        outputCoreSelected outputCoreSelectedFq ∧
      Protocol.Transfer.Concrete.amountEncryption
        senderCoreSelectedFq (action rho).transcript.senderCore.c2
        (action rho).receiver.note.amount
        (action rho).transcript.senderCore.ciphertext ∧
      Protocol.Transfer.Concrete.amountEncryption
        outputCoreSelectedFq (action rho).transcript.outputCore.c2
        (action rho).receiver.note.amount
        (action rho).transcript.outputCore.ciphertext := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      _, _, senderCoreSelected, _, _, _,
      _, _, outputCoreSelected, _, _, _,
      _, _, _, _, hcrypto, _⟩
  rcases hcrypto with
    ⟨_, senderCoreSelectedFq, _, outputCoreSelectedFq, _,
      _, hsenderCompress, _, houtputCompress, _,
      _, hsenderAmount, _, houtputAmount, _⟩
  exact
    ⟨senderCoreSelected, outputCoreSelected,
      senderCoreSelectedFq, outputCoreSelectedFq,
      hsenderCompress, houtputCompress,
      hsenderAmount, houtputAmount⟩

/-- `COMPLIANCE-DETECTION-ENCRYPTION` for the exact deployed relation. -/
theorem specification_compliance_detection_encryption
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderCoreIssuer senderCoreIssuerFq,
      Protocol.Common.Decaf.compressesTo
        senderCoreIssuer senderCoreIssuerFq ∧
      Protocol.Transfer.Concrete.detectionEncryption
        (action rho) senderCoreIssuerFq := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      senderCoreIssuer, _, _, _, _, _, _, _, _, _, _, _,
      _, _, _, _, hcrypto, _⟩
  rcases hcrypto with
    ⟨senderCoreIssuerFq, _, _, _, _,
      hcompress, _, _, _, _, hdetection, _, _, _, _⟩
  exact
    ⟨senderCoreIssuer, senderCoreIssuerFq, hcompress, hdetection⟩

/-- `COMPLIANCE-FLAG-BOOLEAN` for the exact deployed relation. -/
theorem specification_compliance_flag_boolean
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.boolean (action rho).transcript.isFlagged := by
  rcases relationComplianceTranscript rho h with
    ⟨_, hflag, _, _, _, _, _, _, _, _, _, _, _⟩
  exact hflag

/-- `COMPLIANCE-METADATA-BINDING` for the exact deployed relation. -/
theorem specification_compliance_metadata_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Transfer.Concrete.metadataBinding (action rho) := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      _, _, _, _, _, _, _, _, _, _, _, _,
      _, _, _, _, _, hmetadata⟩
  exact hmetadata

/-- `COMPLIANCE-POLICY-SELECTION` for the exact deployed relation. -/
theorem specification_compliance_policy_selection
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.boolean (action rho).assetProof.isRegulated ∧
      Protocol.Transfer.Concrete.effectiveRingPk (action rho) =
        Protocol.Transfer.Concrete.selectPoint
          (action rho).assetProof.isRegulated
          (action rho).assetProof.leaf.ringPk
          Protocol.Transfer.Concrete.unregulatedRingPk ∧
      Protocol.Transfer.Concrete.effectiveDkPub (action rho) =
        Protocol.Transfer.Concrete.selectPoint
          (action rho).assetProof.isRegulated
          (action rho).assetProof.leaf.dkPub
          Protocol.Transfer.Concrete.unregulatedDkPub ∧
      Protocol.Transfer.Concrete.effectiveRingIdHash (action rho) =
        Protocol.Transfer.Concrete.selectField
          (action rho).assetProof.isRegulated
          (action rho).assetProof.leaf.ringIdHash
          Protocol.Transfer.Concrete.unregulatedPolicyHash ∧
      Protocol.Transfer.Concrete.effectivePolicyIdHash (action rho) =
        Protocol.Transfer.Concrete.selectField
          (action rho).assetProof.isRegulated
          (action rho).assetProof.leaf.policyIdHash
          Protocol.Transfer.Concrete.unregulatedPolicyHash ∧
      Protocol.Transfer.Concrete.effectiveResourceHash (action rho) =
        Protocol.Transfer.Concrete.selectField
          (action rho).assetProof.isRegulated
          (action rho).assetProof.leaf.resourceHash
          Protocol.Transfer.Concrete.unregulatedPolicyHash ∧
      Protocol.Transfer.Concrete.effectivePermissionHash (action rho) =
        Protocol.Transfer.Concrete.selectField
          (action rho).assetProof.isRegulated
          (action rho).assetProof.leaf.permissionHash
          Protocol.Transfer.Concrete.unregulatedPolicyHash := by
  exact
    ⟨(relationAssetRegistry rho h).2.1,
      rfl, rfl, rfl, rfl, rfl, rfl⟩

/-- `COMPLIANCE-SALT-DERIVATION` for the exact deployed relation. -/
theorem specification_compliance_salt_derivation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).transcript.salts 0 =
        Protocol.Transfer.Concrete.transferSalt
          (action rho).transcript.transferNonceRoot
          Protocol.Transfer.Concrete.detectionSaltLabel ∧
      (action rho).transcript.salts 1 =
        Protocol.Transfer.Concrete.transferSalt
          (action rho).transcript.transferNonceRoot
          Protocol.Transfer.Concrete.senderCoreSaltLabel ∧
      (action rho).transcript.salts 2 =
        Protocol.Transfer.Concrete.transferSalt
          (action rho).transcript.transferNonceRoot
          Protocol.Transfer.Concrete.senderExtSaltLabel ∧
      (action rho).transcript.salts 3 =
        Protocol.Transfer.Concrete.transferSalt
          (action rho).transcript.transferNonceRoot
          Protocol.Transfer.Concrete.outputCoreSaltLabel ∧
      (action rho).transcript.salts 4 =
        Protocol.Transfer.Concrete.transferSalt
          (action rho).transcript.transferNonceRoot
          Protocol.Transfer.Concrete.outputExtSaltLabel := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, hs0, hs1, hs2, hs3, hs4, _, _, _, _, _⟩
  exact ⟨hs0, hs1, hs2, hs3, hs4⟩

/-- `COMPLIANCE-SHARED-SECRET-SELECTION` for the exact deployed relation. -/
theorem specification_compliance_shared_secret_selection
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderCoreIssuer senderCoreUser senderCoreSelected
      senderExtIssuer senderExtUser senderExtSelected
      outputCoreIssuer outputCoreUser outputCoreSelected
      outputExtIssuer outputExtUser outputExtSelected,
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderCore.epk
        (action rho).transcript.isFlagged
        senderCoreIssuer senderCoreUser senderCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderExt.epk
        (action rho).transcript.isFlagged
        senderExtIssuer senderExtUser senderExtSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputCore.epk
        (action rho).transcript.isFlagged
        outputCoreIssuer outputCoreUser outputCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputExt.epk
        (action rho).transcript.isFlagged
        outputExtIssuer outputExtUser outputExtSelected := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt, _, _⟩
  exact
    ⟨senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt⟩

/-- `COMPLIANCE-THRESHOLD-FLAG` for the exact deployed relation. -/
theorem specification_compliance_threshold_flag
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).transcript.isFlagged =
      Protocol.Transfer.Concrete.thresholdFlag
        (action rho).assetProof.isRegulated
        (action rho).receiver.note.amount
        (action rho).assetProof.leaf.threshold := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, hflag, _, _, _, _, _, _, _, _, _, _⟩
  exact hflag

/-- `DEC-ACK-DERIVATION` for the exact deployed relation. -/
theorem specification_dec_ack_derivation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d =
        Protocol.Common.Decaf.scalarMulWindow2 251
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d ∧
      Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d =
        Protocol.Common.Decaf.scalarMulWindow2 251
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d := by
  exact ⟨rfl, rfl⟩

/-- `DEC-BALANCE-COMMITMENT-DERIVATION` for the exact deployed relation. -/
theorem specification_dec_balance_commitment_derivation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ balanceCommitment,
      Protocol.Common.Decaf.netBalanceCommitment2
        (action rho).required.note.amount
        (action rho).optional.amount
        (action rho).receiver.note.amount
        (action rho).change.note.amount
        (action rho).assetId (action rho).balanceBlinding
        balanceCommitment := by
  rcases (relationBalance rho h).2.2.2.2.2 with
    ⟨balanceCommitment, hcomputed, _⟩
  exact ⟨balanceCommitment, hcomputed⟩

/-- `DEC-BALANCE-COMMITMENT-ENCODING` for the exact deployed relation. -/
theorem specification_dec_balance_commitment_encoding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ balanceCommitment,
      Protocol.Common.Decaf.netBalanceCommitment2
        (action rho).required.note.amount
        (action rho).optional.amount
        (action rho).receiver.note.amount
        (action rho).change.note.amount
        (action rho).assetId (action rho).balanceBlinding
        balanceCommitment ∧
      Protocol.Common.Decaf.compressesTo
        balanceCommitment (action rho).balanceCommitmentEncoding := by
  exact (relationBalance rho h).2.2.2.2.2

/-- `DEC-DIVERSIFIED-GENERATOR-ENCODING` for the exact deployed relation. -/
theorem specification_dec_diversified_generator_encoding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.Decaf.compressesTo
        (action rho).sender.diversifiedGenerator
        (action rho).sender.diversifiedGeneratorEncoding ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).receiver.note.owner.diversifiedGenerator
        (action rho).receiver.note.owner.diversifiedGeneratorEncoding := by
  have hs := (relationCanonicalSender rho h).1.2.1
  have hr := (relationReceiverOutput rho h).2.1
  exact ⟨hs.1.2.1, hr.1.2.1⟩

/-- `DEC-EPHEMERAL-PUBLIC-KEY-DERIVATION` for the exact deployed relation. -/
theorem specification_dec_ephemeral_public_key_derivation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderCoreIssuer senderCoreUser senderCoreSelected
      senderExtIssuer senderExtUser senderExtSelected
      outputCoreIssuer outputCoreUser outputCoreSelected
      outputExtIssuer outputExtUser outputExtSelected,
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderCore.epk
        (action rho).transcript.isFlagged
        senderCoreIssuer senderCoreUser senderCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderExt.epk
        (action rho).transcript.isFlagged
        senderExtIssuer senderExtUser senderExtSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputCore.epk
        (action rho).transcript.isFlagged
        outputCoreIssuer outputCoreUser outputCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputExt.epk
        (action rho).transcript.isFlagged
        outputExtIssuer outputExtUser outputExtSelected := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt, _, _⟩
  exact
    ⟨senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt⟩

/-- `DEC-EPHEMERAL-PUBLIC-KEY-ENCODING` for the exact deployed relation. -/
theorem specification_dec_ephemeral_public_key_encoding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.Decaf.compressesTo
        (action rho).transcript.senderCore.epk
        (action rho).transcript.senderCore.epkEncoding ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).transcript.senderExt.epk
        (action rho).transcript.senderExt.epkEncoding ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).transcript.outputCore.epk
        (action rho).transcript.outputCore.epkEncoding ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).transcript.outputExt.epk
        (action rho).transcript.outputExt.epkEncoding := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, h0, h1, h2, h3, _⟩
  exact ⟨h0, h1, h2, h3⟩

/-- `DEC-SHARED-SECRET-DERIVATION` for the exact deployed relation. -/
theorem specification_dec_shared_secret_derivation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderCoreIssuer senderCoreUser senderCoreSelected
      senderExtIssuer senderExtUser senderExtSelected
      outputCoreIssuer outputCoreUser outputCoreSelected
      outputExtIssuer outputExtUser outputExtSelected,
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderCore.epk
        (action rho).transcript.isFlagged
        senderCoreIssuer senderCoreUser senderCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderExt.epk
        (action rho).transcript.isFlagged
        senderExtIssuer senderExtUser senderExtSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputCore.epk
        (action rho).transcript.isFlagged
        outputCoreIssuer outputCoreUser outputCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputExt.epk
        (action rho).transcript.isFlagged
        outputExtIssuer outputExtUser outputExtSelected := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt, _, _⟩
  exact
    ⟨senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt⟩

/-- `DEC-INCOMING-VIEWING-KEY-NONZERO` for the exact deployed relation. -/
theorem specification_dec_incoming_viewing_key_nonzero
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.Decaf.incomingViewingKeyNonzero
      (action rho).authorization.ivkReduced := by
  exact (relationCanonicalSender rho h).2

/-- `DEC-TRANSMISSION-KEY-DERIVATION` for the exact deployed relation. -/
theorem specification_dec_transmission_key_derivation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ computedTransmission,
      Protocol.Common.Decaf.diversifiedTransmissionKey
        (action rho).authorization.nullifierKey
        (action rho).authorization.authorizationKey
        (action rho).sender.diversifiedGenerator
        (action rho).authorization.ivkReduced
        (action rho).authorization.ivkQuotientA
        computedTransmission ∧
      Protocol.Common.Decaf.equivalent
        computedTransmission (action rho).sender.transmission := by
  exact (relationCanonicalSender rho h).1.2.2.2

/-- `DEC-TRANSMISSION-KEY-ENCODING` for the exact deployed relation. -/
theorem specification_dec_transmission_key_encoding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.Decaf.compressesTo
        (action rho).sender.transmission
        (action rho).sender.transmissionEncoding ∧
      Protocol.Common.Decaf.compressesTo
        (action rho).receiver.note.owner.transmission
        (action rho).receiver.note.owner.transmissionEncoding := by
  have hs := (relationCanonicalSender rho h).1.2.1
  have hr := (relationReceiverOutput rho h).2.1
  exact ⟨hs.1.2.2, hr.1.2.2⟩

/-- `DEC-TRANSMISSION-KEY-NONIDENTITY` for the exact deployed relation. -/
theorem specification_dec_transmission_key_nonidentity
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.Decaf.transmissionKeyNonIdentity
        (action rho).sender.transmission ∧
      Protocol.Common.Decaf.transmissionKeyNonIdentity
        (action rho).receiver.note.owner.transmission := by
  exact
    ⟨(relationCanonicalSender rho h).1.2.1.2,
      (relationReceiverOutput rho h).2.1.2⟩

/-- `DUMMY-AMOUNT-ZERO` for the exact deployed relation. -/
theorem specification_dummy_amount_zero
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∀ spend,
      (action rho).optional = .dummy spend → spend.amount = 0 := by
  intro spend selected
  have fact := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at fact
  rw [selected] at fact
  exact fact.1

/-- `DUMMY-NULLIFIER-DOMAIN-BINDING` for the exact deployed relation. -/
theorem specification_dummy_nullifier_domain_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∀ spend,
      (action rho).optional = .dummy spend →
      spend.nullifier =
        Poseidon377.hash3
          Protocol.Transfer.Concrete.syntheticDummyNullifierDomain
          spend.nullifierSeed spend.authRandomizer 1 := by
  intro spend selected
  have fact := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at fact
  rw [selected] at fact
  exact fact.2.2.2.1

/-- `DUMMY-SLOT-POSITION-BINDING` for the exact deployed relation. -/
theorem specification_dummy_slot_position_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∀ spend,
      (action rho).optional = .dummy spend →
      spend.nullifier =
        Poseidon377.hash3
          Protocol.Transfer.Concrete.syntheticDummyNullifierDomain
          spend.nullifierSeed spend.authRandomizer 1 := by
  intro spend selected
  have fact := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at fact
  rw [selected] at fact
  exact fact.2.2.2.1

/-- `FIELD-AUTH-RANDOMIZER-RANGE` for the exact deployed relation. -/
theorem specification_field_auth_randomizer_range
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).required.authRandomizer.val < 2 ^ 251 ∧
      (∀ spend,
        (action rho).optional = .real spend →
          spend.authRandomizer.val < 2 ^ 251) ∧
      (∀ spend,
        (action rho).optional = .dummy spend →
          spend.authRandomizer.val < 2 ^ 251) := by
  have required := relationRequiredSpend rho h
  rcases required with
    ⟨_, _, requiredBound, _, _, _, _, _, _⟩
  refine ⟨requiredBound, ?_, ?_⟩
  intro spend
  intro selected
  have optional := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at optional
  rw [selected] at optional
  exact optional.2.2.1
  intro spend selected
  have optional := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at optional
  rw [selected] at optional
  exact optional.2.2.1

/-- `FIELD-BALANCE-BLINDING-RANGE` for the exact deployed relation. -/
theorem specification_field_balance_blinding_range
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).balanceBlinding.val < 2 ^ 251 := by
  exact (relationBalance rho h).2.2.2.2.1

/-- `FIELD-DETECTION-RESERVED-ZERO` for the exact deployed relation. -/
theorem specification_field_detection_reserved_zero
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderCoreIssuer senderCoreIssuerFq,
      Protocol.Common.Decaf.compressesTo
        senderCoreIssuer senderCoreIssuerFq ∧
      (action rho).transcript.detectionCiphertext 3 =
        Protocol.Transfer.Concrete.streamBlock
          (Poseidon377.hash2
            Protocol.Transfer.Concrete.issuerDetectionDomain
            senderCoreIssuerFq
            (action rho).transcript.senderCore.epkEncoding) 3 := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      senderCoreIssuer, _, _, _, _, _, _, _, _, _, _, _,
      _, _, _, _, hcrypto, _⟩
  rcases hcrypto with
    ⟨senderCoreIssuerFq, _, _, _, _,
      hcompress, _, _, _, _, hdetection, _, _, _, _⟩
  refine ⟨senderCoreIssuer, senderCoreIssuerFq, hcompress, ?_⟩
  simpa [Protocol.Transfer.Concrete.detectionEncryption] using
    hdetection.2.2.2

/-- `FIELD-EPHEMERAL-SCALAR-RANGE` for the exact deployed relation. -/
theorem specification_field_ephemeral_scalar_range
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ senderCoreIssuer senderCoreUser senderCoreSelected
      senderExtIssuer senderExtUser senderExtSelected
      outputCoreIssuer outputCoreUser outputCoreSelected
      outputExtIssuer outputExtUser outputExtSelected,
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderCore.epk
        (action rho).transcript.isFlagged
        senderCoreIssuer senderCoreUser senderCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.senderRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).senderCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.senderExt.epk
        (action rho).transcript.isFlagged
        senderExtIssuer senderExtUser senderExtSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRCore
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputCore.epk
        (action rho).transcript.isFlagged
        outputCoreIssuer outputCoreUser outputCoreSelected ∧
      Protocol.Transfer.Concrete.sharedSecrets
        (action rho).transcript.outputRExt
        (Protocol.Transfer.Concrete.ack
          (Protocol.Transfer.Concrete.effectiveRingPk (action rho))
          (action rho).receiverCompliance.d)
        (Protocol.Transfer.Concrete.effectiveDkPub (action rho))
        (action rho).transcript.outputExt.epk
        (action rho).transcript.isFlagged
        outputExtIssuer outputExtUser outputExtSelected := by
  rcases relationComplianceTranscript rho h with
    ⟨_, _, _, _, _, _, _, _, _, _, _, _,
      senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt, _, _⟩
  exact
    ⟨senderCoreIssuer, senderCoreUser, senderCoreSelected,
      senderExtIssuer, senderExtUser, senderExtSelected,
      outputCoreIssuer, outputCoreUser, outputCoreSelected,
      outputExtIssuer, outputExtUser, outputExtSelected,
      hsenderCore, hsenderExt, houtputCore, houtputExt⟩

/-- `FIELD-USER-DERIVATION-RANGE` for the exact deployed relation. -/
theorem specification_field_user_derivation_range
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).senderCompliance.d.val < 2 ^ 251 ∧
      (action rho).receiverCompliance.d.val < 2 ^ 251 := by
  exact
    ⟨(relationSenderCompliance rho h).2.2.1,
      (relationReceiverCompliance rho h).2.2.1⟩

/-- `FIELD-USER-POSITION-RANGE` for the exact deployed relation. -/
theorem specification_field_user_position_range
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).senderCompliance.position.val < 2 ^ 32 ∧
      (action rho).receiverCompliance.position.val < 2 ^ 32 := by
  exact
    ⟨(relationSenderCompliance rho h).2.2.2.1,
      (relationReceiverCompliance rho h).2.2.2.1⟩

/-- `NOTE-OUTPUT-ASSET-BINDING` for the exact deployed relation. -/
theorem specification_note_output_asset_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).receiver.note.assetId = (action rho).assetId ∧
      (action rho).change.note.assetId = (action rho).assetId := by
  exact
    ⟨(relationReceiverOutput rho h).1,
      (relationChangeOutput rho h).2.1⟩

/-- `ROUTING-PARAMETERS` for the exact deployed relation. -/
theorem specification_routing_parameters
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Seg76.contract.spec rho ∧
      Seg77.contract.spec rho ∧
      Seg78.contract.spec rho := by
  have facts := transfer_circuitFacts rho h
  exact
    ⟨facts.statementBinding.RoutingPrecisionSelectSeg76,
      facts.statementBinding.RoutingParametersHashSeg77,
      facts.statementBinding.RoutingParametersBindSeg78⟩

/-- `NOTE-OUTPUT-OWNER-BINDING` for the exact deployed relation. -/
theorem specification_note_output_owner_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Transfer.Concrete.noteCommitment
        (action rho).receiver.note ∧
      (action rho).change.note.owner = (action rho).sender ∧
      Protocol.Transfer.Concrete.noteCommitment
        (action rho).change.note := by
  exact
    ⟨(relationReceiverOutput rho h).2.2.1,
      (relationChangeOutput rho h).1,
      (relationChangeOutput rho h).2.2⟩

/-- `NOTE-RECEIVER-AMOUNT-NONZERO` for the exact deployed relation. -/
theorem specification_note_receiver_amount_nonzero
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).receiver.note.amount ≠ 0 := by
  exact (relationReceiverOutput rho h).2.2.2

/-- `NOTE-SPEND-ASSET-BINDING` for the exact deployed relation. -/
theorem specification_note_spend_asset_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).required.note.assetId = (action rho).assetId ∧
      ∀ spend,
        (action rho).optional = .real spend →
          spend.note.assetId = (action rho).assetId := by
  rcases relationRequiredSpend rho h with
    ⟨_, requiredAsset, _, _, _, _, _, _, _⟩
  refine ⟨requiredAsset, ?_⟩
  intro spend selected
  have optional := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at optional
  rw [selected] at optional
  exact optional.2.1

/-- `ROUTING-TAG-DERIVATION` for the exact deployed relation. -/
theorem specification_routing_tag_derivation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Seg79.contract.spec rho ∧
      Seg80.contract.spec rho ∧
      Seg81.contract.spec rho ∧
      Seg82.contract.spec rho ∧
      Seg83.contract.spec rho ∧
      Seg84.contract.spec rho ∧
      Seg85.contract.spec rho ∧
      Seg86.contract.spec rho ∧
      Seg87.contract.spec rho ∧
      Seg88.contract.spec rho ∧
      Seg89.contract.spec rho ∧
      Seg90.contract.spec rho := by
  have facts := transfer_circuitFacts rho h
  exact
    ⟨facts.statementBinding.RoutingRouteWordSeg79,
      facts.statementBinding.RoutingRouteWordSeg80,
      facts.statementBinding.RoutingPermutationHashSeg81,
      facts.statementBinding.RoutingPermutationComposeSeg82,
      facts.statementBinding.RoutingTagPublicRangeSeg83,
      facts.statementBinding.RoutingTagRouteBitsSeg84,
      facts.statementBinding.RoutingTagRandomWordSeg85,
      facts.statementBinding.RoutingTagComposeSeg86,
      facts.statementBinding.RoutingTagPublicRangeSeg87,
      facts.statementBinding.RoutingTagRouteBitsSeg88,
      facts.statementBinding.RoutingTagRandomWordSeg89,
      facts.statementBinding.RoutingTagComposeSeg90⟩

/-- `NOTE-SPEND-COMMITMENT` for the exact deployed relation. -/
theorem specification_note_spend_commitment
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Transfer.Concrete.noteCommitment
        (action rho).required.note ∧
      ∀ spend,
        (action rho).optional = .real spend →
          Protocol.Transfer.Concrete.noteCommitment spend.note := by
  rcases relationRequiredSpend rho h with
    ⟨_, _, _, requiredCommitment, _, _, _, _, _⟩
  refine ⟨requiredCommitment, ?_⟩
  intro spend selected
  have optional := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at optional
  rw [selected] at optional
  exact optional.2.2.2.1

/-- `NOTE-SPEND-OWNER-BINDING` for the exact deployed relation. -/
theorem specification_note_spend_owner_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).required.note.owner = (action rho).sender ∧
      ∀ spend,
        (action rho).optional = .real spend →
          spend.note.owner = (action rho).sender := by
  refine ⟨(relationRequiredSpend rho h).1, ?_⟩
  intro spend selected
  have optional := relationOptionalSpend rho h
  unfold Protocol.Transfer.Concrete.optionalSpend at optional
  rw [selected] at optional
  exact optional.1

/-- `USER-COMPLIANCE-LEAF-HASH` for the exact deployed relation. -/
theorem specification_user_compliance_leaf_hash
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ((action rho).assetProof.isRegulated = 1 →
        Protocol.Common.quadMember (action rho).complianceAnchor
          (Protocol.Transfer.Concrete.complianceLeafHash
            (action rho).senderCompliance)
          (action rho).senderCompliance.path
          (action rho).senderCompliance.position) ∧
      ((action rho).assetProof.isRegulated = 1 →
        Protocol.Common.quadMember (action rho).complianceAnchor
          (Protocol.Transfer.Concrete.complianceLeafHash
            (action rho).receiverCompliance)
          (action rho).receiverCompliance.path
          (action rho).receiverCompliance.position) := by
  exact
    ⟨fun hreg => ((relationSenderCompliance rho h).2.2.2.2 hreg).1,
      fun hreg => ((relationReceiverCompliance rho h).2.2.2.2 hreg).1⟩

/-- `USER-COMPLIANCE-MEMBERSHIP-GATE` for the exact deployed relation. -/
theorem specification_user_compliance_membership_gate
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ((action rho).assetProof.isRegulated = 1 →
        Protocol.Common.quadMember (action rho).complianceAnchor
          (Protocol.Transfer.Concrete.complianceLeafHash
            (action rho).senderCompliance)
          (action rho).senderCompliance.path
          (action rho).senderCompliance.position) ∧
      ((action rho).assetProof.isRegulated = 1 →
        Protocol.Common.quadMember (action rho).complianceAnchor
          (Protocol.Transfer.Concrete.complianceLeafHash
            (action rho).receiverCompliance)
          (action rho).receiverCompliance.path
          (action rho).receiverCompliance.position) := by
  exact
    ⟨fun hreg => ((relationSenderCompliance rho h).2.2.2.2 hreg).1,
      fun hreg => ((relationReceiverCompliance rho h).2.2.2.2 hreg).1⟩

/-- `USER-LEAF-ADDRESS-BINDING` for the exact deployed relation. -/
theorem specification_user_leaf_address_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).senderCompliance.address = (action rho).sender ∧
      (action rho).receiverCompliance.address =
        (action rho).receiver.note.owner := by
  exact
    ⟨(relationSenderCompliance rho h).1,
      (relationReceiverCompliance rho h).1⟩

/-- `USER-LEAF-ASSET-BINDING` for the exact deployed relation. -/
theorem specification_user_leaf_asset_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    (action rho).senderCompliance.assetId = (action rho).assetId ∧
      (action rho).receiverCompliance.assetId = (action rho).assetId := by
  exact
    ⟨(relationSenderCompliance rho h).2.1,
      (relationReceiverCompliance rho h).2.1⟩

/-- `USER-LEAF-CANONICAL-DERIVATION-BINDING` for the exact deployed relation. -/
theorem specification_user_leaf_canonical_derivation_binding
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Transfer.Concrete.complianceLeafHash
          (action rho).senderCompliance =
        Poseidon377.hash5
          Protocol.Transfer.Concrete.complianceLeafDomain
          (action rho).senderCompliance.address.diversifiedGeneratorEncoding
          (action rho).senderCompliance.address.transmissionEncoding
          (action rho).senderCompliance.assetId
          (action rho).senderCompliance.d
          (action rho).senderCompliance.status ∧
      Protocol.Transfer.Concrete.complianceLeafHash
          (action rho).receiverCompliance =
        Poseidon377.hash5
          Protocol.Transfer.Concrete.complianceLeafDomain
          (action rho).receiverCompliance.address.diversifiedGeneratorEncoding
          (action rho).receiverCompliance.address.transmissionEncoding
          (action rho).receiverCompliance.assetId
          (action rho).receiverCompliance.d
          (action rho).receiverCompliance.status := by
  exact ⟨rfl, rfl⟩

/-- `VALUE-AMOUNT-128-RANGE` for the exact deployed relation. -/
theorem specification_value_amount_128_range
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.amount128 (action rho).required.note.amount ∧
      Protocol.Common.amount128 (action rho).optional.amount ∧
      Protocol.Common.amount128 (action rho).receiver.note.amount ∧
      Protocol.Common.amount128 (action rho).change.note.amount := by
  have hb := relationBalance rho h
  exact ⟨hb.1, hb.2.1, hb.2.2.1, hb.2.2.2.1⟩

/-- `VALUE-CONSERVATION` for the exact deployed relation. -/
theorem specification_value_conservation
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    ∃ balanceCommitment,
      Protocol.Common.Decaf.netBalanceCommitment2
        (action rho).required.note.amount
        (action rho).optional.amount
        (action rho).receiver.note.amount
        (action rho).change.note.amount
        (action rho).assetId (action rho).balanceBlinding
        balanceCommitment ∧
      Protocol.Common.Decaf.compressesTo
        balanceCommitment (action rho).balanceCommitmentEncoding := by
  exact (relationBalance rho h).2.2.2.2.2

/-- `VALUE-THRESHOLD-128-RANGE` for the exact deployed relation. -/
theorem specification_value_threshold_128_range
    (rho : Nat → SemanticF)
    (h : relationAll rho) :
    Protocol.Common.amount128 (action rho).assetProof.leaf.threshold := by
  rcases relationComplianceTranscript rho h with
    ⟨hthreshold, _, _, _, _, _, _, _, _, _, _, _, _⟩
  exact hthreshold

end Shieldd.GnarkFormal.Deployed.Contracts.Transfer
