# Circuit Soundness Findings

| ID | Severity | Finding | Evidence | Required mitigation | Status |
| --- | --- | --- | --- | --- | --- |
| `ZK-FIND-GO-UNREGULATED-NONMEMBERSHIP` | High | Go transfer and shielded ICS-20 circuits do not prove asset IMT non-membership on the unregulated branch. Rust proves membership or gap via `is_regulated.select(is_exact_match, is_in_gap)`, but Go uses `AssertEqualIf` and is vacuous when `IsRegulated=false`. | Rust `verify_asset_registry_imt` lines 797-815; Go `transfer_circuit.go` line 256 and `shielded_ics20_withdrawal_circuit.go` line 170. | Resolved: Go `VerifyAssetRegistryIMT` mirrors Rust via `Select(IsRegulated, isExactMatch, isInGap)` through AssetRegistryGap in both circuits; `transfer_metamorphic_test.go` rejects a regulated asset routed through the unregulated branch. | `resolved` |
| `ZK-FIND-WITHDRAWAL-EFFECT-HASH-EXTERNAL` | High | The old two-field encoding reduced two 256-bit halves modulo Fq and was not injective. | Rust now splits the 64-byte action effect hash into four 128-bit limbs; Go consumes all four under the version-3 statement domain. Rust/Go differential and semantic-mutation tests cover the exact order. | Resolved by the byte-injective limb encoding and domain/witness version bumps. | `resolved` |
| `ZK-FIND-DUMMY-NULLIFIER-DOMAIN` | Low | Dummy nullifiers rely on family-separated Poseidon domains. Exact circuit refinements prove the deployed derivations, but domain separation does not make the finite output ranges mathematically disjoint; a synthetic/real equality remains possible with negligible collision probability. | `NoteReshapePadder::synthetic_dummy_nullifier`; shielded Withdrawal and Transfer plans use separate labels; `ZK-ASSUME-SYNTHETIC-NULLIFIER-CROSS-DOMAIN-CR` records the computational boundary. | Prove or cite multi-domain collision resistance for the exact Poseidon377 construction and carry the negligible failure probability through the protocol result. Do not claim unconditional range disjointness. | `accepted-risk` |
| `ZK-FIND-RUST-GO-FIELD-CORRESPONDENCE` | Medium | The Go ABI only length-checked a serialized `StatementFields` copy, which did not constrain the assignment and could differ without changing the proof. | Transfer and withdrawal no longer serialize the duplicate vector. Go reconstructs every statement field from the actual assignment inputs, hashes them natively, and requires the result to equal Rust's claimed hash; NoteReshape has the same reconstructed-hash differential. | Resolved at the deployed language seam. Full source-level encoder injectivity remains tracked separately from this concrete correspondence check. | `resolved` |
| `ZK-FIND-PROOF-BOUND-EFFECT-FILTERING` | Critical | Transfer and withdrawal handlers used unproved encrypted-body sentinels to suppress proof-bound nullifier and output writes, allowing a real nullifier to remain reusable. | All fixed-shape handlers now call `execute_proof_bound_effects`; serial/parallel projections, fee funding, discovery precision, and plans include every proof-bound slot. Regressions use body values that still satisfy the former dummy classifier. | Resolved by deleting consensus filtering and persisting every fixed proof-bound effect. | `resolved` |
| `ZK-FIND-PROOF-BOUND-EFFECT-ATOMICITY` | High | A check-all-then-nullify-one-at-a-time implementation could discover a duplicate input only after staging the first nullifier, relying on outer transaction rollback for safety. | `execute_proof_bound_effects` now calls the SCT's duplicate-checking `nullify_all` once; the duplicate regression asserts rejection before any pending nullifier is staged. | Resolved by making proof-bound nullifier insertion an atomic batch operation. | `resolved` |
| `ZK-FIND-WITHDRAWAL-TRANSMISSION-BINDING` | Critical | Withdrawal spends and change commitments accepted a scalar transmission encoding unrelated to the DTK-bound affine transmission point. | Shared withdrawal context now compresses the DTK-bound sender point once and constrains every spend/change `TransmissionKeyS` to it. A forged-change regression failed before the fix and now rejects. | Resolved by the direct scalar-to-DTK binding. | `resolved` |
| `ZK-FIND-PROOF-ENCODING-CANONICAL` | High | Transfer, NoteReshape, and withdrawal proof decoders accepted variable-length or non-canonical Arkworks encodings. | The shared decoder requires exactly 192 bytes, consumes the full input, and requires canonical compressed reserialization. Tests reject short, trailing, and alternate-infinity encodings. | Resolved by one fail-closed decoder used at protobuf admission and verification. | `resolved` |
| `ZK-FIND-TRANSFER-FALSE-CERTIFICATION` | Critical | Transfer was labelled whole-circuit/full even though its abstract theorem and handwritten wiring snapshot did not refine the deployed relation into independent protocol semantics. | The false path was deleted and V17 was rebuilt from exact rows and independent semantics. V18 changes the relation for nullifier-history classification and is explicitly a candidate with no inherited theorem root. | Keep V18 candidate until its exact relation is independently recertified. | `resolved` |
| `ZK-FIND-TRANSFER-COMPLIANCE-NONCE-REUSE` | High | Compliance enrichment reused one transaction blinding nonce across sibling Transfer actions and internal fee funding. Because each Transfer deterministically expands that root into its salts, tier seeds, and ephemeral scalars, reuse repeated EPK and stream-key material across separately serialized ciphertexts and broke the honest-construction confidentiality premise. | `enrich_plan_with_compliance` now allocates one CSPRNG nonce per shielded action and a separate fee-funding nonce, carries each root only into its matching action, and tracks all allocated encodings in one transaction-local set. `fresh_action_nonce` fails closed on a collision; `transfer_compliance_nonce_allocator_rejects_cross_action_reuse` exercises the collision path and `compliance_enrichment_uses_one_fresh_nonce_per_transfer_action` proves sibling and fee-funding roots are pairwise separated while every fixed slot within one action still agrees. | Resolved by making action-level nonce allocation explicit and collision-checked. Entropy and malicious-sender disclosure remain named computational privacy premises, not R1CS or consensus claims. | `resolved` |
| `ZK-FIND-WITHDRAWAL-FV-MISSING` | High | Withdrawal had no independent protocol semantics or exact deployed proof chain. | Withdrawal V9 completed the exact-row certification architecture. V10 changes the relation for nullifier-history classification and is explicitly a candidate with no inherited theorem root. | Keep V10 candidate until its exact relation is independently recertified. | `resolved` |
| `ZK-FIND-WITHDRAWAL-CONSERVATION-BRIDGE-ARITY` | High | Withdrawal conservation was classified as the existing two-input/one-output net-balance template even though the deployed segment consumes required and optional inputs plus both change and outbound withdrawal outputs. Reusing the narrower bridge would omit the outbound-value leg from the formal interpretation. | The trace now emits the distinct `decaf.conservation_net_balance_commitment2` operation. Its reviewed 2,322-row normalized relation has a dedicated Go parity mirror and exact `ConservationNetBalanceCommitment2Bridge`; the kernel-checked theorem proves four 128-bit ranges, `required + optional = change + withdrawal`, 251-bit blinding, and the blinding-only commitment. A classifier regression rejects the old 2→1 label. | Resolved by giving the deployed 2→2 relation its own authenticated template identity and semantic bridge. | `resolved` |
| `ZK-FIND-FV-SEMANTIC-LABEL-ALIAS` | High | The manifest classifier accepted broad operation prefixes as structural glue and reused full-gadget theorem names for composite bodies whose globally deduplicated Boolean or curve rows lived in separate segments. A mislabeled segment could therefore select a theorem stronger than its exact rows. | Structural operations use an exact closed allowlist. Deployed detection/amount/address encryption bodies have distinct body predicates and theorem names; ACK/shared-secret bodies require explicit on-curve premises supplied by separately classified curve segments. Standalone DLEQ providers are inventory-gated research artifacts and are not selectable by V18 Transfer. Go manifest regressions pin every exact label/theorem/count and reject unknown prefixes or body-as-full aliases. | Resolved by making semantic labels identify exact row presentations and composing omitted global seams explicitly. | `resolved` |
| `ZK-FIND-PADDED-AUTH-HASH-BINDING` | High | NoteReshape padded-slot signatures were derived over the action-body hash, while stateless verification checks every slot against the transaction effect hash. Padded 4→1/8→1 actions assembled through the transaction plan could therefore fail authorization. | `TransactionPlan::apply_auth_data` now replaces real and synthetic NoteReshape signatures together, deriving each padded signature from the final transaction effect hash exactly as Transfer and withdrawal do. `apply_auth_data_rebinds_note_reshape_dummy_signatures_to_transaction_hash` verifies every padded RK/signature pair against that hash. | Resolved by making transaction assembly the single hash-binding point for every fixed-shape spend signature. | `resolved` |
| `ZK-FIND-FV-PROMOTION-FAIL-OPEN` | Critical | The profile catalog could label an unsupported circuit `certified` using an existing Lean path and hand-authored zero-counter report, removing it from the candidate workflow without adding it to the exact Lean backend. | `check-fv-profiles.py` requires the complete runtime registry, exact kind/shape, a backend-registry theorem, canonical coverage root, exact build/axiom targets, and artifact closure for every certified family. Candidate profiles must expose null theorem/coverage roots. Mutation tests reject unsupported self-certification and partial roots. | Resolved by closing certification over the explicit four-family backend registry. | `resolved` |
| `ZK-FIND-COMPLIANCE-POSITION-ALIAS` | Medium | Compliance quad-path verification decomposed a 64-bit position but consumed only the low 32 path bits, allowing multiple non-canonical witness positions to authenticate the same leaf path. | `VerifyQuadPath` now decomposes exactly `2 * ComplianceQuadTreeDepth` bits. The regression accepts the canonical position and rejects the same position with bit 32 set; the 48-bit TCT path has a matching upper-bit rejection test. | Resolved by exact-width position decomposition and boundary regressions for both path gadgets. | `resolved` |
| `ZK-FIND-FV-APPLICABILITY-GAP` | High | The formal workflow classified shielded-pool and gnark changes but could skip transaction authorization, app consensus execution, and view-side plan construction even though all three participate in the circuit soundness postcondition. | The gate now includes the local package closures for transaction, app, and view plus explicit action-handler, transaction-extraction, and wallet-planner paths. Applicability regressions exercise every root. | Resolved by following the FV impact boundary through proving, authorization, consensus execution, and client construction. | `resolved` |
| `ZK-FIND-REQUIRED-SLOT-DUMMY-SURFACE` | High | Transfer and withdrawal modeled every fixed input with a dummy selector even though protocol construction requires at least one real spend. This enlarged the relation and left the first slot's real-spend requirement implicit in planner behavior. | Both circuits, Rust private records, witness encoders, Go decoders, and assignments now use a dedicated required spend plus one optional dummy-capable spend. Required records contain no selector or dummy seed, and legacy wire layouts are rejected. | Resolved by making the minimum-real-input invariant structural in the circuit ABI. | `resolved` |
| `ZK-FIND-DUPLICATE-ADDRESS-WITNESSES` | Medium | Spend, receiver, and change records repeated sender/recipient affine and compressed address representations already present in shared context. The extra copies added constraints and created opportunities for split bindings. | Required/optional spends derive their note address from the shared sender; the receiver carries one canonical recipient representation; change derives the sender address and no longer repeats a recipient path. Withdrawal spend/change records use the same shared derivation. | Resolved by specializing slim required, optional, receiver, and change records and deleting duplicate serialized fields. | `resolved` |
| `ZK-FIND-WITNESS-ABI-MALLEABILITY` | Medium | The binary ABI admitted multiple byte encodings of one circuit assignment: non-boolean bytes, short paths that were zero-padded, field values reduced modulo Fq, noncanonical Fr scalars, oversized withdrawal limbs, and ignored duplicate statement/balance/indexed-key fields. This weakened witness-hash receipts and made parser parity ambiguous. | Shared Rust and Go decoders require canonical booleans, exact path depths and sibling counts, canonical Fq/affine encodings, typed Fr values, and 128-bit effect limbs. Ignored copies were deleted; proof-result field coordinates/hashes are checked rather than reduced. Alias and short-path regressions cover both language boundaries. | Resolved by making the current witness formats one-to-one with decoded typed values and rejecting all legacy layouts. | `resolved` |
| `ZK-FIND-HIDDEN-ARITY-SENTINELS` | Medium | Fixed-shape padding used empty encrypted backreferences and all-zero wrapped keys, publicly revealing which Transfer, withdrawal, and NoteReshape slots were synthetic despite the hidden-arity design. | Synthetic inputs now carry ordinary fixed-length encrypted backreferences; synthetic outputs use ordinary note/memo-key encryption. Public dummy classifiers and observer-side filtering were deleted; zero-value and private selector constraints remain the only padding semantics. | Resolved by making body encodings indistinguishable at the public format layer while retaining fixed proof-bound effects. | `resolved` |
| `ZK-FIND-ACTION-ANCHOR-DIVERGENCE` | Medium | Per-action body anchors were excluded from action effect hashes and ignored during proof extraction, so they could diverge from the transaction anchor without affecting authorization or proof verification. | Stateless extraction now requires every Transfer, NoteReshape, and withdrawal body anchor to equal `TransactionContext.anchor`; proofs continue to use that single transaction anchor. | Resolved without changing the protocol message by turning the retained field into a checked copy. | `resolved` |
| `ZK-FIND-UNAUTHENTICATED-ACTION-API` | Low | Planner methods named `transfer`/`shielded_ics20_withdrawal` returned fixed-shape actions with placeholder padded signatures that cannot pass stateless authorization until transaction assembly replaces them. | These constructors are explicitly named `build_unauth_*`; transaction assembly remains the only completion point for padded signatures over the final transaction effect hash. Old complete-action-looking entry points were removed. | Resolved by making the intermediate authentication state explicit in the API. | `resolved` |
| `ZK-FIND-OUTGOING-VIEW-COMMITMENT-CONTEXT` | Medium | Transfer outputs and withdrawal change wrapped outgoing-view keys with per-output or independently recomputed commitment contexts, while transaction scanning unwraps every key with the serialized aggregate action balance commitment. Valid outputs could therefore become unrecoverable to the sender. | All three families now compute the aggregate commitment once and pass that exact value to every real and synthetic output-key wrapper. Regressions unwrap keys with each serialized body commitment. | Resolved by making outgoing recovery context an explicit family-wide invariant. | `resolved` |
| `ZK-FIND-FORMAL-STAMP-DRIFT` | Medium | Alloy and Tamarin runners overwrote committed evidence on ordinary execution, so CI could silently bless a changed model or output instead of detecting stale reviewed evidence. | Both runners now default to `check`, generate evidence in temporary files, and byte-compare it and its sidecar with the committed stamp; mutation requires explicit `update`. The soundness invariant gate independently checks each stamped model path and model hash. | Resolved by separating verification from an explicit reviewed update operation. | `resolved` |
| `ZK-FIND-CONSENSUS-EXTRACTOR-CHECK-BYPASS` | Critical | The app's proof-artifact extraction path collected Transfer, withdrawal, NoteReshape, and fee-funding proof items without composing the action's full stateless checks, so the proposal fast path could admit artifacts whose action anchor, authorization signatures, or transport were invalid. | `collect_consensus_proof_items_with_artifacts` now uses the combined stateless-validation/extraction helpers for every shielded family. Only the checked combined helpers remain publicly exported; raw public extraction and proof-to-batch APIs were removed. Regressions reject mismatched action anchors, invalid spend authorization, and malformed Transfer proof transport while accepting a valid fixed-shape transaction. | Resolved by making proof extraction an output of successful action validation and closing the lower-level public footgun. | `resolved` |
| `ZK-FIND-TRANSFER-COMPLIANCE-BUNDLE-DECODING` | High | The retired Transfer upload bundle admitted a large parsing and cryptographic-validation surface, including envelopes, public shared points, and DLEQ scalars that were not needed by consensus execution. | Transfer V18 contains no bundle in the witness, statement, protobuf, evidence object, scanner, or accepted-language model. The fixed ciphertext and metadata records have one canonical decoder, and any PRE/upload material is rejected. | Resolved by deleting the unsafe transport surface instead of preserving a second validator. | `resolved` |
| `ZK-FIND-TRANSFER-CIPHERTEXT-FIELD-ALIASES` | Medium | Transfer compliance framing checked canonical `c2` fields but accepted arbitrary 32-byte detection and tier-ciphertext words, reducing them modulo Fq when reconstructing the proof statement. Multiple body byte strings could therefore project to one circuit assignment despite the external canonical-decoding premise. | `TransferComplianceCiphertext::from_bytes` now checks all 12 ciphertext words with `Fq::from_bytes_checked` before retaining their bytes. A word-by-word regression replaces each canonical word with an out-of-range encoding and requires rejection. | Resolved by making the complete proof-bound compliance ciphertext a canonical typed field encoding at admission. | `resolved` |
| `ZK-FIND-WITHDRAWAL-PLAN-INVARIANT-DRIFT` | Medium | Withdrawal plans duplicated public inputs in a cached body and cached balance. Direct protobuf admission and wallet construction could leave anchors, timestamps, compliance metadata, payload, balance commitment, spends, and outputs mutually inconsistent. | `ShieldedIcs20WithdrawalPlan` now persists only blinding, spends, optional change, and payload. It validates canonical facts and derives the fixed family, zero net balance, proof public input, and action body on demand. Removed protobuf tags are reserved and dependent callers/tests were updated. | Resolved by deleting the duplicate projections rather than retaining a refresh/reconciliation protocol. | `resolved` |
| `ZK-FIND-SHIELDED-PLAN-DECODE-CANONICALITY` | Medium | Shared shielded input/output plan decoding invented zero/default membership anchors, paths, indexed leaves, points, thresholds, salts, and then-required DLEQ scalars when protobuf fields were absent or malformed. Nested indexed-policy decoding similarly replaced omitted policy facts with unregulated defaults. A hostile persisted or RPC plan could alias a different witness or crash instead of failing validation. | Plan admission requires both anchors, both paths, the indexed leaf, exact canonical ring/detection points, threshold, salts, and every current V18 policy fact. All Fq/Fr/Decaf values use checked exact decoders returning typed errors. Retired DLEQ/shared-point fields were deleted rather than retained as optional aliases. Regressions delete every required field and inject invalid points/modulus aliases. | Resolved by making protobuf admission total, canonical, and faithful to the authoritative witness facts. | `resolved` |
| `ZK-FIND-COMPLIANCE-PATH-IMPLICIT-PADDING` | Medium | Unregistered/unregulated user-proof responses used zero-length Merkle paths and relied on Rust/Go witness encoders to pad them implicitly. This admitted multiple path shapes, broke the strict ABI, and let incomplete batch responses reach plan construction; batch response cardinality could also index past the query list. | `MerklePath` now has a canonical 16-layer/three-sibling default, validates exact shape and canonical Fq siblings at protobuf admission, and every RPC path parser rejects missing or malformed paths. Compliance enrichment rejects omitted asset proofs and mismatched batch cardinality instead of inventing defaults. App/view regressions cover unregulated Transfer and withdrawal construction under the strict ABI. | Resolved by making disabled-branch paths explicit and fixed-width across provider, RPC, plan, and witness boundaries. | `resolved` |
| `ZK-FIND-FV-CANDIDATE-MANIFEST-VALIDATION` | Medium | The candidate exporter intentionally emits an explicit `unclassified` segment, but the profile checker rejected that kind and did not independently recompute segment coverage and witness-wire totals. A correct candidate could not be regenerated, while malformed manifest accounting was insufficiently constrained. | The checker accepts `unclassified` only as an explicit allowed kind, requires exact fields and contiguous full constraint coverage, recomputes kind totals and unclassified counters, and validates contiguous unique witness wires against public/secret counts. Certified profiles still require zero unclassified constraints. | Resolved by validating candidate incompleteness precisely instead of either rejecting or ignoring it. | `resolved` |
| `ZK-FIND-FV-ARTIFACT-CANONICALITY` | High | PK/VK readers accepted a valid key prefix with arbitrary trailing bytes and gnark's alternate raw/uncompressed key encoding, while VK JSON accepted alternate encodings and modular coordinate aliases. Artifact receipts could therefore hash distinct byte strings that runtime tooling interpreted as the same key. | Central strict PK/VK readers require EOF and compare the input byte-for-byte with canonical `WriteTo` reserialization; they are used by gnarkctl, C-shared proving libraries, and proverdaemon. VK JSON has one canonical encoder/strict decoder and requires canonical in-range base-field decimals. Duplicate, unknown, compact, trailing, appended-key, modular-coordinate, negative-coordinate, and `WriteRawTo` regressions fail. | Resolved by making every receipt-bound key artifact one-to-one with its decoded value. | `resolved` |
| `ZK-FIND-FV-RUNTIME-REGISTRY-DIVERGENCE` | High | FV catalog completeness was checked against a handwritten Python registry and later only the generated Go family slices. Adding or changing a Rust domain/proof family without matching the Go projection could still escape the candidate/certified FV universe. | Versioned canonical family manifests now deterministically project and check the Go runtime, Rust domain, proof-parameter, and aggregation surfaces for NoteReshape, Transfer, and withdrawal. `check-fv-profiles.py` requires those projections plus witness ABI and FV-catalog label/shape parity. Mutation tests reject an unprofiled runtime family and cross-language projection drift. | Resolved by closing the family universe over every runtime and protocol projection, not one handwritten or language-local registry. | `resolved` |
| `ZK-FIND-SYMBOLIC-LEMMA-INVENTORY` | Medium | The Tamarin runner requested a handwritten lemma list but did not prove it was the exact set declared by each model; a newly declared, omitted, or prefix-colliding lemma could escape the release gate. | The runner parses literal `lemma NAME:` declarations, requires exact sorted inventory equality, and matches verified output on identifier boundaries. Tool-free self-tests reject omitted, undeclared, unparsable, and prefix-colliding cases. | Resolved by making the model's declared lemma set a closed release obligation. | `resolved` |
| `ZK-FIND-TRANSFER-PLAN-INVARIANT-DRIFT` | Low | Transfer protobuf admission persisted stale body/balance projections, and same-sender/address plus identical sender-compliance preconditions could fail only during later proving. | `TransferPlan` now persists only authoritative construction facts, validates shared sender/compliance invariants at admission, and derives balance and action body on demand. Removed protobuf tags are reserved; stale duplicate fields are not reconciled. | Resolved by deleting cached projections and making circuit preconditions part of typed plan admission. | `resolved` |
| `ZK-FIND-WITHDRAWAL-UNUSED-CIPHERTEXT` | Low | Withdrawal input bodies admitted nonempty Transfer compliance ciphertext bytes even though the planner clears them and neither the withdrawal statement nor circuit consumes them. Signatures bound the bytes, but the field was an unnecessary alternate payload surface. | Withdrawal stateless validation now requires every input compliance ciphertext to be empty before proof extraction; a planner-built action with a tampered field is rejected. | Resolved by rejecting the unused encoding at the action boundary. | `resolved` |
| `ZK-FIND-FV-METADATA-SCHEMA` | Medium | `circuit_metadata.json` had no declared schema; Go and Rust runtime parsing accepted field drift while the FV profile checker assumed an exact undocumented shape. Its single ambiguous VK hash field actually pinned JSON rather than the binary VK, and nondeterministic floating-point timing telemetry admitted semantically equivalent aliases. | Metadata now declares `shieldd.gnark.circuit_metadata.v2`, separately pins the exact SR1CS, PK, binary VK, JSON VK, and setup-provenance object, excludes timing telemetry, and uses strict canonical Go decoding plus unknown-field-denying Rust decoding. Proof receipt schema v4 binds the exact metadata hash, branch-labelled witness case, and provenance hash; all four deployed metadata files validate against their artifacts. Setup derivation and toxic-waste erasure remain explicitly outside this coherence claim. | Resolved by making metadata a deterministic, versioned, fail-closed artifact contract shared by runtime and FV gates. | `resolved` |
| `ZK-FIND-STALE-ASSET-POLICY-ANCHOR` | Critical | Transfer and Withdrawal accepted recent historical registry roots. After an asset policy or user status update, a prover could reuse the prior root until the history window expired. | `validate_compliance_anchor_facts` now requires exact equality with both `get_asset_imt_root` and `get_user_tree_root`. Async/sync prechecks use the same current roots, and Transfer, Withdrawal, and NoteReshape bind the corresponding statement roots. | Resolved by treating both policy and user-status trees as mutable admission state. | `resolved` |
| `ZK-FIND-TRANSFER-OFF-CURVE-BALANCE` | Medium | Transfer carried a second prover-chosen affine balance point and checked only Decaf cross-ratio equivalence, which accepts off-curve representatives such as `(0,0)`. The point was semantically dead—the statement already compressed the independently computed net-balance point—so it could not change an accepted body, but it enlarged the witness and created a misleading formal obligation. | Transfer V13 deleted the affine copy, its on-curve/equivalence checks, and 64 witness bytes; that historical V13 relation had 227,185 rows, seven fewer than the hardened duplicate-point design. V18 structural regressions still forbid either balance-point field, while statement reconstruction derives field 003 from the four amounts, asset ID, and action blinding and rejects range/hash drift. | Resolved by deleting the redundant representation and binding only the computed point through exact compression and the public statement hash. | `resolved` |
| `ZK-FIND-FV-REUSED-IR-PROJECTION` | High | Generation could reuse an IR file after checking broad circuit/hash/count fields and its segment projection, without authenticating derived classes, wire roles, relation fingerprints, proof-template identities, or template-equivalence wire/row mappings. A tampered IR or cached equivalence witness could therefore feed different generated Lean under the right whole-file identity. Semantic bindings are not projected into this IR; their generator consumes them directly from the manifest. | `load_bound_ir` now reconstructs the complete canonical IR from the supplied SR1CS, segment manifest, and reviewed proof-template registry, then requires the original artifact bytes to equal its canonical serialization. A cached equivalence witness is only an untrusted hint: the extractor resolves authenticated template metadata and freshly verifies normalized rows, seating, row permutations, L/R swaps, and nonzero scaling before use, with no fallback after a bad hint. Mutation regressions reject changed classes, roles, template identities, row mappings, payload bytes, unknown fields, and noncanonical JSON. | Resolved by authenticating a unique canonical encoding and freshly verifying every cached equivalence witness against the reviewed relation bytes. | `resolved` |
| `ZK-FIND-ZERO-WITHDRAWAL` | Medium | `Ics20Withdrawal::validate` accepted amount zero, allowing a proof-bound outbound packet/state transition with no value. | A failing regression established the old behavior. Validation now rejects `Amount::zero()` before proof extraction, and the independent Withdrawal external premise records nonzero amount together with the existing timeout rules. | Resolved by making positive outbound value a stateless payload invariant. | `resolved` |
| `ZK-FIND-DEPRECATED-COMPAT-NOOP` | Low | The deprecated `use_compat_address` protobuf bit was serialized and effect-hashed but had no domain/runtime behavior, creating two authorized payload encodings with identical execution semantics. | The external field number remains reserved by the protobuf interface, but domain decoding rejects `true`, domain encoding always emits `false`, and all internal constructors/callers dropped the field. | Resolved by rejecting the unused semantic branch instead of preserving a silent flag. | `resolved` |
| `ZK-FIND-WITHDRAWAL-TRANSPARENT-ADDRESS-ALIAS` | Low | `use_transparent_address=true` was effect-hashed as a distinct withdrawal body, but a return address without the required transparent components silently fell back to the ordinary packet sender encoding. Two authorized bodies could therefore project to identical packet semantics. | Withdrawal validation and protobuf admission reject transparent mode unless the return address has a canonical transparent encoding. Packet-data and IBC-packet construction are now fallible and revalidate the payload themselves, so a directly constructed invalid domain object also returns an error rather than relying on a prior caller check or fallback. Regressions cover direct validation, protobuf decoding, invalid direct packet encoding, and the exact transparent sender. | Resolved by making transparent mode a fail-closed semantic branch at every packet-construction entry point. | `resolved` |
| `ZK-FIND-FORMAL-EXTERNAL-BOUNDARY` | High | Earlier final refinements hid or omitted accepted-language checks: transaction binding signature, exact family/body/public projection, current-vs-historical registry roots, registry authorization, transaction-wide nullifier uniqueness, proof-bound persistence, exact multiplicity, and atomic transitions. An action-local circuit theorem and a free opaque transition predicate could be mistaken for the full transaction-handler language. | All three protocol models expose these checks as individually named external facts and typed action-local transitions; NoteReshape's opaque `transitionAccepted` and the synthetic outcome model were deleted. `exactExtension` preserves multiplicity, and every deployed root joins its exact target transition to an executor-supplied `CommittedTargetTransaction` whose complete nullifier/output lists are globally fresh, unique, and exact. The four-family obligation ledger, external map, generated consequence registry, and runtime tests distinguish exact circuit facts from caller-supplied executor premises. | Resolved by making every non-R1CS acceptance conjunct and the target-to-transaction join reviewable at the final theorem boundary. This is conditional evidence: Lean checks the supplied complete transaction envelope but does not derive that envelope from Rust's typed action list. | `resolved` |
| `ZK-FIND-PLAN-CACHED-PROJECTIONS` | Medium | NoteReshape, Transfer, and Withdrawal plans persisted placeholder/derived bodies and balances that build paths later ignored or recomputed, admitting contradictory serialized plans and multiplying refresh APIs. | All three schemas now persist authoritative construction facts only. Bodies/balances are derived, fixed family values are structural, removed tags are reserved, signing vectors are deterministically regenerated, and placeholder/refresh paths are deleted. | Resolved by removing the duplicated state rather than adding compatibility shims. | `resolved` |
| `ZK-FIND-TEMPLATE-MATCHER-QUADRATIC` | Medium | The reviewed-template equivalence matcher recomputed parsed row shapes and rescanned all deployed rows inside sort/search loops. Per-branch limits could restart across permutations, so the 17,834-row Transfer DLEQ presentation could consume effectively unbounded release time. | Row aggregates, shapes, frequencies, and shape-indexed candidate lists are precomputed once. One global bounded budget covers every permutation/scale branch, exhaustion is a typed failure with deterministic telemetry, and any returned or cached witness still receives fresh exact verification. Budget, tamper, identity, and presentation-mutated regressions cover the boundary. | Resolved by bounding the entire search and separating heuristic discovery from mandatory exact witness verification. | `resolved` |
| `ZK-FIND-HASH-BINDING-TRUST-BOUNDARY` | High | Exact row proofs showed that the circuit and runtime compute the same sole public hash, but the trust ledger did not state that binding a complete statement through one Poseidon field requires collision resistance. Withdrawal's four effect-hash limbs similarly prove full digest equality without proving BLAKE2b injectivity over effecting bodies. This allowed whole-circuit language to be read as unconditional body binding. | The authoritative ledger now names separate Poseidon public-input and typed BLAKE2b-512 effect-hash collision-resistance assumptions, with exact construction evidence and removal paths. The external map and generated four-family artifacts repeat the boundary; exact field/limb packing remains a proved fact rather than being folded into either assumption. | Resolved by separating exact transcript equality from the cryptographic collision-resistance needed to interpret it as message equality. | `resolved` |
| `ZK-FIND-TRANSFER-SHARED-POINT-DISCONNECT` | Medium | The retired design carried a DLEQ-package shared point independently from the `ACK * esk` secret used by encryption, creating a second representation that required an explicit join. | Transfer V18 contains no DLEQ package or public shared point. The four issuer/user secrets are computed internally, the flag selects one point, and the selected point alone feeds unconditional ciphertext relations. Structural regressions forbid the retired witness and statement fields. | Resolved by deleting the duplicate representation and its transport, not by retaining four permanent join gadgets. | `resolved` |
| `ZK-FIND-COMPLIANCE-EVIDENCE-CANONICALITY` | Medium | Older compliance evidence accepted noncanonical DLEQ scalars and loose discriminants, so distinct byte strings could decode to one typed object and validate against one payload hash. | Evidence v3 has an exact fixed codec over the output reference, asset/flag facts, ciphertext, and factored metadata; it contains no DLEQ scalar, PRE envelope, shared point, or loose optional branch. Validation compares the canonical accepted bytes and payload hash before state advancement. | Resolved by deleting the obsolete proof fields and making the remaining evidence transport one-to-one. | `resolved` |
| `ZK-FIND-DECAF-IDENTITY-OWNERSHIP-ALIAS` | Critical | All four deployed circuits omitted native Decaf non-identity checks for their authorization key and owner diversified generator; Transfer also omitted the receiver generator check. With an identity owner generator, every IVK derives the same identity transmission key, so a prover could open one anchored note commitment under distinct nullifier keys and produce distinct accepted nullifiers for the same note. Transfer's identity receiver branch could additionally create such an ownership-ambiguous output. Identity authorization keys separately widened every circuit language beyond the native key types. | `AssertDecafNonIdentity` enforces the exact on-curve Decaf identity-class predicate `x != 0`. NoteReshape and Withdrawal attack reproductions each demonstrate one anchored commitment with multiple nullifiers before the guard; the Transfer reproduction covers the same relation. Full-circuit negative tests mutate authorization and diversified generators for all four profiles. The exact trace contracts, `Common.Decaf.nonIdentity`, family concrete semantics, security consequences, and deployed roots carry each required row through Lean. | Resolved in every deployed circuit relation rather than relying on honest Rust construction or registry admission. Keep authorization and owner-generator rows in all four authenticated contracts and Transfer's receiver-generator row; they jointly prevent creation and later repeated spending of identity-owned notes. | `resolved` |
| `ZK-FIND-IVK-ZERO-TRANSMISSION-IDENTITY` | Critical | The prior repair still allowed a correctly decomposed IVK to reduce to scalar zero and did not state or prove nonidentity for every transmission point committed as a note owner. Scalar zero maps every diversified generator to the identity; an identity transmission key can detach commitment ownership from authorization/nullifier facts, while distinct owner material that collides only after `mod r` can alias the same IVK without a raw Poseidon field collision. Transfer's independent receiver transmission point was also unchecked. | All four circuits now contain one exact `auth.ivk_reduced != 0` inverse row. NoteReshape guards the computed shared transmission; Transfer guards claimed sender and receiver transmissions; Withdrawal guards the computed sender transmission. Isolated primitive tests cover both inverse predicates, exact trace/multiplicity tests pin every role, and generated inverse-row seams plus independent `Concrete`/`Security` predicates carry the rows into each final theorem. Full-circuit zero-IVK and raw point mutations are supporting evidence only because they also violate KDF decomposition or downstream commitments; they are not misclassified as isolated row evidence. | Resolved at the finite circuit-relation and native-construction boundaries by the explicit scalar and point guards. The remaining accepted-proof owner-uniqueness boundary is only nonzero modulo-`r` collision resistance, named separately as `ZK-ASSUME-IVK-HASH-TO-SCALAR-BINDING`; the five-target zero-preimage probability is constructor rejection/availability, not a soundness assumption. | `resolved` |
| `ZK-FIND-TRANSFER-DETECTION-ASSET-FLAG-ALIAS` | High | The old detection word arithmetically combined the asset with a flag at bit 253, so field reduction and high asset bits did not give an injective `(asset, flag)` encoding. Slot plaintexts also lacked an explicit relation-level `u32` bound on every branch. | V18 word 0 is the exact asset, word 1 the salt, word 2 `sender_slot + flag * 2^32`, and word 3 the receiver slot. Both slots are constrained to 32 bits. Native decoder tests reject the legacy asset/flag alias; `TestTransferDetectionRejectsSlotIDsOutsideCanonicalU32Range` mutates each slot and its ciphertext/statement consistently and still requires circuit rejection. | Resolved by a byte/field-injective plaintext layout, statement-domain/witness version bumps, canonical decoding, and exact formal detection semantics. | `resolved` |
| `ZK-FIND-TRANSFER-ASSET-SENTINEL` | High | Asset id zero is the indexed-tree low sentinel, not an asset policy. Without an explicit nonzero assertion, exact equality with that authenticated structural leaf could be interpreted as regulated membership. | Transfer V18 asserts `shared.asset_id != 0` before the membership/gap branch. `TestTransferWiringRejectsTheIndexedTreeZeroSentinelAsAnAsset` pins the assertion and exact ring/leaf-hash inputs, while native IMT insertion and policy registration already reject zero. | Resolved by enforcing the sentinel distinction in registry admission, the deployed circuit, the handwritten semantics, and exact trace regressions. | `resolved` |
| `ZK-FIND-WITHDRAWAL-ASSET-SENTINEL` | High | Withdrawal likewise could treat the authenticated zero IMT sentinel as a regulated asset-policy leaf if the action relation did not distinguish structural state from policy state. | Withdrawal V9 explicitly asserts the shared outbound asset is nonzero before `AssetRegistryGap`; `TestShieldedIcs20WithdrawalWiringBindsCompactLeafAndSentinel` pins that assertion together with the exact compact leaf hash. Native policy admission rejects zero. | Resolved by carrying the sentinel exclusion through native state, V9 R1CS, independent semantics, and the exact deployed adapter. | `resolved` |
| `ZK-FIND-COMPLIANCE-ZERO-DERIVATION` | High | A compliance leaf with derived scalar `d = 0` makes `ACK = d·ring_pk` the identity, collapsing the intended registered audit capability and making its shared-secret mask publicly predictable. | `validate_derivation_scalar` rejects a zero expected derivation before registry mutation, and `test_compliance_leaf_validation_rejects_zero_d` pins the failure. Transfer's external registry premise and both family documents now state the nonzero-admission invariant. | Resolved at the only state-admission boundary; the circuit proves membership in that authorized registry rather than attempting to prove hash-preimage rarity. | `resolved` |
| `ZK-FIND-TRANSFER-ZERO-TIER-SCALAR` | Medium | Honest Transfer construction could sample a zero ephemeral tier scalar. The resulting identity EPK/shared secret makes the corresponding `c2` mask predictable and leaks that creator-known tier despite an otherwise valid proof. | All four tier randomizers now use `sample_nonzero_scalar`; `transfer_nonce_sampling_rejects_zero` uses a deterministic zero-then-one RNG and proves the sampler retries. This is honest-construction privacy hardening, not a claim that consensus can force a malicious creator to hide plaintext it already knows. | Resolved by rejection sampling and by keeping malicious-sender disclosure outside the R1CS confidentiality claim. | `resolved` |
| `ZK-FIND-TRANSFER-EFFECT-HASH-COMPLIANCE-SPEC` | High | The runtime Transfer effect hash already covered its proof-bound receiver compliance records, but the independent accepted-language model represented that fact only inside prose attached to the generic authorization-signature predicate. The final theorem therefore lacked a separately enumerable premise for the exact ordered ciphertext/metadata projection and could appear complete even if that projection later drifted. | `ExternalChecks.effectHashBindsComplianceRecords` and `ConsensusExternalFacts.effectHashBindsComplianceRecords` now expose the fact at the theorem boundary. `effect_hash_binds_proof_bound_compliance_records_but_not_witness_anchor` mutates the ciphertext, metadata, and normalized anchor independently; the specification matrix assigns the exact runtime test and BLAKE2b assumption to `EXT-TRANSFER-EFFECT-HASH-COMPLIANCE-BINDING`. | Resolved by turning an implicit prose claim into an explicit external-acceptance conjunct with closed evidence and assumption ownership. | `resolved` |
| `ZK-FIND-FV-MUTATION-EVIDENCE-MASKING` | High | Several full-circuit negative tests mutated a public statement field while retaining the fixture's original claimed hash. Rejection could therefore come entirely from the statement-hash equality and falsely appear to demonstrate nullifier, randomized-key, output-commitment, compliance, balance, or dummy-slot constraints. A test selector alone could not distinguish this collateral failure. | Public-field adversarial tests now mutate the binary witness and Go assignment together and recompute the exact family statement hash, except tests explicitly scoped to statement binding. Wrong-slot synthetic nullifiers use the valid derivation for another fixed slot before rehashing. The specification matrix also closes every runnable test in reviewed Rust/Go sources as selected evidence or a typed explicit exclusion, with exact source, selector, execution, fingerprint, and census pins. | Resolved by requiring evidence isolation in the tests themselves and a closed reviewed-test census in the certification gate. Universal soundness still comes from exact-row Lean refinement; negative tests exercise the intended seams rather than substituting for that proof. | `resolved` |
| `ZK-FIND-FV-ADDRESS-PACKING-VISIBILITY` | Medium | The handwritten Transfer address-encryption specification originally treated the three plaintext limbs as an unexplained projection of two Fq values. That left reducedness/injectivity dependent on an invisible frontend detail and could invite either a false alias finding or an unsound provider theorem. The deployed circuit itself was not vulnerable: gnark's native `ToBinary(..., 256)` already clamps to 253 bits, checks `<= p-1`, and pads three zero bits. | `TestGnarkToBinary256RejectsNonCanonicalAddressPlaintextAlias` proves the exact frontend accepts a canonical field and rejects its `x + p` alias. `NativeToBinary256Circuit` and `AddressPackingBackendCircuit` expose the comparator, recovery, zero padding, and 31-byte chunk equations to the handwritten provider consumed by Transfer refinement. | Resolved by documenting and proving the actual backend predicate instead of adding redundant circuit rows or overstating the issue as a deployed vulnerability. | `resolved` |
| `ZK-FIND-FV-BRANCH-WITNESS-COVERAGE` | Medium | One anonymous prove/verify witness was insufficient release evidence for circuits with materially different regulated, flagged, unregulated, real, and dummy semantic branches. It showed one key/R1CS round trip but not that the canonical builders could reach the other branches under the same deployed key. | Profile schema v2 carries case-labelled `proof_witnesses`; receipt schema v4 binds `proof_case`. Transfer receipts cover `regulated_unflagged`, `regulated_flagged_hidden`, and `unregulated_hidden`; Withdrawal covers regulated/optional-real and unregulated/optional-dummy. Exact-row Lean proofs remain the universal soundness evidence. | Resolved by treating branch witnesses as key-coherence/satisfiability evidence and never as a substitute for universal relation refinement. | `resolved` |
| `ZK-FIND-FV-SETUP-PROVENANCE-OVERCLAIM` | High | A successful Groth16 prove/verify receipt establishes coherence with the stored key, not how that key was generated or whether toxic waste was destroyed. Without a separate provenance contract, “key-bound” evidence could be misread as a trusted-setup derivation proof. | Circuit metadata v2 and proof receipt v4 bind a canonical setup-provenance v2 object. It records `fresh_setup` and exact per-case generation self-tests while explicitly stating `setup_transcript=not_recorded` and `toxic_waste_erasure=not_mechanically_verified`; the gate is named key coherence, not derivation. The residual trust is tracked in `ZK-ASSUME-GROTH16-SETUP-TOXIC-WASTE`. | Resolved as an evidence-claim flaw by making the limitation machine-readable. The operational toxic-waste premise remains an explicit assumption until a verifiable ceremony or transparent proof system replaces it. | `resolved` |
| `ZK-FIND-AGGREGATE-DEV-SRS-TRAPDOOR` | Critical | Consensus required a SnarkPack aggregate generated and verified with `DevSrs`, whose fixed public RNG seed reconstructs the KZG and pairing-commitment trapdoors. Aggregate acceptance therefore could not soundly attest that its constituent Groth16 proofs were valid. Prepared-proposal reuse, extracted caches, and detached sidecars enlarged that unsafe architecture. | The aggregate transaction action, proposal tail, sidecar transport, retry cache, candidate envelope, host/consensus verifier, and production app dependency were deleted. Every production acceptance sink reconstructs proof items from the transaction and verifies each one under its exact `DeployedProofKey`; ProcessProposal unconditionally re-verifies even warm artifacts before replay, and only then promotes `Groth16Verified`. SnarkPack types and SRS code remain isolated to research, benchmarks, and their own fuzzing. A production-absence invariant and four-family adversarial tests close the deployed boundary. | Resolved by removing the unsafe aggregation architecture from consensus, not by treating the deterministic SRS as redundant. `DevSrs` remains unsuitable for any future deployed soundness boundary. | `resolved` |
| `ZK-FIND-WITHDRAWAL-POLICY-OPENING-OVERREACH` | Medium | Withdrawal carried the full regulated-policy opening even though its arithmetic relation only needed the authenticated asset classification/leaf commitment; route and complete policy authorization are current-state handler obligations. Repeating unused openings enlarged the witness and formal surface and risked split native/circuit policy views. | V9 carries only `(value, nextIndex, nextValue, paramsHash, ringHash)`, recomputes the canonical five-field IMT leaf, and exposes current-policy route cross-indexing as an external premise. `TestShieldedIcs20WithdrawalV11OmitsPolicyOpeningsAndRedundantFields` forbids every removed policy field. | Resolved by minimizing the circuit opening to committed facts and assigning live route/policy interpretation to the typed execution boundary. | `resolved` |
| `ZK-FIND-WITHDRAWAL-CROSS-SPEND-NONCE-COUPLING` | Low | Withdrawal plan validation required independent spends to share a transaction blinding nonce that the Withdrawal circuit and accepted action semantics do not consume as a shared fact. This rejected otherwise valid multi-spend plans and introduced a misleading cross-record invariant. | The equality check was removed while sender, anchor, asset, and compliance witnesses remain aligned. `multi_spend_tx_blinding_nonces_are_independent` constructs distinct nonces and requires plan admission. | Resolved by deleting the semantically dead coupling rather than preserving or formalizing it. | `resolved` |
| `ZK-FIND-TRANSFER-HANDWRITTEN-STATEMENT-DUPLICATE` | High | During statement refinement, the independent Lean statement list briefly duplicated the required randomized-key encoding before the optional nullifier. That produced one extra projection relative to the deployed Rust/Go statement and theorem contract, demonstrating that a handwritten seam needs an exact index guard even when the final length theorem would eventually fail. | `Concrete.statementFields` now has the exact deployed order with one required RK. `test_v18_statement_fields_have_exact_deployed_indices` pins all 44 source-level roles and indices, while Rust/Go statement differential tests and generated indexed semantic bindings independently pin the executable order. | Resolved by correcting the independent specification and adding exact ordered seam regression rather than relying only on a count. | `resolved` |
| `ZK-FIND-CONSENSUS-ADMISSION-RESOURCE-BOUNDS` | High | CheckTx and proposal admission lacked one fixed protocol envelope for raw bytes, action count, transaction/block spend-nullifier count, proposal transaction count, payload bytes, and proof-worker fan-out. Several expensive paths decoded, allocated, hashed, or scheduled attacker-controlled work before applying an equivalent bound, and environment-controlled concurrency made validator resource behavior node-specific. | Shared constants now cap transactions at 96 KiB, 512 actions, and 256 spend nullifiers; proposals at 4,096 transactions, 1 MiB payload, and 32,768 spend nullifiers; PrepareProposal scans at most 4,096 user candidates. CheckTx, extraction, PrepareProposal, ProcessProposal, and benchmark preflight enforce the corresponding limit before expensive work. Canonical native and protobuf nullifier counters exhaustively match every current action variant, including fee-funding Transfer inputs. Independent proof verification is partitioned over the four deployed keys and capped at eight blocking chunks per key. | Resolved by making the resource envelope protocol-owned, fail-closed, and mutation-tested at every admission sink. The exact Comet payload limit is inclusive; only values above the limit are rejected. | `resolved` |
| `ZK-FIND-ASYNC-WORKER-LIFETIME` | High | Proof extraction, action checks, and proposal preparation returned on the first join error. Tokio could abort async siblings, but already-running blocking crypto work is not cancellable, so failed attacker inputs could leave unaccounted work running after admission returned and accumulate across requests. | Shared drain helpers retain the first failure while awaiting every spawned sibling. Transaction historical/stateless/read groups, CheckTx's state and stateless branches, independent Groth16 key coordinators, and parallel PrepareProposal workers all drain before returning. `structured_join_drain_waits_for_siblings_after_error` and its transaction counterpart pin the behavior; the static completeness gate rejects early-return patterns in the reviewed worker surfaces. | Resolved by giving every spawned validation group a structured lifetime and draining it on success, rejection, panic, and backend error. | `resolved` |
| `ZK-FIND-CHECKTX-ANCHOR-CACHE-GROWTH` | Medium | Persistent compliance and claimed-SCT anchor caches would retain attacker-controlled historical keys or require eviction semantics that could multiply validation work. | CheckTx owns no persistent anchor-result cache. Each request captures `storage.latest_snapshot()`, constructs an isolated `App`, and performs historical anchor checks against that snapshot. The shared stateless cache contains only anchor-independent extraction/proof artifacts and cannot promote historical acceptance. | Resolved by deleting the persistent anchor-cache design instead of maintaining bounded historical-result caches. | `resolved` |
| `ZK-FIND-CHECKTX-CONTEXT-STALE-REUSE` | Medium | A shared CheckTx historical context could survive state advancement or refresh-worker failure, allowing speculative validation to use facts from a different snapshot. Consensus replay would reject such a transaction, but stale mempool admission could be amplified into wasted proof/state work and proposal churn. | The shared-context and refresh-worker path is absent. Mempool captures the latest storage snapshot for each admitted request and passes it into a new isolated `App`; no historical context is reused across requests. | Resolved by per-request snapshot ownership rather than versioning a shared historical context. | `resolved` |
| `ZK-FIND-STATELESS-CACHE-IDENTITY-AND-BOUNDS` | High | The stateless artifact cache treated a SHA-256 digest as the complete transaction identity and bounded only entry count. A digest alias could reuse an invalid or verified result for different bytes, while many maximum-size entries could retain attacker-controlled memory far beyond an explicit byte budget. Cache insertion APIs also exposed verification-tier promotion too broadly. | Every lookup and replacement compares the complete raw transaction bytes after digest indexing. The cache retains at most 4,096 entries, 96 KiB per transaction, and 64 MiB of raw transaction bytes; oversized entries bypass retention. Clock/accounting drift clears the cache rather than exceeding a bound. Only crate-private paths can insert `Extracted`, `Groth16Verified`, or `Invalid`, and exact-byte collision/replacement plus sustained-churn tests cover the invariants. | Resolved by making the digest an index rather than an identity, bounding all attacker-controlled dimensions, and restricting verification-tier promotion. | `resolved` |
| `ZK-FIND-WITHDRAWAL-CHECK-EXECUTE-TOCTOU` | High | Withdrawal historical checking resolved enablement, route policy, channel, connection, client, sequence, and packet facts before transaction execution, but other actions or intervening state could invalidate those facts before the outbound packet and accounting writes. Reusing a stale checked projection could send on a changed route or begin mutation before discovering a later fallible prerequisite. | The enable switch is rechecked at the mutation sink. `CheckedWithdrawal` is non-forgeable outside the transfer component and carries the exact withdrawal, packet, resolved route, channel, connection, sequence, complete policy, and block time. Execution re-resolves and compares route/channel/connection/policy, then reruns send-packet checks for the same client status, consensus state, timeout, sequence, empty packet slot, and exact packet. All fallible reads, arithmetic, and event construction complete before the first write; stale route, policy, sequence, and client tests require rejection without mutation, and proof-backed transaction rollback covers completed earlier-action effects and deferred indexes. | Resolved by treating the checked value as a revalidation token, performing exact live checks immediately before mutation, and preserving transaction-wide rollback. | `resolved` |
| `ZK-FIND-WITHDRAWAL-BARE-EXECUTOR` | Critical | An unused public `Ics20WithdrawalWithHandler` path could execute the transparent payload's packet and accounting without the shielded action's proof, authorization signatures, nullifiers, change commitment, compliance anchors, or target timestamp. No production caller used it, but its public surface was an alternate state-transition architecture outside the reviewed acceptance join. | The bare handler and its action-handler adapter are deleted, their module exports are removed, and the repository completeness gate rejects either obsolete source path or either public API token. The only remaining outbound execution call is reached through `ShieldedIcs20Withdrawal::check_and_execute` after proof-bound and external checks. | Resolved by deleting the alternate executor rather than maintaining a second partially checked path. | `resolved` |
| `ZK-FIND-FV-SEMANTIC-IMPORT-CLOSURE` | Medium | The semantic-digest closure originally recognized only an unindented one-module Lean `import` line without a trailing comment. A valid multi-module, indented, or commented import could leave a semantic helper outside the digest; after a one-time reviewed repin, later weakening of that helper would not change theorem signatures or the semantic pin. | Semantic import parsing now accepts leading whitespace, every module on an ordinary import line, and trailing line comments. Mutation tests place helpers behind each syntax and prove that changing any imported helper invalidates the digest. | Resolved by closing the parser grammar used to compute the recursively imported semantic source bundle. | `resolved` |
| `ZK-FIND-CONSENSUS-DIAGNOSTIC-IO` | Medium | PrepareProposal and ProcessProposal contained locally configured aggregate-debug filesystem behavior. Even if intended only for diagnostics, validator-local I/O on consensus acceptance paths creates node-dependent failure and latency surfaces. | The environment switches, filesystem reads/writes, and detached aggregate debug path were removed from production acceptance. `consensus_acceptance_source_has_no_diagnostic_io` plus the static specification-completeness gate reject the diagnostic tokens and imports in the reviewed consensus surface. | Resolved by keeping consensus admission free of locally configured diagnostic I/O. | `resolved` |
