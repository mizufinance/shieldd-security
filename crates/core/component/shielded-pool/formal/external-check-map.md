# External Check Map

Acceptance obligations here are enforced outside the arithmetic circuit.
“Composed” means the handwritten acceptance refinement requires the named fact
and the runtime has a corresponding enforcement path; it does not move that
runtime path into the circuit. Wallet-only rows are explicitly marked as
construction premises and are absent from `ConsensusAccepted`.

| ID | Obligation | Rust symbol | Go symbol | Status |
| --- | --- | --- | --- | --- |
| `EXT-FIXED-SHAPE` | Decoding and stateless validation accept only the fixed action shape and exact signature count selected by the active protocol version. | `validate_shape`, `verify_auth_sigs`, `transfer_verify_auth_sigs` | `n/a` | `composed` |
| `EXT-PUBLIC-PROJECTION` | The verifier public input is reconstructed from the decoded body in the native field order and domain, including the enclosing transaction anchor. | `*_extract_public`, `*_statement_hash_from_public` | `Reconstructed*StatementFieldsFromWitness*` | `composed` |
| `EXT-PROOF-FAMILY` | Every extracted proof is assigned an exact deployed family. Ordinary acceptance independently verifies every item under that family and attaches the resulting capability to its exact action slot. Aggregate ProcessProposal acceptance validates the bundle version, SRS, segment coverage, family order, real and padded counts, statement inputs, and family verification key before reducing every SnarkPack result and attaching capabilities to the same exact slots. | `collect_consensus_proof_items_with_artifacts`, `independently_verify_proof_families`, `batch::verify_each_with_capabilities`, `VerifiedBatchItem::ensure_binds`, `validate_aggregate_verify_plan_inputs`, `plan_aggregate_bundle_verification`, `proof_verification_key_for_family`, `verify_shipping_family_aggregate_profiled_status`, `reduce_aggregate_verify_outcomes`, `attach_verified_capabilities` | `groth16.Verify`, SnarkPack aggregate verification | `composed` |
| `EXT-AUTH-SIGNATURES` | Every fixed-slot randomized verification key is nonidentity before its spend authorization signature verifies over `TransactionContext.effect_hash`, including dummy slots. Transfer's effect hash contains the exact receiver compliance ciphertext and metadata; only the separately checked witness anchor is normalized, so a delegated builder cannot replace encryption randomness or payload bytes after authorization. | `TransferBody::effect_hash`, `verify_auth_sigs` | `n/a` | `composed` |
| `EXT-ACTION-AUTHORIZATION-COMPLETENESS` | Every effecting transaction action that requires authority is independently authorized over its canonical effecting data under a nonidentity action-specific verification key. Asset registration grants use a registrar key, and regulated asset/user admission uses a registration-authority key. Typed construction/decoding, signature verification, genesis admission, and registrar-state admission all enforce nonidentity through the shared helper. IBC relay remains intentionally permissionless and is authorized by its client/proof/state checks rather than an action key. This system-wide invariant is outside the six target refinements' arbitrary `otherStep` and uses the `ZK-ASSUME-SPEND-AUTH-RDSA` cryptographic boundary. | `ensure_nonidentity_spend_auth_key`, `AssetRegistrationGrant::verify`, `AssetRegistrationGrant::try_from`, `MsgRegisterAsset::validate_authorization_keys`, `AssetPolicy::validate_crypto_keys`, `UserRegistrationGrant::verify`, `Content::validate_authorization_keys`, `NativeAssetRegistration::validate_authorization_keys`, `ComplianceRegistryRead::is_compliance_registrar`, `ComplianceRegistryComponentWrite::admit_genesis_compliance_registrar`; regressions `asset_registration_grant_rejects_identity_registrar_key`, `user_registration_grant_rejects_identity_registration_authority`, `asset_policy_rejects_identity_registration_authority_key`, `genesis_rejects_identity_authorization_keys` | `n/a` | `native envelope` |
| `EXT-BINDING-SIGNATURE` | Every transaction containing a NoteReshape, Transfer, Withdrawal, or fee-funding proof has a nonidentity aggregate action/fee balance key and an ordinary binding signature over `TransactionBody.auth_hash()`. The identity-key all-zero sentinel is accepted only when `num_proofs() == 0`. Honest construction rejects zero aggregate blinding for proof-bearing plans, and consensus independently rejects any proof-bearing identity-key transaction. Because the ordered spend-authorization bytes are auth-hashed, the nonidentity binding signature also prevents same-key signature permutation malleability without requiring RK uniqueness. | `valid_binding_signature`, `TransactionPlan::apply_auth_data`, `proof_bearing_transaction_rejects_identity_binding_key_signature_permutation`, `apply_auth_data_rejects_zero_blinding_for_proof_bearing_transaction` | `n/a` | `composed` |
| `EXT-SCT-ANCHOR` | Each action body anchor equals the enclosing transaction context anchor, and that anchor is either the accepted empty root or a recorded SCT root. | `validate_action_anchor`, `claimed_anchor_is_valid` | `n/a` | `composed` |
| `EXT-NULLIFIER-STATE` | Every proof-bound nullifier is absent from both live generation trees. Each real input below the proof-bound recent-position floor additionally carries a canonical, origin-hiding proof bundle for the complete retired-generation prefix, ending at the exact proof-bound history head. The typed action transition then requires action-local uniqueness and gives an exact current-generation extension; transaction-final atomicity and rollback remain separate runtime obligations. | `verify_historical_proofs`, `ensure_historical_coverage`, `tx_parameters_historical_check_with_context`, `nullify_all`, `deferred_index_records_only_transactions_that_commit` | `HistoryRequired`, `innerCircuit`, `outerCircuit` | `composed` |
| `EXT-OUTPUT-STATE` | The typed action transition requires the delta to equal every proof-bound output. Given the external complete transaction envelope, `TransactionAccepted` includes that target multiset in its output list and binds durable transaction-final output state to the multiplicity-exact extension modulo storage ordering; it makes no append-order, SCT-position, or arbitrary-remainder provenance claim. The action-local theorem alone is insufficient, and executor rollback is separately tested. | `execute_proof_bound_effects`, transaction `StateDelta` commit, `deferred_index_records_only_transactions_that_commit` | `n/a` | `composed` |
| `EXT-ASSET-ROOT` | Transfer, Withdrawal, and NoteReshape use the current mutable asset-policy root; historical asset roots are never admissible or served by the anchor RPC. The root is the canonical IMT commitment of the native policy registry. | `validate_compliance_anchor_facts`, `get_asset_imt_root`, `current_anchors`, `IndexedLeaf::commitment_parts` | `n/a` | `composed` |
| `EXT-USER-ROOT` | Transfer, withdrawal, and NoteReshape use the exact current mutable user-status root; a status update immediately invalidates proofs against the prior root. | `get_user_tree_root`, `validate_compliance_anchor_facts` | `n/a` | `composed` |
| `EXT-REGISTRY-AUTHORIZATION` | Current asset policies and user leaves arise from structurally valid, authorized transitions. User leaves begin `Active`; the typed Bankd host boundary permits only `Active -> Frozen` and `Frozen -> Active` in this release. Asset detection and ring keys are non-identity before storage or tree mutation. | `AssetPolicy::validate_crypto_keys`, `UserGrantAdmission::verify`, `apply_user_status_action`, `validate_host_source` | `n/a` | `composed` |
| `EXT-TIMESTAMP-FRESHNESS` | Transfer and Withdrawal target timestamps are nonzero independently of block time. The shared pure checker rejects negative signed consensus time before conversion and accepts only the inclusive fixed ±3600-second window around current consensus time at execution. | `check_timestamp_freshness` | `n/a` | `composed` |
| `EXT-TRANSFER-ENCODING` | Transfer input compliance ciphertexts are empty; the receiver's fixed ciphertext and metadata records decode canonically; any retired upload/PRE material is rejected. | `transfer_check_lengths`, `parse_transfer_output_compliance` | `n/a` | `composed` |
| `EXT-TRANSFER-EFFECT-HASH-COMPLIANCE-BINDING` | The canonical Transfer effect hash commits the exact ordered receiver compliance ciphertext and metadata records consumed by proof extraction. Only the witness anchor is normalized out of this hash, and that copy is independently required to equal the enclosing transaction anchor. | `TransferBody::effect_hash`, `effect_hash_binds_proof_bound_compliance_records_but_not_witness_anchor` | `n/a` | `composed` |
| `EXT-TRANSFER-NONCE-PRIVACY` | Honest construction uses one CSPRNG-sampled nonce root per Transfer action, distinct across all Transfers and fee funding in the enclosing transaction. The root deterministically expands into salts and tier randomness, so reuse would repeat EPK/stream material. This is a private `HonestConstructionFacts` premise, not a consensus-checkable acceptance rule. | `enrich_plan_with_compliance`, `build_transfer_compliance` | `n/a` | `construction-assumed` |
| `EXT-HONEST-COMPLIANCE-TIER-SCALAR-NONZERO` | Honest construction rejection-samples all four compliance-encryption tier scalars until they are nonzero. This prevents an accidental identity EPK and predictable mask; it is a wallet-private confidentiality premise, not a consensus rule against a malicious creator disclosing creator-known plaintext. | `sample_nonzero_scalar`, `transfer_nonce_sampling_rejects_zero` | `n/a` | `construction-assumed` |
| `EXT-WITHDRAWALS-ENABLED` | The global outbound ICS-20 switch is checked during historical admission and rechecked immediately at the shielded Withdrawal mutation sink. No bare non-proof Withdrawal executor exists. | `ensure_withdrawals_enabled`, `historical_check_rejects_disabled_withdrawals`, `execution_rechecks_disabled_withdrawals_before_mutation` | `n/a` | `composed` |
| `EXT-WITHDRAWAL-PAYLOAD` | Withdrawal payload validation admits one canonical encoding and rejects deprecated compatibility or other noncanonical encodings instead of normalizing them. It rejects zero value, zero or non-minute-rounded timeout, and transparent-address mode when the return address has no canonical transparent encoding; all payload fields remain effect-hashed. | `Ics20Withdrawal::validate`, `TryFrom<pb::Ics20Withdrawal>` | `n/a` | `composed` |
| `EXT-WITHDRAWAL-ROUTE` | If the outbound asset ID has a current native policy, its canonical parameter/ring sub-hashes are committed under the current asset root and that exact policy authorizes the route encoded by the effect-hashed payload. A nonempty regulated compliance memo must contain only the canonical wrapper and its embedded asset ID must equal the withdrawn denomination's asset ID. An asset absent from the current registry follows the unregulated runtime branch. | `withdrawal_check`, `validated_withdrawal_route`, `get_asset_policy`, `AssetPolicy::permits_ibc_route`, `withdrawal_check_rejects_unauthorized_regulated_route`, `withdrawal_check_enforces_regulated_memo_shape_and_asset_binding`, `IndexedLeaf::commitment_parts` | `n/a` | `composed` |
| `EXT-WITHDRAWAL-CHANNEL-CONNECTION` | The exact source channel exists, is open, unordered, negotiated as `ics20-1`, has exactly one connection hop, and names an existing open connection and counterparty channel. Missing, pre-open, closed, ordered, wrong-version, zero-hop, ambiguous-hop, missing-connection, non-open-connection, and substituted routes are rejected. | `withdrawal_check`, `resolve_ibc_route`, `withdrawal_check_rejects_missing_route_state`, `withdrawal_check_rejects_missing_counterparty_channel`, `withdrawal_check_rejects_closed_channel`, `withdrawal_check_rejects_preopen_channel`, `withdrawal_check_rejects_ordered_channel`, `withdrawal_check_rejects_non_ics20_channel_version`, `withdrawal_check_rejects_zero_connection_hops`, `withdrawal_check_rejects_ambiguous_connection_hops`, `withdrawal_check_rejects_missing_connection`, `withdrawal_check_rejects_non_open_connection` | `n/a` | `composed` |
| `EXT-WITHDRAWAL-CLIENT-STATUS` | The connection's light client and latest consensus state exist, the client is not frozen or expired, and the consensus timestamp is not in the future relative to execution. Missing, frozen, expired, missing-consensus, and future-consensus cases have separate negative regressions. | `withdrawal_check`, `withdrawal_check_rejects_missing_client`, `withdrawal_check_rejects_missing_latest_consensus_state`, `withdrawal_check_rejects_future_latest_consensus_timestamp`, `withdrawal_check_rejects_frozen_light_client`, `withdrawal_check_rejects_expired_light_client` | `n/a` | `composed` |
| `EXT-WITHDRAWAL-TIMEOUTS` | Both counterparty height and timestamp are strictly before the effect-hashed packet timeouts. | `withdrawal_check`, `withdrawal_check_rejects_elapsed_height_and_timestamp_timeouts` | `n/a` | `composed` |
| `EXT-WITHDRAWAL-CHECKED-TOKEN` | Immediately before mutation, the checked-withdrawal token is revalidated against the exact channel and connection snapshots, client, route, complete policy snapshot, allocated send sequence and empty packet slot, and packet. A changed policy is rejected even when both old and new policies authorize the same route. | `withdrawal_execute`, `withdrawal_execute_rejects_stale_route_token_before_mutation`, `withdrawal_execute_rejects_stale_policy_token_before_mutation`, `withdrawal_execute_rejects_stale_sequence_token_before_mutation`, `withdrawal_execute_rejects_stale_client_token_before_mutation` | `n/a` | `composed` |
| `EXT-WITHDRAWAL-EFFECT-HASH` | The four public limbs are consecutive little-endian 128-bit chunks of the embedded withdrawal body's BLAKE2b-512 effect hash. | `shielded_ics20_withdrawal_extract_public`, `withdrawal_effect_hash_limbs` | `ReconstructedShieldedIcs20WithdrawalStatementFieldsFromWitnessV11` | `composed` |
| `EXT-WITHDRAWAL-STATE` | The typed action transition requires withdrawal enablement, exact local-asset escrow credit or returning-voucher debit, exact sequence allocation, packet insertion, canonical send/outbound event emission, nullifier consumption, and change-output insertion in one action-local post-state. Given the external complete Withdrawal-effects envelope, `TransactionAccepted.withdrawalEffects` additionally requires target balance-effect and event multiplicity, their exact durable final folds/extensions, a fresh and persistent exact packet, and an advanced target-route sequence. This is a conditional transaction-final model, not a Lean proof of the executor join. Focused runtime tests exercise both accounting branches, reject voucher underflow and local escrow overflow before packet allocation, assert every canonical send-packet attribute, and reject exhausted sequences and occupied packet slots before mutation; the two-chain `ics20_transfer_no_timeouts` test exercises a real proof, packet commitment proof, relay, and balance transition. `proved_withdrawal_accounting_failure_rolls_back_action_effects` proves same-action rollback when packet accounting fails after proof-bound nullifier/change staging. `proved_withdrawal_then_later_failure_rolls_back_first_action` proves that packet, sequence, accounting, event, nullifier, change-output, and deferred-index effects from a completed first Withdrawal all roll back when a later action fails. | `ensure_withdrawals_enabled`, `withdrawal_check`, `withdrawal_execute`, `withdrawal_execute_updates_supply_and_packet_state`, `voucher_withdrawal_subtracts_channel_asset_balance_and_commits_packet`, `voucher_withdrawal_rejects_insufficient_channel_asset_balance`, `local_withdrawal_rejects_escrow_overflow_before_packet_allocation`, `withdrawal_check_rejects_exhausted_send_sequence`, `withdrawal_execute_rejects_occupied_packet_slot_before_mutation`, `ics20_transfer_no_timeouts`, `proved_withdrawal_accounting_failure_rolls_back_action_effects`, `proved_withdrawal_then_later_failure_rolls_back_first_action` | `n/a` | `composed` |
| `EXT-PUBLIC-INPUT-HASH-CRYPTO` | Distinct certified statement vectors do not collide under their fixed domain-separated Poseidon public-input hash. | `*_statement_hash_from_public`, `public_input_hash` | `*StatementHash*` | `assumed` |
| `EXT-EFFECT-HASH-CRYPTO` | Distinct effecting protobuf messages do not collide under the BLAKE2b-512 effect hash. | `EffectHash::from_proto_effecting_data` | `n/a` | `assumed` |
