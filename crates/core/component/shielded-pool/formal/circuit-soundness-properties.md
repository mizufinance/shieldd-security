# ZK Circuit Soundness Properties

The property rows describe the required proof architecture. The current V8
NoteReshape, V20 Transfer, and V12 Withdrawal artifacts add per-asset user
status constraints. Their status-specific refinement remains a candidate until
the handwritten semantic layer is extended and reviewed.

| ID | Circuit(s) | Precise statement | In-circuit assertions | External checks | Adversary view | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `NO-DOUBLE-SPEND` | transfer, NoteReshape, shielded ICS-20 withdrawal | Each real input nullifier is uniquely determined by `(NK, commitment, position)` and the commitment path reaches the public anchor. Every dummy nullifier uses a family/slot-separated synthetic derivation; synthetic/real noncollision is computational under `ZK-ASSUME-SYNTHETIC-NULLIFIER-CROSS-DOMAIN-CR`, not an unconditional range-disjointness theorem. | `Nullifier`; `VerifyStateCommitmentPath`; real/dummy mux and synthetic-seed relations; exact `history_required = is_real && position < recent_position_floor` classification in every family. | Consensus checks both live nullifier generations for every slot. Each real old input additionally supplies one canonical complete-prefix historical bundle ending at the exact window history head; transaction-wide duplicate rejection and atomic `nullify_all` insertion then consume every fixed slot. | Prover tries to spend the same note twice, omit or truncate retired history, misclassify an old note as recent, reveal its exact old origin generation through bundle shape, repeat a nullifier across actions, suppress a dummy nullifier, or find a cross-domain synthetic/real collision. | `composed-computational` |
| `BALANCE-CONSERVATION` | all circuit families | Input amounts minus output/withdrawal amounts equal the constrained balance commitment for the one circuit-bound asset. | NoteReshape/Withdrawal conservation-specific balance ladders; Transfer net-balance ladder; amount bounds; NoteReshape claimed-point on-curve/equivalence; exact statement compression. Transfer and Withdrawal expose no duplicate affine balance witness. | Body extraction, proof verification, and the mandatory nonidentity-BvK authorization-hash signature on every proof-bearing transaction bind the public commitment. Identity-key sentinel mode is unavailable to all four target accepted languages. | Prover creates value, changes withdrawal amount, substitutes a second balance representation, or permutes same-RK authorization signatures without invalidating the accepted transaction. | `refined` |
| `NOTE-OWNERSHIP-SPEND-AUTH` | all spend circuits | Every real input binds `RK = AK + r*G`, its note address derives from the shared IVK, and its authorization signature binds the transaction effect hash. Every NoteReshape, Transfer, and Withdrawal relation excludes the Decaf identity class for its authorization key and every sender/shared diversified generator and transmission point, and rejects a zero reduced IVK. A padded dummy input has no AK relation: its arbitrary RK is externally signature-authorized, while zero amount, a synthetic nullifier, and disabled membership prevent it from authorizing a state spend. Distinct admitted owner material is bound computationally by collision resistance of the exact nonzero Poseidon-to-scalar map after reduction modulo `r`. | `RandomizedVerificationKey` gated to real slots; `DiversifiedTransmissionKey`; unconditional address relations; role-specific exact non-identity `x != 0` rows; exact reduced-IVK `!= 0` row; dummy zero-amount/nullifier mux. | `verify_auth_sigs` checks every fixed-shape RK, including padded slots, before execution. Native key/address predicates and the native predicate census cover the corresponding admitted roles. `ZK-ASSUME-IVK-HASH-TO-SCALAR-BINDING` states only the remaining nonzero modulo-`r` collision boundary; zero targets are rejected and affect honest-constructor availability, not accepted-proof soundness. Untrusted address decoding rejects identity values; only infallible native derivation is conditional on `ZK-ASSUME-DIVERSIFIER-HASH-TO-GENERATOR-NONIDENTITY`. | Prover spends a note controlled by another key, supplies a zero-IVK or identity-class key/address, finds distinct owner material with one admitted reduced IVK, turns a dummy slot into a state spend, or reuses a signature on another transaction effect. | `composed-computational` |
| `OUTPUT-WELL-FORMEDNESS` | transfer, NoteReshape, shielded ICS-20 withdrawal | Each output commitment opens to the note fields constrained by the circuit, and regulated compliance leaves bind the complete address, asset, canonical address-derived scalar, and status facts. Every distinct output owner has non-identity diversified-generator and transmission points. | `NoteCommitmentWithCompressedDivGen`; five-field `ComplianceLeafCommitmentFromCompressed`; output commitment equality; shared sender/address context; role-specific exact Decaf non-identity rows. | Native address allocation and compliance admission enforce canonical address predicates; the exact current user root and `Active` circuit checks enforce status. | Prover supplies an identity-class output owner, substitutes an address/status, publishes a different commitment opening, or relies on malformed ciphertext outside this circuit claim. | `refined` |
| `REGULATED-STATUS-SOUNDNESS` | transfer, NoteReshape, shielded ICS-20 withdrawal | A regulated action proves the required sender and recipient leaves under `ComplianceAnchor`, with the address, asset, canonical address-derived scalar, and per-asset status committed in each five-field leaf. Every required status equals `Active`. | Five-field compliance-leaf hash; exact path; regulated-gated root equality; regulated-gated `status = Active`; authenticated asset classification. | `validate_compliance_anchors` requires the exact current user and asset roots, so a pre-freeze proof becomes stale immediately. Deposit admission performs the equivalent native `Active` check. | Prover uses a pre-freeze root, substitutes another address/asset/status, or authorizes a regulated transfer, withdrawal, fee payment, or reshape from a `Frozen` leaf. | `candidate` |
| `CIPHERTEXT-CORRECTNESS` | transfer | Every Transfer detection, amount, and address ciphertext field equals its in-circuit Poseidon stream plaintext relation. Address disclosure contains exactly the diversified-generator and transmission-key encodings. Encryption is unconditional for both policy branches; `isRegulated` gates the threshold result and issuer/user shared-secret selection, not ciphertext equality. Detection plaintext is exactly asset, salt, canonical flag, and reserved zero; proof-bound public routing tags provide address candidate filtering separately. This is an integrity/functional-correctness claim and does not by itself prove computational confidentiality or audit recoverability. Withdrawal has sender/asset membership checks but no compliance-ciphertext surface. | Go `VerifyPoseidonEncryptionTransferDetection`, `VerifyPoseidonEncryptionTransferAmount`, and `VerifyPoseidonEncryptionTransferAddress`; exact V20 encryption-template providers; native reduced-field packing of the two disclosed address fields; Decaf compression, scalar multiplication, and selection rows. | The Transfer handler canonically decodes the fixed ciphertext and metadata records, rejects any retired upload/PRE material, and verifies the proof and compliance anchors. Honest privacy separately assumes one fresh CSPRNG nonce root per Transfer action, with no sibling/fee-funding reuse, and rejection-samples nonzero tier scalars; this private construction premise is not consensus-checkable. Withdrawal stateless validation rejects nonempty input compliance ciphertext instead of admitting an unused body field. | Prover changes a disclosed plaintext field, salt, EPK, c2, policy branch, routing tags, metadata, or ciphertext without changing the accepted statement, reuses honest-construction randomness across actions, or smuggles an uninterpreted ciphertext into withdrawal. | `refined` |
| `STATEMENT-INTEGRITY` | all circuit families | `ClaimedStatementHash` must hash exactly the semantic public fields intended for the circuit family, in the same order in Rust and Go. | Rust `*_statement_fields`; Go circuit statement-field reconstruction and Poseidon hash. | Rust fixtures carry the claimed hash, not a second ignored field vector; Go reconstructs every field independently, hashes the result, and compares it with the Rust claim. | Prover exploits a Rust/Go field-order mismatch, an ignored duplicate encoding, or a length-only ABI check. | `composed` |
| `WITNESS-ABI-CANONICALITY` | all gnark-backed circuit families | Every accepted witness byte string has one typed interpretation: booleans and statuses are canonical, tree paths have exact depth/arity, field and point encodings are canonical, Fr scalars are below the companion order, and withdrawal effect limbs are 128-bit. The current formats are NoteReshape V8, Transfer V20, and Withdrawal V12. | Circuits enforce semantic ranges, exact path positions, old/recent SCT history classification, five-field user commitments, and regulated-gated `Active` status. | Shared Rust/Go binary decoders reject aliases before assignment; Rust parity-tests native commitments; receipts hash the exact accepted witness. | Prover or build tooling changes bytes without changing the reduced circuit assignment, supplies divergent Rust/Go policy views, weakens artifact receipts, or creates cross-language parser disagreement. | `composed` |
| `FIXED-ARITY-PRIVACY` | transfer, NoteReshape, shielded ICS-20 withdrawal | Public action bodies do not reveal synthetic slots through special-length or all-zero encryption sentinels. | Dummy selectors, zero-value constraints, synthetic nullifiers, and disabled membership remain private circuit facts. | Synthetic slots use ordinary fixed-length backreference and note-key encryption; observers and consensus process all fixed slots uniformly. | Observer classifies the real input/output arity without breaking encryption. | `composed` |
| `ACTION-ANCHOR-CONSISTENCY` | transfer, NoteReshape, shielded ICS-20 withdrawal | The retained action-body anchor must equal the single transaction anchor used as the proof's public membership root. | Circuit statement binds the transaction-context anchor. | Stateless extraction rejects a body-anchor copy that differs from `TransactionContext.anchor`; stateful validation checks the transaction anchor. | Submitter mutates an effect-hash-exempt body anchor or creates two apparent anchor claims for one action. | `composed` |
| `CONSENSUS-PROOF-EXTRACTION-COMPOSITION` | transfer, NoteReshape, shielded ICS-20 withdrawal, fee funding | A proof artifact may enter execution or a `FullyVerified` cache entry only after the same transaction-derived public statement is verified under its exact deployed family, either by independent Groth16 verification or by the consensus SnarkPack aggregate protocol. | Each circuit constrains the proof relation and claimed statement hash. | `collect_consensus_proof_items_with_artifacts` invokes the combined validation/extraction helper for every shielded action. The ordinary path runs `independently_verify_proof_families`; aggregate ProcessProposal validates the protocol version, production SRS identifier, segment coverage, family ordering, real/padded counts, family verifying key, statement inputs, and every aggregate outcome before `attach_verified_capabilities` binds capabilities to exact action slots. ProcessProposal ignores warm verification labels. Its only verification reuse accepts an exact same-height proposal digest previously produced and verified by this validator's own PrepareProposal; otherwise it replays the full aggregate path. Production SRS loading fails closed, while insecure development SRS use is confined to explicit Orbis/test/fuzz configurations. The closed FFI, gRPC, host, mempool, ABCI, and app sink census plus adversarial regressions cover invalid proofs, extracted-cache hits, proposal handling, and state mutation. | Submitter or a future caller targets an artifact-only path, wrong family key, malformed aggregate statement, warm-cache label, or unrelated proposal digest to execute a proof not verified against its transaction-derived statement. | `composed` |
| `OUTGOING-VIEW-BINDING` | transfer, NoteReshape, shielded ICS-20 withdrawal | Every output's outgoing-view key is wrapped under the serialized aggregate action balance commitment, including synthetic padding and withdrawal change. | The circuit constrains the aggregate balance commitment to the complete input/output value relation. | Action construction passes the same aggregate commitment stored in the body to every `Note::encrypt_key` call; transaction payload-key recovery unwraps with that body commitment. Family regressions perform outgoing recovery from the serialized body. | Sender loses recovery of a real or synthetic output because construction and transaction scanning use different commitment contexts. | `composed` |
| `PLAN-BOUNDARY-CANONICALITY` | transfer, NoteReshape, shielded ICS-20 withdrawal | Every admitted or constructed shielded plan has canonical scalars/paths and one authoritative set of spends, outputs, blinding, and payload facts. Public bodies and balances are derived when building the action; they are not persisted duplicate caches. Transfer and Withdrawal also require one sender address and one identical sender compliance witness across spends. | The circuit binds the derived body and aligned private records to one shared public statement. | Checked protobuf decoders and plan validation reject aliases, missing proofs, wrong cardinality, mixed senders/anchors/compliance facts, and invalid payloads. Removed protobuf tags are reserved and stale body/balance fields are rejected rather than reconciled. | Attacker supplies a persisted/RPC plan with contradictory cached projections, malformed values, or facts that prove against a different action body. | `composed` |
| `FV-ARTIFACT-CANONICALITY` | all gnark-backed circuit families | Each receipt-bound proving key, binary verifying key, JSON verifying key, circuit-metadata document, and setup-provenance document has one accepted representation and one decoded value; no accepted raw/uncompressed key form, suffix, alternate JSON representation, field-modulus alias, or schema alias can change an artifact hash without changing its meaning. | Not applicable inside the arithmetic relation. | Key readers require EOF and byte-equality with canonical `WriteTo` reserialization. Canonical VK JSON requires in-range canonical decimal coordinates and rejects duplicate/unknown/trailing fields and formatting aliases. Circuit metadata v2 separately pins PK, binary VK, JSON VK, exact SR1CS, and setup provenance; receipt schema v4 binds metadata, branch-labelled proof case, and provenance. The provenance explicitly records that the setup transcript and toxic-waste erasure are not mechanically verified. | Build or release tooling supplies gnark's alternate raw key encoding, appends ignored bytes, rewrites JSON, uses a coordinate plus the field modulus, changes metadata fields while preserving a decoded key, or misreads key coherence as setup derivation evidence. | `composed` |
| `FV-PROFILE-COMPLETENESS` | all gnark runtime circuit families | The FV catalog is an exact shape-preserving partition of every family accepted by the prover runtime, and each profile's witness ABI, manifest accounting, artifact bindings, and certification status satisfy its declared policy. | Candidate manifests expose unclassified constraints explicitly; certified manifests require complete semantic classification and independent theorem roots. | Versioned family manifests deterministically project and parity-check Go runtime, Rust domain, proof-parameter, and aggregation registries. The profile checker requires exact runtime/ABI/catalog parity and recomputes segment/wire totals. | Developer adds or changes a family on any runtime/protocol surface, hides unclassified constraints, or promotes a profile without extending the closed certification backend. | `composed` |
| `SYMBOLIC-LEMMA-CLOSURE` | retained compliance research models | Every Tamarin lemma declared by a committed research model is named and verified by the release runner, and the runner names no absent or prefix-only lemma. This property is release-integrity for those artifacts; the legacy DLEQ models are not deployed Transfer evidence. | Not an arithmetic-circuit assertion. | The symbolic runner requires exact model/runner inventory equality and identifier-bounded verified output, with mutation self-tests. | A research-model change adds an unproved lemma or exploits name-prefix matching while leaving the artifact gate green. | `refined` |
| `NOTE-RESHAPE-LEAN-SOUNDNESS` | fixed-padded NoteReshape 1→8 and 8→1 (`note_reshape1x8`, `note_reshape8x1`) | V8 adds the current user root, a shared-owner five-field user leaf, and regulated-gated `Active` status. | V8 SR1CS and witnesses are pinned; the handwritten semantic layer must be extended before status-specific promotion. | Handlers require exact current roots, signature verification for every RK, live SCT membership, and state-transition preconditions. | Prover exploits an unproved V8 status row or semantic join. | `candidate` |
| `TRANSFER-LEAN-SOUNDNESS` | fixed Transfer 2x2 V20 | V20 authenticates sender and recipient five-field leaves with canonical address-derived scalars and requires both statuses to be `Active` when regulated. | V20 SR1CS, manifests, witnesses, and native/Go statement parity are pinned; the handwritten semantic layer must be extended before status-specific promotion. | Fixed body/key selection, current roots, historical SCT verification, signatures, timestamp, transaction-wide uniqueness, output persistence, and atomic effects remain external obligations. | Prover exploits an unproved V20 status row or statement projection. | `candidate` |
| `WITHDRAWAL-LEAN-SOUNDNESS` | fixed shielded ICS-20 Withdrawal 2x1 V12 | V12 authenticates the sender five-field leaf with its canonical address-derived scalar and requires `Active` status when regulated. | V12 SR1CS, manifests, both branch witnesses, and native/Go statement parity are pinned; the handwritten semantic layer must be extended before status-specific promotion. | Fixed body/key selection, current roots, historical SCT verification, signatures, current route checks, IBC execution, and atomicity remain external obligations. | Prover exploits an unproved V12 status row or semantic join. | `candidate` |
| `ZK-PROP-AMOUNT-RANGE-128` | all circuit families | Every amount is `< 2^128` on both sides of the circuit boundary: each amount has an explicit 128-bit decomposition, while Transfer's two pair sums use separate 129-bit radix-4 ladders. The Rust `Amount` wire form cannot represent `>= 2^128` nor alias a smaller value mod the curve order. | `TestAmountRangeBoundIs128Bits` (`tools/gnark/internal/circuits/amount_range_test.go`): `2^128 - 1` satisfiable and `2^128` unsatisfiable across the shared amount surfaces; conservation and Transfer net-balance tests pin the aggregate construction. | Rust `amount_is_structurally_below_2_pow_128` and `scalar_embedding_of_max_amount_does_not_wrap` (`crates/core/num/src/amount.rs`) reject out-of-range amounts before proving and pin the no-wrap embedding. | Prover inflates supply via a field-overflowing amount the abstract `Int` models never see, or a decomposition/aggregate-width regression silently widens the bound. | `refined` |
| `ZK-PROP-NOTE-RESHAPE-STATEMENT-SEAM` | NoteReshape | Rust statement serialization and Go circuit witness assignment agree on field order, encoding, and hash for the fixed-padded 1→8 and 8→1 shapes. | `TestRustGoStatementFieldDifferential` (`tools/gnark/internal/abi`) covers both retained shapes; NoteReshape circuit tests cover valid assignments and statement mutations. | Rust witness generation and Go reconstruction use the same manifest-driven shape and statement-field rules. | Prover exploits a Rust/Go wire-order, endianness, or ABI mismatch so a proof verifies against a differently parsed statement. | `refined` |
