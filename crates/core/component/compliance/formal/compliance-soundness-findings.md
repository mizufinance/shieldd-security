# Compliance Ciphertext Findings

| ID | Severity | Finding | Evidence | Required mitigation | Status |
| --- | --- | --- | --- | --- | --- |
| `CC-FIND-PUBLIC-SHARED-POINT` | Critical | The retired transport published an Orbis upload bundle containing the DH point that opened each encrypted tier seed. | V16 `TransferOutputBody` contains only `compliance_ciphertext` and `compliance_metadata`; evidence explicitly excludes PRE envelopes and DH points; scanner extraction stores only ciphertext and metadata. | Resolved by deleting the upload-bundle field and every deployed shared-point/DLEQ statement field. Keep wire-shape regressions and do not enable PRE without a non-disclosing protocol. | `resolved` |
| `CC-FIND-ORBIS-SEED-UNBOUND` | High | The retired duplicate AES envelope was not circuit-bound to the seed used by c2 and the Poseidon payload. | V16 has no seed envelope or upload package. `export_orbis_pending_scan` and `import_orbis_audit_entries` fail closed. | Resolved by deleting the unverifiable envelope and disabling Orbis v0. A future v1 must use one circuit-bound seed ciphertext. | `resolved` |
| `CC-FIND-C2-MALLEABILITY` | Medium | Standalone decode surfaces once allowed c2 reasoning independent of the accepted Transfer statement. | V16 commits every c2 in the 41-field statement and constrains `c2 = seed + compress(shared_secret)` in the same tier relation; the standalone PRE decode surface is absent. | Keep per-tier c2 mutation tests and exact Rust/Go statement parity. | `resolved` |
| `CC-FIND-UNUSED-STREAM-DOMAIN` | Medium | Tier encryption formerly ignored the declared stream-cipher domain. | `compliance_stream_block` uses `COMPLIANCE_STREAM_CIPHER_DOMAIN` across native Rust and Go/R1CS encryption paths. | Keep native/circuit parity and stream-word mutations. | `resolved` |
| `CC-FIND-DESIGNATED-VERIFIER-CLAIM` | Low | A closed-world correspondence model was previously presented as end-to-end confidentiality evidence. | V16 documentation scopes exact Lean refinement to algebraic constraints, records computational assumptions separately, and treats old DLEQ/Tamarin work as research-only. | Any future confidentiality certification must include attacker knowledge and the exact serialized transport. | `resolved` |
| `CC-FIND-DLEQ-WEAK-FS` | Medium | The generic Lean weak-Fiat-Shamir theorem did not originally bind an adversarially selected statement as strongly as the implementation transcript. | Research module `Dleq/FiatShamir.lean` now includes `dleq_fs_knowledge_soundness_strong`; residual FKMV non-malleability remains an assumption. DLEQ is not deployed by Transfer V16. | Complete the adversarial-statement reduction before reusing this research in any future deployed protocol. | `accepted-risk` |
| `CC-FIND-DLEQ-CHALLENGE-BIAS` | Low | A uniform 250-bit challenge idealization understated the truncation error by at most a 1.07 factor. | Research `Challenge.lean` proves injective embedding; the ledger states the quantitative `1.07/2^250` bound. No V16 statement field uses DLEQ. | Keep the corrected quantitative statement in the research artifact. | `resolved` |
| `CC-FIND-TRANSFER-IDENTITY-DIVERSIFIER-OWNERSHIP` | Critical | Transfer did not mirror Rust's non-identity checks for its authorization key or sender/receiver diversified generators. The identity generator collapses DTK derivation, so the same note commitment and state path can be opened under distinct nullifier keys and accepted with distinct nullifiers; the receiver path also allowed creation of such an ownership-ambiguous note. | Three exact one-row `x != 0` predicates now guard `Auth.AK`, the sender generator, and the receiver generator. `TestTransferIdentityDiversifierEnablesSameCommitmentDifferentNullifiersWithoutGuard` reproduces and closes the ownership alias, while `TestTransferCircuitRejectsIdentityAuthorizationAndDiversifiedGenerators` mutates all three fields in the full circuit. Independent Transfer semantics include the same `Common.Decaf.nonIdentity` obligations. | Keep the identity exclusions inside the authenticated circuit relation and exact FV contract; native construction or registry admission is not an adequate verifier-side substitute. | `resolved` |
| `CC-FIND-DETECTION-ASSET-FLAG-ALIAS` | High | The old detection plaintext combined asset and flag at field bit 253, which was not an injective pair encoding under field arithmetic and high asset bits. | V16 stores the exact asset separately and packs the Boolean flag into bit 32 of a separately range-constrained sender-slot word; native and circuit alias regressions cover both changes. | Resolved by the V16 plaintext layout and statement/witness domain bump. | `resolved` |
| `CC-FIND-ZERO-EPHEMERAL-SCALAR` | Medium | Honest construction could sample a zero tier scalar, yielding an identity EPK/shared secret and predictable seed mask. | Every tier uses rejection sampling, and a deterministic zero-then-one RNG regression requires a retry. | Resolved for honest construction; malicious creator disclosure remains out of scope. | `resolved` |
