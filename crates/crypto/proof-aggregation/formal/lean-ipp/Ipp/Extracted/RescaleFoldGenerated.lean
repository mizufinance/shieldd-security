-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def mul_helper
  {T : Type} {F : Type} (coreopsarithMulAssignInst : core.ops.arith.MulAssign T
  F) (corecloneCloneInst : core.clone.Clone T) (corecloneCloneInst1 :
  core.clone.Clone F) (t : T) (f : F) :
  Result T
  := do
  let clone ← corecloneCloneInst.clone t
  let t1 ← corecloneCloneInst1.clone f
  coreopsarithMulAssignInst.mul_assign clone t1
def gipa.rescale_fold_inner_loop.body
  {T : Type} {S : Type} (corecloneCloneInst : core.clone.Clone T)
  (coreopsarithAddInst : core.ops.arith.Add T T T) (coreopsarithMulAssignInst :
  core.ops.arith.MulAssign T S) (corecloneCloneInst1 : core.clone.Clone S)
  (scaled_half : Slice T) (unscaled_half : Slice T) (scalar : S)
  (iter : core.ops.range.Range Std.Usize) (folded : alloc.vec.Vec T) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec T))
    (alloc.vec.Vec T))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done folded)
  | some index =>
    let t ← Slice.index_usize scaled_half index
    let t1 ←
      mul_helper coreopsarithMulAssignInst corecloneCloneInst
        corecloneCloneInst1 t scalar
    let t2 ← Slice.index_usize unscaled_half index
    let t3 ← corecloneCloneInst.clone t2
    let t4 ← coreopsarithAddInst.add t1 t3
    let folded1 ← alloc.vec.Vec.push folded t4
    ok (cont (iter1, folded1))
def gipa.rescale_fold_inner_loop
  {T : Type} {S : Type} (corecloneCloneInst : core.clone.Clone T)
  (coreopsarithAddInst : core.ops.arith.Add T T T) (coreopsarithMulAssignInst :
  core.ops.arith.MulAssign T S) (corecloneCloneInst1 : core.clone.Clone S)
  (iter : core.ops.range.Range Std.Usize) (scaled_half : Slice T)
  (unscaled_half : Slice T) (scalar : S) (folded : alloc.vec.Vec T) :
  Result (alloc.vec.Vec T)
  := do
  loop
    (fun (iter1, folded1) => gipa.rescale_fold_inner_loop.body
      corecloneCloneInst coreopsarithAddInst coreopsarithMulAssignInst
      corecloneCloneInst1 scaled_half unscaled_half scalar iter1 folded1)
    (iter, folded)
def gipa.rescale_fold_inner
  {T : Type} {S : Type} (corecloneCloneInst : core.clone.Clone T)
  (coreopsarithAddInst : core.ops.arith.Add T T T) (coreopsarithMulAssignInst :
  core.ops.arith.MulAssign T S) (corecloneCloneInst1 : core.clone.Clone S)
  (scaled_half : Slice T) (unscaled_half : Slice T) (scalar : S) :
  Result (alloc.vec.Vec T)
  := do
  let i := Slice.len scaled_half
  let folded := alloc.vec.Vec.with_capacity T i
  let i1 := Slice.len scaled_half
  gipa.rescale_fold_inner_loop corecloneCloneInst coreopsarithAddInst
    coreopsarithMulAssignInst corecloneCloneInst1
    { start := 0#usize, «end» := i1 } scaled_half unscaled_half scalar folded

end ark_ip_proofs
