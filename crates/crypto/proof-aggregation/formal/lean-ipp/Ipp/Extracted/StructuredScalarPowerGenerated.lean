-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def tipa.structured_scalar_message.structured_scalar_power_loop.body
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (s : F) (iter : core.ops.range.Range Std.Usize)
  (powers : alloc.vec.Vec F) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec F))
    (alloc.vec.Vec F))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done powers)
  | some i =>
    let i1 ← i - 1#usize
    let t ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice F) powers i1
    let t1 ← corecloneCloneInst.clone t
    let t2 ← corecloneCloneInst.clone s
    let t3 ← coreopsarithMulInst.mul t1 t2
    let powers1 ← alloc.vec.Vec.push powers t3
    ok (cont (iter1, powers1))
def tipa.structured_scalar_message.structured_scalar_power_loop
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (iter : core.ops.range.Range Std.Usize) (s : F)
  (powers : alloc.vec.Vec F) :
  Result (alloc.vec.Vec F)
  := do
  loop
    (fun (iter1, powers1) =>
      tipa.structured_scalar_message.structured_scalar_power_loop.body
      corecloneCloneInst coreopsarithMulInst s iter1 powers1)
    (iter, powers)
def tipa.structured_scalar_message.structured_scalar_power
  {F : Type} (corecloneCloneInst : core.clone.Clone F)
  (num_traitsidentitiesOneInst : num_traits.identities.One F)
  (coreopsarithMulInst : core.ops.arith.Mul F F F) (num : Std.Usize)
  (s : F) :
  Result (alloc.vec.Vec F)
  := do
  let t ← num_traitsidentitiesOneInst.one
  let y ←
    lift (Std.Array.to_slice (Array.make 1#usize [ t ]))
  let ret := alloc.slice.Slice.into_vec y
  tipa.structured_scalar_message.structured_scalar_power_loop
    corecloneCloneInst coreopsarithMulInst { start := 1#usize, «end» := num }
    s ret

end ark_ip_proofs
