-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def applications.groth16_aggregation.inverse_powers_with_inverse_loop.body
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (r_inv : F) (iter : core.ops.range.Range Std.Usize)
  (powers : alloc.vec.Vec F) (current : F) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec F) ×
    F) (alloc.vec.Vec F))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done powers)
  | some _ =>
    let t ← corecloneCloneInst.clone current
    let powers1 ← alloc.vec.Vec.push powers t
    let t1 ← corecloneCloneInst.clone r_inv
    let current1 ← coreopsarithMulInst.mul current t1
    ok (cont (iter1, powers1, current1))
def applications.groth16_aggregation.inverse_powers_with_inverse_loop
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (iter : core.ops.range.Range Std.Usize) (r_inv : F)
  (powers : alloc.vec.Vec F) (current : F) :
  Result (alloc.vec.Vec F)
  := do
  loop
    (fun (iter1, powers1, current1) =>
      applications.groth16_aggregation.inverse_powers_with_inverse_loop.body
      corecloneCloneInst coreopsarithMulInst r_inv iter1 powers1 current1)
    (iter, powers, current)
def applications.groth16_aggregation.inverse_powers_with_inverse
  {F : Type} (corecloneCloneInst : core.clone.Clone F)
  (num_traitsidentitiesOneInst : num_traits.identities.One F)
  (coreopsarithMulInst : core.ops.arith.Mul F F F) (len : Std.Usize)
  (r_inv : F) :
  Result (alloc.vec.Vec F)
  := do
  let powers := alloc.vec.Vec.with_capacity F len
  let current ← num_traitsidentitiesOneInst.one
  applications.groth16_aggregation.inverse_powers_with_inverse_loop
    corecloneCloneInst coreopsarithMulInst { start := 0#usize, «end» := len }
    r_inv powers current

end ark_ip_proofs
