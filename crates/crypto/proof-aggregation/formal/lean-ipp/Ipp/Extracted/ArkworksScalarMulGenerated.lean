-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime
import Ipp.Extracted.ArkworksFqMulGenerated
import Ipp.Extracted.ArkworksFqOpsGenerated
import Ipp.Extracted.ArkworksFqInvGenerated
import Ipp.Extracted.ArkworksFqSqrtBytesGenerated
import Ipp.Extracted.ArkworksFq2Generated
import Ipp.Extracted.ArkworksFq6Generated
import Ipp.Extracted.ArkworksG1Generated
import Ipp.Extracted.ArkworksG2Generated

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def s3_07_arkworks_fq_spike.g1_mul_affine_step
  (accumulator : s3_07_arkworks_fq_spike.G1ProjMont)
  (base : s3_07_arkworks_fq_spike.G1AffineMont) (bit : Bool) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  let doubled ← s3_07_arkworks_fq_spike.g1_double accumulator
  if bit
  then s3_07_arkworks_fq_spike.g1_add_mixed doubled base
  else ok doubled
def s3_07_arkworks_fq_spike.g1_mul_affine_loop0_loop0.body
  (base : s3_07_arkworks_fq_spike.G1AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) (limb : Std.Usize)
  (accumulator : s3_07_arkworks_fq_spike.G1ProjMont) (started : Bool)
  (bit_index : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G1ProjMont × Bool × Std.Usize)
    (s3_07_arkworks_fq_spike.G1ProjMont × Bool))
  := do
  if bit_index > 0#usize
  then
    let bit_index1 ← bit_index - 1#usize
    let i ← MacCampaign.Array.index_usize scalar limb
    let i1 ← i >>> bit_index1
    let i2 ← lift (i1 &&& 1#u64)
    if started
    then
      let accumulator1 ←
        s3_07_arkworks_fq_spike.g1_mul_affine_step accumulator base (i2 =
          1#u64)
      ok (cont (accumulator1, true, bit_index1))
    else
      if i2 = 1#u64
      then
        let accumulator1 ←
          s3_07_arkworks_fq_spike.g1_mul_affine_step accumulator base true
        ok (cont (accumulator1, true, bit_index1))
      else ok (cont (accumulator, false, bit_index1))
  else ok (done (accumulator, started))
def s3_07_arkworks_fq_spike.g1_mul_affine_loop0_loop0
  (base : s3_07_arkworks_fq_spike.G1AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G1ProjMont) (started : Bool)
  (limb : Std.Usize) (bit_index : Std.Usize) :
  Result (s3_07_arkworks_fq_spike.G1ProjMont × Bool)
  := do
  loop
    (fun (accumulator1, started1, bit_index1) =>
      s3_07_arkworks_fq_spike.g1_mul_affine_loop0_loop0.body base scalar limb
      accumulator1 started1 bit_index1)
    (accumulator, started, bit_index)
def s3_07_arkworks_fq_spike.g1_mul_affine_loop0.body
  (base : s3_07_arkworks_fq_spike.G1AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G1ProjMont) (started : Bool)
  (limb : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G1ProjMont × Bool × Std.Usize)
    s3_07_arkworks_fq_spike.G1ProjMont)
  := do
  if limb > 0#usize
  then
    let limb1 ← limb - 1#usize
    let (accumulator1, started1) ←
      s3_07_arkworks_fq_spike.g1_mul_affine_loop0_loop0 base scalar accumulator
        started limb1 64#usize
    ok (cont (accumulator1, started1, limb1))
  else ok (done accumulator)
def s3_07_arkworks_fq_spike.g1_mul_affine_loop0
  (base : s3_07_arkworks_fq_spike.G1AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G1ProjMont) (started : Bool)
  (limb : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  loop
    (fun (accumulator1, started1, limb1) =>
      s3_07_arkworks_fq_spike.g1_mul_affine_loop0.body base scalar accumulator1
      started1 limb1)
    (accumulator, started, limb)
def s3_07_arkworks_fq_spike.g1_mul_affine
  (base : s3_07_arkworks_fq_spike.G1AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  let accumulator ← s3_07_arkworks_fq_spike.g1_zero
  s3_07_arkworks_fq_spike.g1_mul_affine_loop0 base scalar accumulator false
    4#usize
def s3_07_arkworks_fq_spike.g2_mul_projective_step
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont)
  (base : s3_07_arkworks_fq_spike.G2ProjMont) (bit : Bool) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  let doubled ← s3_07_arkworks_fq_spike.g2_double accumulator
  if bit
  then s3_07_arkworks_fq_spike.g2_add doubled base
  else ok doubled
def s3_07_arkworks_fq_spike.g2_mul_affine_step
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont)
  (base : s3_07_arkworks_fq_spike.G2AffineMont) (bit : Bool) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  let doubled ← s3_07_arkworks_fq_spike.g2_double accumulator
  if bit
  then s3_07_arkworks_fq_spike.g2_add_mixed doubled base
  else ok doubled
def s3_07_arkworks_fq_spike.g2_mul_projective_loop0_loop0.body
  (base : s3_07_arkworks_fq_spike.G2ProjMont) (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (limb : Std.Usize) (accumulator : s3_07_arkworks_fq_spike.G2ProjMont)
  (started : Bool) (bit_index : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G2ProjMont × Bool × Std.Usize)
    (s3_07_arkworks_fq_spike.G2ProjMont × Bool))
  := do
  if bit_index > 0#usize
  then
    let bit_index1 ← bit_index - 1#usize
    let i ← MacCampaign.Array.index_usize scalar limb
    let i1 ← i >>> bit_index1
    let i2 ← lift (i1 &&& 1#u64)
    if started
    then
      let accumulator1 ←
        s3_07_arkworks_fq_spike.g2_mul_projective_step accumulator base (i2 =
          1#u64)
      ok (cont (accumulator1, true, bit_index1))
    else
      if i2 = 1#u64
      then
        let accumulator1 ←
          s3_07_arkworks_fq_spike.g2_mul_projective_step accumulator base true
        ok (cont (accumulator1, true, bit_index1))
      else ok (cont (accumulator, false, bit_index1))
  else ok (done (accumulator, started))
def s3_07_arkworks_fq_spike.g2_mul_projective_loop0_loop0
  (base : s3_07_arkworks_fq_spike.G2ProjMont) (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont) (started : Bool)
  (limb : Std.Usize) (bit_index : Std.Usize) :
  Result (s3_07_arkworks_fq_spike.G2ProjMont × Bool)
  := do
  loop
    (fun (accumulator1, started1, bit_index1) =>
      s3_07_arkworks_fq_spike.g2_mul_projective_loop0_loop0.body base scalar
      limb accumulator1 started1 bit_index1)
    (accumulator, started, bit_index)
def s3_07_arkworks_fq_spike.g2_mul_projective_loop0.body
  (base : s3_07_arkworks_fq_spike.G2ProjMont) (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont) (started : Bool)
  (limb : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G2ProjMont × Bool × Std.Usize)
    s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  if limb > 0#usize
  then
    let limb1 ← limb - 1#usize
    let (accumulator1, started1) ←
      s3_07_arkworks_fq_spike.g2_mul_projective_loop0_loop0 base scalar
        accumulator started limb1 64#usize
    ok (cont (accumulator1, started1, limb1))
  else ok (done accumulator)
def s3_07_arkworks_fq_spike.g2_mul_projective_loop0
  (base : s3_07_arkworks_fq_spike.G2ProjMont) (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont) (started : Bool)
  (limb : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  loop
    (fun (accumulator1, started1, limb1) =>
      s3_07_arkworks_fq_spike.g2_mul_projective_loop0.body base scalar
      accumulator1 started1 limb1)
    (accumulator, started, limb)
def s3_07_arkworks_fq_spike.g2_mul_projective
  (base : s3_07_arkworks_fq_spike.G2ProjMont) (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  let accumulator ← s3_07_arkworks_fq_spike.g2_zero
  s3_07_arkworks_fq_spike.g2_mul_projective_loop0 base scalar accumulator false
    4#usize
def s3_07_arkworks_fq_spike.g2_mul_affine_loop0_loop0.body
  (base : s3_07_arkworks_fq_spike.G2AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) (limb : Std.Usize)
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont) (started : Bool)
  (bit_index : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G2ProjMont × Bool × Std.Usize)
    (s3_07_arkworks_fq_spike.G2ProjMont × Bool))
  := do
  if bit_index > 0#usize
  then
    let bit_index1 ← bit_index - 1#usize
    let i ← MacCampaign.Array.index_usize scalar limb
    let i1 ← i >>> bit_index1
    let i2 ← lift (i1 &&& 1#u64)
    if started
    then
      let accumulator1 ←
        s3_07_arkworks_fq_spike.g2_mul_affine_step accumulator base (i2 =
          1#u64)
      ok (cont (accumulator1, true, bit_index1))
    else
      if i2 = 1#u64
      then
        let accumulator1 ←
          s3_07_arkworks_fq_spike.g2_mul_affine_step accumulator base true
        ok (cont (accumulator1, true, bit_index1))
      else ok (cont (accumulator, false, bit_index1))
  else ok (done (accumulator, started))
def s3_07_arkworks_fq_spike.g2_mul_affine_loop0_loop0
  (base : s3_07_arkworks_fq_spike.G2AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont) (started : Bool)
  (limb : Std.Usize) (bit_index : Std.Usize) :
  Result (s3_07_arkworks_fq_spike.G2ProjMont × Bool)
  := do
  loop
    (fun (accumulator1, started1, bit_index1) =>
      s3_07_arkworks_fq_spike.g2_mul_affine_loop0_loop0.body base scalar limb
      accumulator1 started1 bit_index1)
    (accumulator, started, bit_index)
def s3_07_arkworks_fq_spike.g2_mul_affine_loop0.body
  (base : s3_07_arkworks_fq_spike.G2AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont) (started : Bool)
  (limb : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G2ProjMont × Bool × Std.Usize)
    s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  if limb > 0#usize
  then
    let limb1 ← limb - 1#usize
    let (accumulator1, started1) ←
      s3_07_arkworks_fq_spike.g2_mul_affine_loop0_loop0 base scalar accumulator
        started limb1 64#usize
    ok (cont (accumulator1, started1, limb1))
  else ok (done accumulator)
def s3_07_arkworks_fq_spike.g2_mul_affine_loop0
  (base : s3_07_arkworks_fq_spike.G2AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize)
  (accumulator : s3_07_arkworks_fq_spike.G2ProjMont) (started : Bool)
  (limb : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  loop
    (fun (accumulator1, started1, limb1) =>
      s3_07_arkworks_fq_spike.g2_mul_affine_loop0.body base scalar accumulator1
      started1 limb1)
    (accumulator, started, limb)
def s3_07_arkworks_fq_spike.g2_mul_affine
  (base : s3_07_arkworks_fq_spike.G2AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  let accumulator ← s3_07_arkworks_fq_spike.g2_zero
  s3_07_arkworks_fq_spike.g2_mul_affine_loop0 base scalar accumulator false
    4#usize
def s3_07_arkworks_fq_spike.extract_s3_28
  (g1_affine : s3_07_arkworks_fq_spike.G1AffineMont)
  (g2_projective : s3_07_arkworks_fq_spike.G2ProjMont)
  (g2_affine : s3_07_arkworks_fq_spike.G2AffineMont)
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) :
  Result (s3_07_arkworks_fq_spike.G1ProjMont ×
    s3_07_arkworks_fq_spike.G2ProjMont × s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  let gpm ← s3_07_arkworks_fq_spike.g1_mul_affine g1_affine scalar
  let gpm1 ← s3_07_arkworks_fq_spike.g2_mul_projective g2_projective scalar
  let gpm2 ← s3_07_arkworks_fq_spike.g2_mul_affine g2_affine scalar
  ok (gpm, gpm1, gpm2)

end ark_ip_proofs
