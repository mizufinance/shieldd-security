-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def applications.groth16_aggregation.build_shifted_ck_2_inner_loop.body
  {G : Type} {F : Type} (coreopsarithMulInst : core.ops.arith.Mul G F G)
  (ck_2 : Slice G) (inverse_powers : Slice F)
  (iter : core.ops.range.Range Std.Usize) (shifted : alloc.vec.Vec G) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec G))
    (alloc.vec.Vec G))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done shifted)
  | some index =>
    let t ← Slice.index_usize ck_2 index
    let t1 ← Slice.index_usize inverse_powers index
    let t2 ← coreopsarithMulInst.mul t t1
    let shifted1 ← alloc.vec.Vec.push shifted t2
    ok (cont (iter1, shifted1))
def applications.groth16_aggregation.build_shifted_ck_2_inner_loop
  {G : Type} {F : Type} (coreopsarithMulInst : core.ops.arith.Mul G F G)
  (iter : core.ops.range.Range Std.Usize) (ck_2 : Slice G)
  (inverse_powers : Slice F) (shifted : alloc.vec.Vec G) :
  Result (alloc.vec.Vec G)
  := do
  loop
    (fun (iter1, shifted1) =>
      applications.groth16_aggregation.build_shifted_ck_2_inner_loop.body
      coreopsarithMulInst ck_2 inverse_powers iter1 shifted1)
    (iter, shifted)
def applications.groth16_aggregation.build_shifted_ck_2_inner
  {G : Type} {F : Type} (coremarkerCopyInst : core.marker.Copy G)
  (coreopsarithMulInst : core.ops.arith.Mul G F G) (coremarkerCopyInst1 :
  core.marker.Copy F) (ck_2 : Slice G) (inverse_powers : Slice F) :
  Result (alloc.vec.Vec G)
  := do
  let i := Slice.len ck_2
  let shifted := alloc.vec.Vec.with_capacity G i
  let i1 := Slice.len ck_2
  applications.groth16_aggregation.build_shifted_ck_2_inner_loop
    coreopsarithMulInst { start := 0#usize, «end» := i1 } ck_2 inverse_powers
    shifted

end ark_ip_proofs
