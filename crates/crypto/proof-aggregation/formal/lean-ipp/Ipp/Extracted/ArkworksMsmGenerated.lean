-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime
import Ipp.Extracted.ArkworksFqMulGenerated
import Ipp.Extracted.ArkworksFqOpsGenerated
import Ipp.Extracted.ArkworksFqInvGenerated
import Ipp.Extracted.ArkworksFqSqrtBytesGenerated
import Ipp.Extracted.ArkworksFq2Generated
import Ipp.Extracted.ArkworksFq6Generated
import Ipp.Extracted.ArkworksG1Generated
import Ipp.Extracted.ArkworksG2Generated

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
structure s3_07_arkworks_fq_spike.WnafDigit where
  negative : Bool
  magnitude : MacCampaign.U64
def s3_07_arkworks_fq_spike.G1ProjMont.Insts.CoreCloneClone.clone
  (self : s3_07_arkworks_fq_spike.G1ProjMont) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  ok self
@[reducible]
def s3_07_arkworks_fq_spike.G1ProjMont.Insts.CoreCloneClone : core.clone.Clone
  s3_07_arkworks_fq_spike.G1ProjMont := {
  clone := s3_07_arkworks_fq_spike.G1ProjMont.Insts.CoreCloneClone.clone
}
def s3_07_arkworks_fq_spike.G2ProjMont.Insts.CoreCloneClone.clone
  (self : s3_07_arkworks_fq_spike.G2ProjMont) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  ok self
@[reducible]
def s3_07_arkworks_fq_spike.G2ProjMont.Insts.CoreCloneClone : core.clone.Clone
  s3_07_arkworks_fq_spike.G2ProjMont := {
  clone := s3_07_arkworks_fq_spike.G2ProjMont.Insts.CoreCloneClone.clone
}
def s3_07_arkworks_fq_spike.msm_window_size_loop.body
  (n : Std.Usize) (log : Std.Usize) :
  Result (ControlFlow (Std.Usize × Std.Usize) Std.Usize)
  := do
  if n > 1#usize
  then let n1 ← n >>> 1#i32
       let log1 ← log + 1#usize
       ok (cont (n1, log1))
  else ok (done log)
def s3_07_arkworks_fq_spike.msm_window_size_loop
  (n : Std.Usize) (log : Std.Usize) : Result Std.Usize := do
  loop
    (fun (n1, log1) => s3_07_arkworks_fq_spike.msm_window_size_loop.body n1
      log1)
    (n, log)
def s3_07_arkworks_fq_spike.msm_window_size
  (size : Std.Usize) : Result Std.Usize := do
  if size < 32#usize
  then ok 3#usize
  else
    let log ← s3_07_arkworks_fq_spike.msm_window_size_loop size 0#usize
    log + 2#usize
def s3_07_arkworks_fq_spike.make_wnaf_digits_loop.body
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) (w : Std.Usize) (radix : MacCampaign.U64)
  (window_mask : MacCampaign.U64) (digits_count : Std.Usize) (carry : MacCampaign.U64)
  (digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) (i : Std.Usize) :
  Result (ControlFlow (MacCampaign.U64 × (alloc.vec.Vec
    s3_07_arkworks_fq_spike.WnafDigit) × Std.Usize) (alloc.vec.Vec
    s3_07_arkworks_fq_spike.WnafDigit))
  := do
  if i < digits_count
  then
    let bit_offset ← i * w
    let u64_idx ← bit_offset / 64#usize
    let bit_idx ← bit_offset % 64#usize
    let i1 ← 64#usize - w
    let bit_buf ←
      if bit_idx < i1
      then do
           let i2 ← MacCampaign.Array.index_usize scalar u64_idx
           i2 >>> bit_idx
      else
        do
        let s ← lift (MacCampaign.Array.to_slice scalar)
        let i2 := Slice.len s
        let i3 ← i2 - 1#usize
        if u64_idx = i3
        then let i4 ← MacCampaign.Array.index_usize scalar u64_idx
             i4 >>> bit_idx
        else
          let i4 ← MacCampaign.Array.index_usize scalar u64_idx
          let i5 ← i4 >>> bit_idx
          let i6 ← 1#usize + u64_idx
          let i7 ← MacCampaign.Array.index_usize scalar i6
          let i8 ← 64#usize - bit_idx
          let i9 ← i7 <<< i8
          ok (i5 ||| i9)
    let i2 ← lift (bit_buf &&& window_mask)
    let coef ← carry + i2
    let i3 ← radix / 2#u64
    let i4 ← coef + i3
    let next_carry ← i4 >>> w
    let i5 ← digits_count - 1#usize
    let (last, magnitude) ←
      if i = i5
      then ok (true, coef)
      else
        do
        let i6 ← if next_carry = 0#u64
                   then ok coef
                   else radix - coef
        ok (false, i6)
    let negative ←
      if last
      then ok false
      else if next_carry != 0#u64
           then ok (magnitude != 0#u64)
           else ok false
    let digits1 ←
      alloc.vec.Vec.push digits ({ negative, magnitude } :
        s3_07_arkworks_fq_spike.WnafDigit)
    let i6 ← i + 1#usize
    ok (cont (next_carry, digits1, i6))
  else ok (done digits)
def s3_07_arkworks_fq_spike.make_wnaf_digits_loop
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) (w : Std.Usize) (radix : MacCampaign.U64)
  (window_mask : MacCampaign.U64) (digits_count : Std.Usize) (carry : MacCampaign.U64)
  (digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) (i : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  := do
  loop
    (fun (carry1, digits1, i1) =>
      s3_07_arkworks_fq_spike.make_wnaf_digits_loop.body scalar w radix
      window_mask digits_count carry1 digits1 i1)
    (carry, digits, i)
def s3_07_arkworks_fq_spike.make_wnaf_digits
  (scalar : MacCampaign.Array MacCampaign.U64 4#usize) (w : Std.Usize) (num_bits : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  := do
  let radix ← 1#u64 <<< w
  let window_mask ← radix - 1#u64
  let i ← num_bits + w
  let i1 ← i - 1#usize
  let digits_count ← i1 / w
  let digits :=
    alloc.vec.Vec.with_capacity s3_07_arkworks_fq_spike.WnafDigit digits_count
  s3_07_arkworks_fq_spike.make_wnaf_digits_loop scalar w radix window_mask
    digits_count 0#u64 digits 0#usize
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0_loop0.body
  (digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (j : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit))
  := do
  let i := alloc.vec.Vec.len digits
  if j < i
  then
    let wd ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.WnafDigit) digits j
    let all_digits1 ← alloc.vec.Vec.push all_digits wd
    let j1 ← j + 1#usize
    ok (cont (all_digits1, j1))
  else ok (done all_digits)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0_loop0
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) (j : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  := do
  loop
    (fun (all_digits1, j1) =>
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0_loop0.body digits
      all_digits1 j1)
    (all_digits, j)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0.body
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) (size : Std.Usize) (c : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (i : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit))
  := do
  if i < size
  then
    let a ← Slice.index_usize scalars i
    let digits ← s3_07_arkworks_fq_spike.make_wnaf_digits a c 253#usize
    let all_digits1 ←
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0_loop0 all_digits digits
        0#usize
    let i1 ← i + 1#usize
    ok (cont (all_digits1, i1))
  else ok (done all_digits)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) (size : Std.Usize) (c : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (i : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  := do
  loop
    (fun (all_digits1, i1) =>
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0.body scalars size c
      all_digits1 i1)
    (all_digits, i)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop0.body
  (bases : Slice s3_07_arkworks_fq_spike.G1AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (window : Std.Usize)
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (point : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont))
  := do
  if point < size
  then
    let i ← point * digits_count
    let i1 ← i + window
    let digit ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.WnafDigit) all_digits i1
    let buckets1 ←
      if digit.magnitude != 0#u64
      then
        do
        let i2 ← digit.magnitude - 1#u64
        let bucket ← lift (MacCampaign.castUsize i2)
        let base ←
          if digit.negative
          then
            do
            let gam ← Slice.index_usize bases point
            s3_07_arkworks_fq_spike.g1_affine_neg gam
          else Slice.index_usize bases point
        let gpm ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
            s3_07_arkworks_fq_spike.G1ProjMont) buckets bucket
        let gpm1 ← s3_07_arkworks_fq_spike.g1_add_mixed gpm base
        let (_, index_mut_back) ←
          alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
            s3_07_arkworks_fq_spike.G1ProjMont) buckets bucket
        ok (index_mut_back gpm1)
      else ok buckets
    let point1 ← point + 1#usize
    ok (cont (buckets1, point1))
  else ok (done buckets)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop0
  (bases : Slice s3_07_arkworks_fq_spike.G1AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (window : Std.Usize)
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (point : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  := do
  loop
    (fun (buckets1, point1) =>
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop0.body bases size
      digits_count all_digits window buckets1 point1)
    (buckets, point)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop1.body
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (running_sum : s3_07_arkworks_fq_spike.G1ProjMont)
  (result : s3_07_arkworks_fq_spike.G1ProjMont) (bucket : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G1ProjMont ×
    s3_07_arkworks_fq_spike.G1ProjMont × Std.Usize)
    s3_07_arkworks_fq_spike.G1ProjMont)
  := do
  if bucket > 0#usize
  then
    let bucket1 ← bucket - 1#usize
    let gpm ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.G1ProjMont) buckets bucket1
    let running_sum1 ← s3_07_arkworks_fq_spike.g1_add running_sum gpm
    let result1 ← s3_07_arkworks_fq_spike.g1_add result running_sum1
    ok (cont (running_sum1, result1, bucket1))
  else ok (done result)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop1
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (running_sum : s3_07_arkworks_fq_spike.G1ProjMont)
  (result : s3_07_arkworks_fq_spike.G1ProjMont) (bucket : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  loop
    (fun (running_sum1, result1, bucket1) =>
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop1.body buckets
      running_sum1 result1 bucket1)
    (running_sum, result, bucket)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1.body
  (bases : Slice s3_07_arkworks_fq_spike.G1AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (bucket_count : Std.Usize)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (window : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont))
  := do
  if window < digits_count
  then
    let gpm ← s3_07_arkworks_fq_spike.g1_zero
    let buckets ←
      alloc.vec.from_elem
        s3_07_arkworks_fq_spike.G1ProjMont.Insts.CoreCloneClone gpm
        bucket_count
    let buckets1 ←
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop0 bases size
        digits_count all_digits window buckets 0#usize
    let result ←
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1_loop1 buckets1 gpm gpm
        bucket_count
    let window_sums1 ← alloc.vec.Vec.push window_sums result
    let window1 ← window + 1#usize
    ok (cont (window_sums1, window1))
  else ok (done window_sums)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1
  (bases : Slice s3_07_arkworks_fq_spike.G1AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (bucket_count : Std.Usize) (window : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  := do
  loop
    (fun (window_sums1, window1) =>
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1.body bases size
      digits_count all_digits bucket_count window_sums1 window1)
    (window_sums, window)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2_loop0.body
  (c : Std.Usize) (result : s3_07_arkworks_fq_spike.G1ProjMont)
  (bit : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G1ProjMont × Std.Usize)
    s3_07_arkworks_fq_spike.G1ProjMont)
  := do
  if bit < c
  then
    let result1 ← s3_07_arkworks_fq_spike.g1_double result
    let bit1 ← bit + 1#usize
    ok (cont (result1, bit1))
  else ok (done result)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2_loop0
  (c : Std.Usize) (result : s3_07_arkworks_fq_spike.G1ProjMont)
  (bit : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  loop
    (fun (result1, bit1) =>
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2_loop0.body c result1 bit1)
    (result, bit)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2.body
  (c : Std.Usize)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (result : s3_07_arkworks_fq_spike.G1ProjMont) (window : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G1ProjMont × Std.Usize)
    s3_07_arkworks_fq_spike.G1ProjMont)
  := do
  if window > 1#usize
  then
    let window1 ← window - 1#usize
    let gpm ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.G1ProjMont) window_sums window1
    let result1 ← s3_07_arkworks_fq_spike.g1_add result gpm
    let result2 ←
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2_loop0 c result1 0#usize
    ok (cont (result2, window1))
  else ok (done result)
def s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2
  (c : Std.Usize)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G1ProjMont)
  (result : s3_07_arkworks_fq_spike.G1ProjMont) (window : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  loop
    (fun (result1, window1) =>
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2.body c window_sums result1
      window1)
    (result, window)
def s3_07_arkworks_fq_spike.g1_msm_unchecked
  (bases : Slice s3_07_arkworks_fq_spike.G1AffineMont)
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) :
  Result s3_07_arkworks_fq_spike.G1ProjMont
  := do
  let i := Slice.len bases
  let i1 := Slice.len scalars
  let size ← if i < i1
               then ok (Slice.len bases)
               else ok (Slice.len scalars)
  if size = 0#usize
  then s3_07_arkworks_fq_spike.g1_zero
  else
    let c ← s3_07_arkworks_fq_spike.msm_window_size size
    let i2 ← 253#usize + c
    let i3 ← i2 - 1#usize
    let digits_count ← i3 / c
    let i4 ← size * digits_count
    let all_digits :=
      alloc.vec.Vec.with_capacity s3_07_arkworks_fq_spike.WnafDigit i4
    let all_digits1 ←
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop0 scalars size c all_digits
        0#usize
    let window_sums :=
      alloc.vec.Vec.with_capacity s3_07_arkworks_fq_spike.G1ProjMont
        digits_count
    let bucket_count ← 1#usize <<< c
    let window_sums1 ←
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop1 bases size digits_count
        all_digits1 window_sums bucket_count 0#usize
    let lowest ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.G1ProjMont) window_sums1 0#usize
    let result ← s3_07_arkworks_fq_spike.g1_zero
    let result1 ←
      s3_07_arkworks_fq_spike.g1_msm_unchecked_loop2 c window_sums1 result
        digits_count
    s3_07_arkworks_fq_spike.g1_add lowest result1
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0_loop0.body
  (digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (j : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit))
  := do
  let i := alloc.vec.Vec.len digits
  if j < i
  then
    let wd ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.WnafDigit) digits j
    let all_digits1 ← alloc.vec.Vec.push all_digits wd
    let j1 ← j + 1#usize
    ok (cont (all_digits1, j1))
  else ok (done all_digits)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0_loop0
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) (j : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  := do
  loop
    (fun (all_digits1, j1) =>
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0_loop0.body digits
      all_digits1 j1)
    (all_digits, j)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0.body
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) (size : Std.Usize) (c : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (i : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit))
  := do
  if i < size
  then
    let a ← Slice.index_usize scalars i
    let digits ← s3_07_arkworks_fq_spike.make_wnaf_digits a c 253#usize
    let all_digits1 ←
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0_loop0 all_digits digits
        0#usize
    let i1 ← i + 1#usize
    ok (cont (all_digits1, i1))
  else ok (done all_digits)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) (size : Std.Usize) (c : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (i : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  := do
  loop
    (fun (all_digits1, i1) =>
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0.body scalars size c
      all_digits1 i1)
    (all_digits, i)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop0.body
  (bases : Slice s3_07_arkworks_fq_spike.G2AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (window : Std.Usize)
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (point : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont))
  := do
  if point < size
  then
    let i ← point * digits_count
    let i1 ← i + window
    let digit ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.WnafDigit) all_digits i1
    let buckets1 ←
      if digit.magnitude != 0#u64
      then
        do
        let i2 ← digit.magnitude - 1#u64
        let bucket ← lift (MacCampaign.castUsize i2)
        let base ←
          if digit.negative
          then
            do
            let gam ← Slice.index_usize bases point
            s3_07_arkworks_fq_spike.g2_affine_neg gam
          else Slice.index_usize bases point
        let gpm ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
            s3_07_arkworks_fq_spike.G2ProjMont) buckets bucket
        let gpm1 ← s3_07_arkworks_fq_spike.g2_add_mixed gpm base
        let (_, index_mut_back) ←
          alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
            s3_07_arkworks_fq_spike.G2ProjMont) buckets bucket
        ok (index_mut_back gpm1)
      else ok buckets
    let point1 ← point + 1#usize
    ok (cont (buckets1, point1))
  else ok (done buckets)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop0
  (bases : Slice s3_07_arkworks_fq_spike.G2AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (window : Std.Usize)
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (point : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  loop
    (fun (buckets1, point1) =>
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop0.body bases size
      digits_count all_digits window buckets1 point1)
    (buckets, point)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop1.body
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (running_sum : s3_07_arkworks_fq_spike.G2ProjMont)
  (result : s3_07_arkworks_fq_spike.G2ProjMont) (bucket : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G2ProjMont ×
    s3_07_arkworks_fq_spike.G2ProjMont × Std.Usize)
    s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  if bucket > 0#usize
  then
    let bucket1 ← bucket - 1#usize
    let gpm ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.G2ProjMont) buckets bucket1
    let running_sum1 ← s3_07_arkworks_fq_spike.g2_add running_sum gpm
    let result1 ← s3_07_arkworks_fq_spike.g2_add result running_sum1
    ok (cont (running_sum1, result1, bucket1))
  else ok (done result)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop1
  (buckets : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (running_sum : s3_07_arkworks_fq_spike.G2ProjMont)
  (result : s3_07_arkworks_fq_spike.G2ProjMont) (bucket : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  loop
    (fun (running_sum1, result1, bucket1) =>
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop1.body buckets
      running_sum1 result1 bucket1)
    (running_sum, result, bucket)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1.body
  (bases : Slice s3_07_arkworks_fq_spike.G2AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (bucket_count : Std.Usize)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (window : Std.Usize) :
  Result (ControlFlow ((alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont) ×
    Std.Usize) (alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont))
  := do
  if window < digits_count
  then
    let gpm ← s3_07_arkworks_fq_spike.g2_zero
    let buckets ←
      alloc.vec.from_elem
        s3_07_arkworks_fq_spike.G2ProjMont.Insts.CoreCloneClone gpm
        bucket_count
    let buckets1 ←
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop0 bases size
        digits_count all_digits window buckets 0#usize
    let result ←
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1_loop1 buckets1 gpm gpm
        bucket_count
    let window_sums1 ← alloc.vec.Vec.push window_sums result
    let window1 ← window + 1#usize
    ok (cont (window_sums1, window1))
  else ok (done window_sums)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1
  (bases : Slice s3_07_arkworks_fq_spike.G2AffineMont) (size : Std.Usize)
  (digits_count : Std.Usize)
  (all_digits : alloc.vec.Vec s3_07_arkworks_fq_spike.WnafDigit)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (bucket_count : Std.Usize) (window : Std.Usize) :
  Result (alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  loop
    (fun (window_sums1, window1) =>
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1.body bases size
      digits_count all_digits bucket_count window_sums1 window1)
    (window_sums, window)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2_loop0.body
  (c : Std.Usize) (result : s3_07_arkworks_fq_spike.G2ProjMont)
  (bit : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G2ProjMont × Std.Usize)
    s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  if bit < c
  then
    let result1 ← s3_07_arkworks_fq_spike.g2_double result
    let bit1 ← bit + 1#usize
    ok (cont (result1, bit1))
  else ok (done result)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2_loop0
  (c : Std.Usize) (result : s3_07_arkworks_fq_spike.G2ProjMont)
  (bit : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  loop
    (fun (result1, bit1) =>
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2_loop0.body c result1 bit1)
    (result, bit)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2.body
  (c : Std.Usize)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (result : s3_07_arkworks_fq_spike.G2ProjMont) (window : Std.Usize) :
  Result (ControlFlow (s3_07_arkworks_fq_spike.G2ProjMont × Std.Usize)
    s3_07_arkworks_fq_spike.G2ProjMont)
  := do
  if window > 1#usize
  then
    let window1 ← window - 1#usize
    let gpm ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.G2ProjMont) window_sums window1
    let result1 ← s3_07_arkworks_fq_spike.g2_add result gpm
    let result2 ←
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2_loop0 c result1 0#usize
    ok (cont (result2, window1))
  else ok (done result)
def s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2
  (c : Std.Usize)
  (window_sums : alloc.vec.Vec s3_07_arkworks_fq_spike.G2ProjMont)
  (result : s3_07_arkworks_fq_spike.G2ProjMont) (window : Std.Usize) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  loop
    (fun (result1, window1) =>
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2.body c window_sums result1
      window1)
    (result, window)
def s3_07_arkworks_fq_spike.g2_msm_unchecked
  (bases : Slice s3_07_arkworks_fq_spike.G2AffineMont)
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) :
  Result s3_07_arkworks_fq_spike.G2ProjMont
  := do
  let i := Slice.len bases
  let i1 := Slice.len scalars
  let size ← if i < i1
               then ok (Slice.len bases)
               else ok (Slice.len scalars)
  if size = 0#usize
  then s3_07_arkworks_fq_spike.g2_zero
  else
    let c ← s3_07_arkworks_fq_spike.msm_window_size size
    let i2 ← 253#usize + c
    let i3 ← i2 - 1#usize
    let digits_count ← i3 / c
    let i4 ← size * digits_count
    let all_digits :=
      alloc.vec.Vec.with_capacity s3_07_arkworks_fq_spike.WnafDigit i4
    let all_digits1 ←
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop0 scalars size c all_digits
        0#usize
    let window_sums :=
      alloc.vec.Vec.with_capacity s3_07_arkworks_fq_spike.G2ProjMont
        digits_count
    let bucket_count ← 1#usize <<< c
    let window_sums1 ←
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop1 bases size digits_count
        all_digits1 window_sums bucket_count 0#usize
    let lowest ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        s3_07_arkworks_fq_spike.G2ProjMont) window_sums1 0#usize
    let result ← s3_07_arkworks_fq_spike.g2_zero
    let result1 ←
      s3_07_arkworks_fq_spike.g2_msm_unchecked_loop2 c window_sums1 result
        digits_count
    s3_07_arkworks_fq_spike.g2_add lowest result1
def s3_07_arkworks_fq_spike.g1_msm
  (bases : Slice s3_07_arkworks_fq_spike.G1AffineMont)
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) :
  Result (core.result.Result s3_07_arkworks_fq_spike.G1ProjMont Std.Usize)
  := do
  let i := Slice.len bases
  let i1 := Slice.len scalars
  if i != i1
  then
    let i2 := Slice.len bases
    let i3 := Slice.len scalars
    if i2 < i3
    then let i4 := Slice.len bases
         ok (core.result.Result.Err i4)
    else let i4 := Slice.len scalars
         ok (core.result.Result.Err i4)
  else
    let gpm ← s3_07_arkworks_fq_spike.g1_msm_unchecked bases scalars
    ok (core.result.Result.Ok gpm)
def s3_07_arkworks_fq_spike.g2_msm
  (bases : Slice s3_07_arkworks_fq_spike.G2AffineMont)
  (scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) :
  Result (core.result.Result s3_07_arkworks_fq_spike.G2ProjMont Std.Usize)
  := do
  let i := Slice.len bases
  let i1 := Slice.len scalars
  if i != i1
  then
    let i2 := Slice.len bases
    let i3 := Slice.len scalars
    if i2 < i3
    then let i4 := Slice.len bases
         ok (core.result.Result.Err i4)
    else let i4 := Slice.len scalars
         ok (core.result.Result.Err i4)
  else
    let gpm ← s3_07_arkworks_fq_spike.g2_msm_unchecked bases scalars
    ok (core.result.Result.Ok gpm)
def s3_07_arkworks_fq_spike.extract_s3_30
  (g1_bases : Slice s3_07_arkworks_fq_spike.G1AffineMont)
  (g1_scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize))
  (g2_bases : Slice s3_07_arkworks_fq_spike.G2AffineMont)
  (g2_scalars : Slice (MacCampaign.Array MacCampaign.U64 4#usize)) :
  Result ((core.result.Result s3_07_arkworks_fq_spike.G1ProjMont Std.Usize) ×
    (core.result.Result s3_07_arkworks_fq_spike.G2ProjMont Std.Usize))
  := do
  let r ← s3_07_arkworks_fq_spike.g1_msm g1_bases g1_scalars
  let r1 ← s3_07_arkworks_fq_spike.g2_msm g2_bases g2_scalars
  ok (r, r1)

end ark_ip_proofs
