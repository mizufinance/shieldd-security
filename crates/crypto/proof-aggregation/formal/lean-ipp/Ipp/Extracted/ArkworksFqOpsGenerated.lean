-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime
import Ipp.Extracted.ArkworksFqMulGenerated

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def s3_07_arkworks_fq_spike.adc
  (left : MacCampaign.U64) (right : MacCampaign.U64) (carry : MacCampaign.U64) :
  Result s3_07_arkworks_fq_spike.Mac
  := do
  let i ← lift (MacCampaign.castU128 left)
  let i1 ← lift (MacCampaign.castU128 right)
  let i2 ← i + i1
  let i3 ← lift (MacCampaign.castU128 carry)
  let value ← i2 + i3
  let i4 ← lift (MacCampaign.castU64 value)
  let i5 ← value >>> 64#i32
  let i6 ← lift (MacCampaign.castU64 i5)
  ok { low := i4, carry := i6 }
def s3_07_arkworks_fq_spike.add_raw
  (left : MacCampaign.Array MacCampaign.U64 6#usize) (right : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result (MacCampaign.Array MacCampaign.U64 6#usize)
  := do
  let i ← MacCampaign.Array.index_usize left 0#usize
  let i1 ← MacCampaign.Array.index_usize right 0#usize
  let limb0 ← s3_07_arkworks_fq_spike.adc i i1 0#u64
  let i2 ← MacCampaign.Array.index_usize left 1#usize
  let i3 ← MacCampaign.Array.index_usize right 1#usize
  let limb1 ← s3_07_arkworks_fq_spike.adc i2 i3 limb0.carry
  let i4 ← MacCampaign.Array.index_usize left 2#usize
  let i5 ← MacCampaign.Array.index_usize right 2#usize
  let limb2 ← s3_07_arkworks_fq_spike.adc i4 i5 limb1.carry
  let i6 ← MacCampaign.Array.index_usize left 3#usize
  let i7 ← MacCampaign.Array.index_usize right 3#usize
  let limb3 ← s3_07_arkworks_fq_spike.adc i6 i7 limb2.carry
  let i8 ← MacCampaign.Array.index_usize left 4#usize
  let i9 ← MacCampaign.Array.index_usize right 4#usize
  let limb4 ← s3_07_arkworks_fq_spike.adc i8 i9 limb3.carry
  let i10 ← MacCampaign.Array.index_usize left 5#usize
  let i11 ← MacCampaign.Array.index_usize right 5#usize
  let limb5 ← s3_07_arkworks_fq_spike.adc i10 i11 limb4.carry
  ok
    (MacCampaign.Array.make 6#usize [
      limb0.low, limb1.low, limb2.low, limb3.low, limb4.low, limb5.low
      ])
def s3_07_arkworks_fq_spike.sub_raw
  (left : MacCampaign.Array MacCampaign.U64 6#usize) (right : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result (MacCampaign.Array MacCampaign.U64 6#usize)
  := do
  let i ← MacCampaign.Array.index_usize left 0#usize
  let i1 ← MacCampaign.Array.index_usize right 0#usize
  let limb0 ← s3_07_arkworks_fq_spike.sbb i i1 0#u64
  let i2 ← MacCampaign.Array.index_usize left 1#usize
  let i3 ← MacCampaign.Array.index_usize right 1#usize
  let (i4, i5) := limb0
  let limb1 ← s3_07_arkworks_fq_spike.sbb i2 i3 i5
  let i6 ← MacCampaign.Array.index_usize left 2#usize
  let i7 ← MacCampaign.Array.index_usize right 2#usize
  let (i8, i9) := limb1
  let limb2 ← s3_07_arkworks_fq_spike.sbb i6 i7 i9
  let i10 ← MacCampaign.Array.index_usize left 3#usize
  let i11 ← MacCampaign.Array.index_usize right 3#usize
  let (i12, i13) := limb2
  let limb3 ← s3_07_arkworks_fq_spike.sbb i10 i11 i13
  let i14 ← MacCampaign.Array.index_usize left 4#usize
  let i15 ← MacCampaign.Array.index_usize right 4#usize
  let (i16, i17) := limb3
  let limb4 ← s3_07_arkworks_fq_spike.sbb i14 i15 i17
  let i18 ← MacCampaign.Array.index_usize left 5#usize
  let i19 ← MacCampaign.Array.index_usize right 5#usize
  let (i20, i21) := limb4
  let (i22, _) ← s3_07_arkworks_fq_spike.sbb i18 i19 i21
  ok (MacCampaign.Array.make 6#usize [ i4, i8, i12, i16, i20, i22 ])
def s3_07_arkworks_fq_spike.gt
  (left : MacCampaign.Array MacCampaign.U64 6#usize) (right : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result Bool
  := do
  let i ← MacCampaign.Array.index_usize left 5#usize
  let i1 ← MacCampaign.Array.index_usize right 5#usize
  if i > i1
  then ok true
  else
    if i = i1
    then
      let i2 ← MacCampaign.Array.index_usize left 4#usize
      let i3 ← MacCampaign.Array.index_usize right 4#usize
      if i2 > i3
      then ok true
      else
        if i2 = i3
        then
          let i4 ← MacCampaign.Array.index_usize left 3#usize
          let i5 ← MacCampaign.Array.index_usize right 3#usize
          if i4 > i5
          then ok true
          else
            if i4 = i5
            then
              let i6 ← MacCampaign.Array.index_usize left 2#usize
              let i7 ← MacCampaign.Array.index_usize right 2#usize
              if i6 > i7
              then ok true
              else
                if i6 = i7
                then
                  let i8 ← MacCampaign.Array.index_usize left 1#usize
                  let i9 ← MacCampaign.Array.index_usize right 1#usize
                  if i8 > i9
                  then ok true
                  else
                    if i8 = i9
                    then
                      let i10 ← MacCampaign.Array.index_usize left 0#usize
                      let i11 ← MacCampaign.Array.index_usize right 0#usize
                      ok (i10 > i11)
                    else ok false
                else ok false
            else ok false
        else ok false
    else ok false
def s3_07_arkworks_fq_spike.add
  (a : s3_07_arkworks_fq_spike.FqMont) (b : s3_07_arkworks_fq_spike.FqMont) :
  Result s3_07_arkworks_fq_spike.FqMont
  := do
  let a1 ← s3_07_arkworks_fq_spike.add_raw a b
  let a2 ← s3_07_arkworks_fq_spike.subtract_modulus a1
  ok a2
def s3_07_arkworks_fq_spike.sub
  (a : s3_07_arkworks_fq_spike.FqMont) (b : s3_07_arkworks_fq_spike.FqMont) :
  Result s3_07_arkworks_fq_spike.FqMont
  := do
  let b1 ← s3_07_arkworks_fq_spike.gt b a
  let left ←
    if b1
    then s3_07_arkworks_fq_spike.add_raw a s3_07_arkworks_fq_spike.MODULUS
    else ok a
  let a1 ← s3_07_arkworks_fq_spike.sub_raw left b
  ok a1
def s3_07_arkworks_fq_spike.neg
  (a : s3_07_arkworks_fq_spike.FqMont) :
  Result s3_07_arkworks_fq_spike.FqMont
  := do
  let a1 := MacCampaign.Array.replicate 6#usize 0#u64
  let b ← core.array.equality.PartialEqArray.eq core.cmp.PartialEqU64 a a1
  if b
  then ok a
  else
    let a2 ←
      s3_07_arkworks_fq_spike.sub_raw s3_07_arkworks_fq_spike.MODULUS a
    ok a2
def s3_07_arkworks_fq_spike.square_reduce_round
  (r : MacCampaign.Array MacCampaign.U64 12#usize) (i : Std.Usize) (carry2 : MacCampaign.U64) :
  Result ((MacCampaign.Array MacCampaign.U64 12#usize) × MacCampaign.U64)
  := do
  let i1 ← MacCampaign.Array.index_usize r i
  let k ← lift (MacCampaign.wrappingMul64 i1 s3_07_arkworks_fq_spike.INV)
  let i2 ← MacCampaign.Array.index_usize s3_07_arkworks_fq_spike.MODULUS 0#usize
  let reduction0 ← s3_07_arkworks_fq_spike.mac i1 k i2 0#u64
  let i3 ← i + 1#usize
  let i4 ← MacCampaign.Array.index_usize r i3
  let i5 ← MacCampaign.Array.index_usize s3_07_arkworks_fq_spike.MODULUS 1#usize
  let reduction1 ← s3_07_arkworks_fq_spike.mac i4 k i5 reduction0.carry
  let r1 ← MacCampaign.Array.update r i3 reduction1.low
  let i6 ← i + 2#usize
  let i7 ← MacCampaign.Array.index_usize r1 i6
  let i8 ← MacCampaign.Array.index_usize s3_07_arkworks_fq_spike.MODULUS 2#usize
  let reduction2 ← s3_07_arkworks_fq_spike.mac i7 k i8 reduction1.carry
  let r2 ← MacCampaign.Array.update r1 i6 reduction2.low
  let i9 ← i + 3#usize
  let i10 ← MacCampaign.Array.index_usize r2 i9
  let i11 ← MacCampaign.Array.index_usize s3_07_arkworks_fq_spike.MODULUS 3#usize
  let reduction3 ← s3_07_arkworks_fq_spike.mac i10 k i11 reduction2.carry
  let r3 ← MacCampaign.Array.update r2 i9 reduction3.low
  let i12 ← i + 4#usize
  let i13 ← MacCampaign.Array.index_usize r3 i12
  let i14 ← MacCampaign.Array.index_usize s3_07_arkworks_fq_spike.MODULUS 4#usize
  let reduction4 ← s3_07_arkworks_fq_spike.mac i13 k i14 reduction3.carry
  let r4 ← MacCampaign.Array.update r3 i12 reduction4.low
  let i15 ← i + 5#usize
  let i16 ← MacCampaign.Array.index_usize r4 i15
  let i17 ← MacCampaign.Array.index_usize s3_07_arkworks_fq_spike.MODULUS 5#usize
  let reduction5 ← s3_07_arkworks_fq_spike.mac i16 k i17 reduction4.carry
  let r5 ← MacCampaign.Array.update r4 i15 reduction5.low
  let i18 ← i + 6#usize
  let i19 ← MacCampaign.Array.index_usize r5 i18
  let top ← s3_07_arkworks_fq_spike.adc i19 reduction5.carry carry2
  let r6 ← MacCampaign.Array.update r5 i18 top.low
  ok (r6, top.carry)
def s3_07_arkworks_fq_spike.square_loop0_loop0.body
  (i : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (a : s3_07_arkworks_fq_spike.FqMont) (r : MacCampaign.Array MacCampaign.U64 12#usize)
  (carry : MacCampaign.U64) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) ×
    s3_07_arkworks_fq_spike.FqMont × (MacCampaign.Array MacCampaign.U64 12#usize) × MacCampaign.U64)
    (s3_07_arkworks_fq_spike.FqMont × (MacCampaign.Array MacCampaign.U64 12#usize) × MacCampaign.U64))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done (a, r, carry))
  | some j =>
    let i1 ← i + j
    let i2 ← MacCampaign.Array.index_usize r i1
    let i3 ← MacCampaign.Array.index_usize a i
    let i4 ← MacCampaign.Array.index_usize a j
    let product ← s3_07_arkworks_fq_spike.mac i2 i3 i4 carry
    let a1 ← MacCampaign.Array.update r i1 product.low
    ok (cont (iter1, a, a1, product.carry))
def s3_07_arkworks_fq_spike.square_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (a : s3_07_arkworks_fq_spike.FqMont)
  (r : MacCampaign.Array MacCampaign.U64 12#usize) (carry : MacCampaign.U64) (i : Std.Usize) :
  Result (s3_07_arkworks_fq_spike.FqMont × (MacCampaign.Array MacCampaign.U64 12#usize) ×
    MacCampaign.U64)
  := do
  loop
    (fun (iter1, a1, r1, carry1) =>
      s3_07_arkworks_fq_spike.square_loop0_loop0.body i iter1 a1 r1 carry1)
    (iter, a, r, carry)
def s3_07_arkworks_fq_spike.square_loop0.body
  (iter : core.ops.range.Range Std.Usize) (a : s3_07_arkworks_fq_spike.FqMont)
  (r : MacCampaign.Array MacCampaign.U64 12#usize) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) ×
    s3_07_arkworks_fq_spike.FqMont × (MacCampaign.Array MacCampaign.U64 12#usize))
    (s3_07_arkworks_fq_spike.FqMont × (MacCampaign.Array MacCampaign.U64 12#usize)))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done (a, r))
  | some i =>
    let i1 ← i + 1#usize
    let (a1, r1, carry) ←
      s3_07_arkworks_fq_spike.square_loop0_loop0
        { start := i1, «end» := 6#usize } a r 0#u64 i
    let i2 ← 6#usize + i
    let a2 ← MacCampaign.Array.update r1 i2 carry
    ok (cont (iter1, a1, a2))
def s3_07_arkworks_fq_spike.square_loop0
  (iter : core.ops.range.Range Std.Usize) (a : s3_07_arkworks_fq_spike.FqMont)
  (r : MacCampaign.Array MacCampaign.U64 12#usize) :
  Result (s3_07_arkworks_fq_spike.FqMont × (MacCampaign.Array MacCampaign.U64 12#usize))
  := do
  loop
    (fun (iter1, a1, r1) => s3_07_arkworks_fq_spike.square_loop0.body iter1 a1
      r1)
    (iter, a, r)
def s3_07_arkworks_fq_spike.square_loop1.body
  (iter : core.ops.range.Range Std.Usize) (r : MacCampaign.Array MacCampaign.U64 12#usize) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (MacCampaign.Array MacCampaign.U64
    12#usize)) (MacCampaign.Array MacCampaign.U64 12#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done r)
  | some i =>
    let i1 ← 12#usize - i
    let i2 ← MacCampaign.Array.index_usize r i1
    let i3 ← i2 <<< 1#i32
    let i4 ← 11#usize - i
    let i5 ← MacCampaign.Array.index_usize r i4
    let i6 ← i5 >>> 63#i32
    let i7 ← lift (i3 ||| i6)
    let a ← MacCampaign.Array.update r i1 i7
    ok (cont (iter1, a))
def s3_07_arkworks_fq_spike.square_loop1
  (iter : core.ops.range.Range Std.Usize) (r : MacCampaign.Array MacCampaign.U64 12#usize) :
  Result (MacCampaign.Array MacCampaign.U64 12#usize)
  := do
  loop
    (fun (iter1, r1) => s3_07_arkworks_fq_spike.square_loop1.body iter1 r1)
    (iter, r)
def s3_07_arkworks_fq_spike.square_loop2.body
  (iter : core.ops.range.Range Std.Usize) (a : s3_07_arkworks_fq_spike.FqMont)
  (r : MacCampaign.Array MacCampaign.U64 12#usize) (carry : MacCampaign.U64) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) ×
    s3_07_arkworks_fq_spike.FqMont × (MacCampaign.Array MacCampaign.U64 12#usize) × MacCampaign.U64)
    (MacCampaign.Array MacCampaign.U64 12#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done r)
  | some i =>
    let i1 ← 2#usize * i
    let i2 ← MacCampaign.Array.index_usize r i1
    let i3 ← MacCampaign.Array.index_usize a i
    let diagonal ← s3_07_arkworks_fq_spike.mac i2 i3 i3 carry
    let r1 ← MacCampaign.Array.update r i1 diagonal.low
    let i4 ← i1 + 1#usize
    let i5 ← MacCampaign.Array.index_usize r1 i4
    let next ← s3_07_arkworks_fq_spike.adc i5 0#u64 diagonal.carry
    let i6 ← i1 + 1#usize
    let a1 ← MacCampaign.Array.update r1 i6 next.low
    ok (cont (iter1, a, a1, next.carry))
def s3_07_arkworks_fq_spike.square_loop2
  (iter : core.ops.range.Range Std.Usize) (a : s3_07_arkworks_fq_spike.FqMont)
  (r : MacCampaign.Array MacCampaign.U64 12#usize) (carry : MacCampaign.U64) :
  Result (MacCampaign.Array MacCampaign.U64 12#usize)
  := do
  loop
    (fun (iter1, a1, r1, carry1) => s3_07_arkworks_fq_spike.square_loop2.body
      iter1 a1 r1 carry1)
    (iter, a, r, carry)
def s3_07_arkworks_fq_spike.square_loop3.body
  (iter : core.ops.range.Range Std.Usize) (r : MacCampaign.Array MacCampaign.U64 12#usize)
  (carry2 : MacCampaign.U64) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (MacCampaign.Array MacCampaign.U64
    12#usize) × MacCampaign.U64) (MacCampaign.Array MacCampaign.U64 12#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done r)
  | some i =>
    let (r1, carry21) ←
      s3_07_arkworks_fq_spike.square_reduce_round r i carry2
    ok (cont (iter1, r1, carry21))
def s3_07_arkworks_fq_spike.square_loop3
  (iter : core.ops.range.Range Std.Usize) (r : MacCampaign.Array MacCampaign.U64 12#usize)
  (carry2 : MacCampaign.U64) :
  Result (MacCampaign.Array MacCampaign.U64 12#usize)
  := do
  loop
    (fun (iter1, r1, carry21) => s3_07_arkworks_fq_spike.square_loop3.body
      iter1 r1 carry21)
    (iter, r, carry2)
def s3_07_arkworks_fq_spike.square
  (a : s3_07_arkworks_fq_spike.FqMont) :
  Result s3_07_arkworks_fq_spike.FqMont
  := do
  let r := MacCampaign.Array.replicate 12#usize 0#u64
  let (a1, r1) ←
    s3_07_arkworks_fq_spike.square_loop0
      { start := 0#usize, «end» := 5#usize } a r
  let i ← MacCampaign.Array.index_usize r1 10#usize
  let i1 ← i >>> 63#i32
  let a2 ← MacCampaign.Array.update r1 11#usize i1
  let r2 ←
    s3_07_arkworks_fq_spike.square_loop1
      { start := 2#usize, «end» := 11#usize } a2
  let i2 ← MacCampaign.Array.index_usize r2 1#usize
  let i3 ← i2 <<< 1#i32
  let a3 ← MacCampaign.Array.update r2 1#usize i3
  let r3 ←
    s3_07_arkworks_fq_spike.square_loop2
      { start := 0#usize, «end» := 6#usize } a1 a3 0#u64
  let r4 ←
    s3_07_arkworks_fq_spike.square_loop3
      { start := 0#usize, «end» := 6#usize } r3 0#u64
  let i4 ← MacCampaign.Array.index_usize r4 6#usize
  let i5 ← MacCampaign.Array.index_usize r4 7#usize
  let i6 ← MacCampaign.Array.index_usize r4 8#usize
  let i7 ← MacCampaign.Array.index_usize r4 9#usize
  let i8 ← MacCampaign.Array.index_usize r4 10#usize
  let i9 ← MacCampaign.Array.index_usize r4 11#usize
  let a4 ←
    s3_07_arkworks_fq_spike.subtract_modulus
      (MacCampaign.Array.make 6#usize [ i4, i5, i6, i7, i8, i9 ])
  ok a4
def s3_07_arkworks_fq_spike.extract_f03b
  (a : s3_07_arkworks_fq_spike.FqMont) (b : s3_07_arkworks_fq_spike.FqMont) :
  Result (s3_07_arkworks_fq_spike.FqMont × s3_07_arkworks_fq_spike.FqMont ×
    s3_07_arkworks_fq_spike.FqMont × s3_07_arkworks_fq_spike.FqMont)
  := do
  let fm ← s3_07_arkworks_fq_spike.add a b
  let fm1 ← s3_07_arkworks_fq_spike.sub a b
  let fm2 ← s3_07_arkworks_fq_spike.neg a
  let fm3 ← s3_07_arkworks_fq_spike.square a
  ok (fm, fm1, fm2, fm3)

end ark_ip_proofs
