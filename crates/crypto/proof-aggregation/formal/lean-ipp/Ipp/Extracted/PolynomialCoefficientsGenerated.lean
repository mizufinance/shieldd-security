-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def tipa.polynomial_coefficients_from_transcript_loop0_loop0.body
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (factor : F)
  (iter : core.ops.range.Range Std.Usize) (coefficients : alloc.vec.Vec F) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec F))
    (alloc.vec.Vec F))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done coefficients)
  | some j =>
    let t ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice F)
        coefficients j
    let t1 ← corecloneCloneInst.clone t
    let t2 ← corecloneCloneInst.clone factor
    let t3 ← coreopsarithMulInst.mul t1 t2
    let coefficients1 ← alloc.vec.Vec.push coefficients t3
    ok (cont (iter1, coefficients1))
def tipa.polynomial_coefficients_from_transcript_loop0_loop0
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (iter : core.ops.range.Range Std.Usize)
  (coefficients : alloc.vec.Vec F) (factor : F) :
  Result (alloc.vec.Vec F)
  := do
  loop
    (fun (iter1, coefficients1) =>
      tipa.polynomial_coefficients_from_transcript_loop0_loop0.body
      corecloneCloneInst coreopsarithMulInst factor iter1 coefficients1)
    (iter, coefficients)
def tipa.polynomial_coefficients_from_transcript_loop0.body
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (transcript : alloc.vec.Vec F)
  (iter : core.ops.range.Range Std.Usize) (coefficients : alloc.vec.Vec F)
  (power_2_r : F) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec F) ×
    F) (alloc.vec.Vec F))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done coefficients)
  | some i =>
    let t ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice F) transcript
        i
    let x ← corecloneCloneInst.clone t
    let t1 ← corecloneCloneInst.clone power_2_r
    let factor ← coreopsarithMulInst.mul x t1
    let i1 ← lift (UScalar.cast .U32 i)
    let i2 ← core.num.Usize.pow 2#usize i1
    let coefficients1 ←
      tipa.polynomial_coefficients_from_transcript_loop0_loop0
        corecloneCloneInst coreopsarithMulInst
        { start := 0#usize, «end» := i2 } coefficients factor
    let t2 ← corecloneCloneInst.clone power_2_r
    let power_2_r1 ← coreopsarithMulInst.mul t2 power_2_r
    ok (cont (iter1, coefficients1, power_2_r1))
def tipa.polynomial_coefficients_from_transcript_loop0
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithMulInst :
  core.ops.arith.Mul F F F) (iter : core.ops.range.Range Std.Usize)
  (transcript : alloc.vec.Vec F) (coefficients : alloc.vec.Vec F)
  (power_2_r : F) :
  Result (alloc.vec.Vec F)
  := do
  loop
    (fun (iter1, coefficients1, power_2_r1) =>
      tipa.polynomial_coefficients_from_transcript_loop0.body
      corecloneCloneInst coreopsarithMulInst transcript iter1 coefficients1
      power_2_r1)
    (iter, coefficients, power_2_r)
def tipa.polynomial_coefficients_from_transcript_loop1.body
  {F : Type} (corecloneCloneInst : core.clone.Clone F)
  (num_traitsidentitiesZeroInst : num_traits.identities.Zero F)
  (coefficients : alloc.vec.Vec F) (logical_len : Std.Usize)
  (iter : core.ops.range.Range Std.Usize) (interleaved : alloc.vec.Vec F) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × (alloc.vec.Vec F))
    (alloc.vec.Vec F))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done interleaved)
  | some i =>
    let t ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice F)
        coefficients i
    let t1 ← corecloneCloneInst.clone t
    let interleaved1 ← alloc.vec.Vec.push interleaved t1
    let i1 ← i + 1#usize
    if i1 < logical_len
    then
      let t2 ← num_traitsidentitiesZeroInst.zero
      let interleaved2 ← alloc.vec.Vec.push interleaved1 t2
      ok (cont (iter1, interleaved2))
    else ok (cont (iter1, interleaved1))
def tipa.polynomial_coefficients_from_transcript_loop1
  {F : Type} (corecloneCloneInst : core.clone.Clone F)
  (num_traitsidentitiesZeroInst : num_traits.identities.Zero F)
  (iter : core.ops.range.Range Std.Usize) (coefficients : alloc.vec.Vec F)
  (logical_len : Std.Usize) (interleaved : alloc.vec.Vec F) :
  Result (alloc.vec.Vec F)
  := do
  loop
    (fun (iter1, interleaved1) =>
      tipa.polynomial_coefficients_from_transcript_loop1.body
      corecloneCloneInst num_traitsidentitiesZeroInst coefficients logical_len
      iter1 interleaved1)
    (iter, interleaved)
def tipa.polynomial_coefficients_from_transcript
  {F : Type} (corecloneCloneInst : core.clone.Clone F)
  (num_traitsidentitiesOneInst : num_traits.identities.One F)
  (num_traitsidentitiesZeroInst : num_traits.identities.Zero F)
  (coreopsarithMulInst : core.ops.arith.Mul F F F)
  (transcript : alloc.vec.Vec F) (r_shift : F) :
  Result (alloc.vec.Vec F)
  := do
  let t ← num_traitsidentitiesOneInst.one
  let y ←
    lift (Std.Array.to_slice (Array.make 1#usize [ t ]))
  let ret := alloc.slice.Slice.into_vec y
  let power_2_r ← corecloneCloneInst.clone r_shift
  let i := alloc.vec.Vec.len transcript
  let coefficients ←
    tipa.polynomial_coefficients_from_transcript_loop0 corecloneCloneInst
      coreopsarithMulInst { start := 0#usize, «end» := i } transcript ret
      power_2_r
  let logical_len := alloc.vec.Vec.len coefficients
  let i1 ← logical_len * 2#usize
  let i2 ← i1 - 1#usize
  let interleaved := alloc.vec.Vec.with_capacity F i2
  tipa.polynomial_coefficients_from_transcript_loop1 corecloneCloneInst
    num_traitsidentitiesZeroInst { start := 0#usize, «end» := logical_len }
    coefficients logical_len interleaved

end ark_ip_proofs
