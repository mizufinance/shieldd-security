-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime
import Ipp.Extracted.ArkworksFqMulGenerated
import Ipp.Extracted.ArkworksFqOpsGenerated

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
@[irreducible]
def s3_07_arkworks_fq_spike.R2 : MacCampaign.Array MacCampaign.U64 6#usize :=
  MacCampaign.Array.make 6#usize [
    13224372171368877346#u64, 227991066186625457#u64, 2496666625421784173#u64,
    13825906835078366124#u64, 9475172226622360569#u64, 30958721782860680#u64
    ]
def s3_07_arkworks_fq_spike.shr_join
  (low : MacCampaign.U64) (high : MacCampaign.U64) : Result MacCampaign.U64 := do
  let i ← low >>> 1#i32
  let i1 ← high <<< 63#i32
  ok (i ||| i1)
def s3_07_arkworks_fq_spike.div2
  (value : MacCampaign.Array MacCampaign.U64 6#usize) : Result (MacCampaign.Array MacCampaign.U64 6#usize) := do
  let i ← MacCampaign.Array.index_usize value 0#usize
  let i1 ← MacCampaign.Array.index_usize value 1#usize
  let i2 ← s3_07_arkworks_fq_spike.shr_join i i1
  let i3 ← MacCampaign.Array.index_usize value 2#usize
  let i4 ← s3_07_arkworks_fq_spike.shr_join i1 i3
  let i5 ← MacCampaign.Array.index_usize value 3#usize
  let i6 ← s3_07_arkworks_fq_spike.shr_join i3 i5
  let i7 ← MacCampaign.Array.index_usize value 4#usize
  let i8 ← s3_07_arkworks_fq_spike.shr_join i5 i7
  let i9 ← MacCampaign.Array.index_usize value 5#usize
  let i10 ← s3_07_arkworks_fq_spike.shr_join i7 i9
  let i11 ← i9 >>> 1#i32
  ok (MacCampaign.Array.make 6#usize [ i2, i4, i6, i8, i10, i11 ])
def s3_07_arkworks_fq_spike.is_even
  (value : MacCampaign.Array MacCampaign.U64 6#usize) : Result Bool := do
  let i ← MacCampaign.Array.index_usize value 0#usize
  let i1 ← i >>> 1#i32
  let i2 ← i1 <<< 1#i32
  ok (i2 = i)
def s3_07_arkworks_fq_spike.add_modulus_raw
  (value : MacCampaign.Array MacCampaign.U64 6#usize) : Result (MacCampaign.Array MacCampaign.U64 6#usize) := do
  s3_07_arkworks_fq_spike.add_raw value s3_07_arkworks_fq_spike.MODULUS
def s3_07_arkworks_fq_spike.half_coefficient_odd
  (value : MacCampaign.Array MacCampaign.U64 6#usize) : Result (MacCampaign.Array MacCampaign.U64 6#usize) := do
  let a ← s3_07_arkworks_fq_spike.add_modulus_raw value
  s3_07_arkworks_fq_spike.div2 a
def s3_07_arkworks_fq_spike.half_coefficient
  (value : MacCampaign.Array MacCampaign.U64 6#usize) : Result (MacCampaign.Array MacCampaign.U64 6#usize) := do
  let b ← s3_07_arkworks_fq_spike.is_even value
  if b
  then s3_07_arkworks_fq_spike.div2 value
  else s3_07_arkworks_fq_spike.half_coefficient_odd value
def s3_07_arkworks_fq_spike.inv_loop0_loop0.body
  (u : MacCampaign.Array MacCampaign.U64 6#usize) (b : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result (ControlFlow ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize))
    ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize)))
  := do
  let b1 ← s3_07_arkworks_fq_spike.is_even u
  if b1
  then
    let u1 ← s3_07_arkworks_fq_spike.div2 u
    let b2 ← s3_07_arkworks_fq_spike.half_coefficient b
    ok (cont (u1, b2))
  else ok (done (u, b))
def s3_07_arkworks_fq_spike.inv_loop0_loop0
  (u : MacCampaign.Array MacCampaign.U64 6#usize) (b : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize))
  := do
  loop
    (fun (u1, b1) => s3_07_arkworks_fq_spike.inv_loop0_loop0.body u1 b1)
    (u, b)
def s3_07_arkworks_fq_spike.inv_loop0_loop1.body
  (v : MacCampaign.Array MacCampaign.U64 6#usize) (c : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result (ControlFlow ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize))
    ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize)))
  := do
  let b ← s3_07_arkworks_fq_spike.is_even v
  if b
  then
    let v1 ← s3_07_arkworks_fq_spike.div2 v
    let c1 ← s3_07_arkworks_fq_spike.half_coefficient c
    ok (cont (v1, c1))
  else ok (done (v, c))
def s3_07_arkworks_fq_spike.inv_loop0_loop1
  (v : MacCampaign.Array MacCampaign.U64 6#usize) (c : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize))
  := do
  loop
    (fun (v1, c1) => s3_07_arkworks_fq_spike.inv_loop0_loop1.body v1 c1)
    (v, c)
def s3_07_arkworks_fq_spike.inv_loop0.body
  (one : MacCampaign.Array MacCampaign.U64 6#usize) (u : MacCampaign.Array MacCampaign.U64 6#usize)
  (v : MacCampaign.Array MacCampaign.U64 6#usize) (b : MacCampaign.Array MacCampaign.U64 6#usize)
  (c : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result (ControlFlow ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize) ×
    (MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize)) ((MacCampaign.Array MacCampaign.U64
    6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize)))
  := do
  let b1 ← core.array.equality.PartialEqArray.ne core.cmp.PartialEqU64 u one
  if b1
  then
    let b2 ←
      core.array.equality.PartialEqArray.ne core.cmp.PartialEqU64 v one
    if b2
    then
      let (u1, b3) ← s3_07_arkworks_fq_spike.inv_loop0_loop0 u b
      let (v1, c1) ← s3_07_arkworks_fq_spike.inv_loop0_loop1 v c
      let b4 ← s3_07_arkworks_fq_spike.gt u1 v1
      if b4
      then
        let u2 ← s3_07_arkworks_fq_spike.sub_raw u1 v1
        let fm ← s3_07_arkworks_fq_spike.sub b3 c1
        ok (cont (u2, v1, fm, c1))
      else
        let v2 ← s3_07_arkworks_fq_spike.sub_raw v1 u1
        let fm ← s3_07_arkworks_fq_spike.sub c1 b3
        ok (cont (u1, v2, b3, fm))
    else ok (done (u, b, c))
  else ok (done (u, b, c))
def s3_07_arkworks_fq_spike.inv_loop0
  (one : MacCampaign.Array MacCampaign.U64 6#usize) (u : MacCampaign.Array MacCampaign.U64 6#usize)
  (v : MacCampaign.Array MacCampaign.U64 6#usize) (b : MacCampaign.Array MacCampaign.U64 6#usize)
  (c : MacCampaign.Array MacCampaign.U64 6#usize) :
  Result ((MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64 6#usize) × (MacCampaign.Array MacCampaign.U64
    6#usize))
  := do
  loop
    (fun (u1, v1, b1, c1) => s3_07_arkworks_fq_spike.inv_loop0.body one u1 v1
      b1 c1)
    (u, v, b, c)
def s3_07_arkworks_fq_spike.inv
  (a : s3_07_arkworks_fq_spike.FqMont) :
  Result (Option s3_07_arkworks_fq_spike.FqMont)
  := do
  let a1 := MacCampaign.Array.replicate 6#usize 0#u64
  let b ← core.array.equality.PartialEqArray.eq core.cmp.PartialEqU64 a a1
  if b
  then ok none
  else
    let c := MacCampaign.Array.replicate 6#usize 0#u64
    let (u, b1, c1) ←
      s3_07_arkworks_fq_spike.inv_loop0
        (MacCampaign.Array.make 6#usize [ 1#u64, 0#u64, 0#u64, 0#u64, 0#u64, 0#u64 ]) a
        s3_07_arkworks_fq_spike.MODULUS s3_07_arkworks_fq_spike.R2 c
    let b2 ←
      core.array.equality.PartialEqArray.eq core.cmp.PartialEqU64 u
        (MacCampaign.Array.make 6#usize [ 1#u64, 0#u64, 0#u64, 0#u64, 0#u64, 0#u64 ])
    if b2
    then ok (some b1)
    else ok (some c1)

end ark_ip_proofs
