-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- Deterministically assembled by normalize-aeneas-lean-v7.
import Ipp.Extracted.AeneasRuntime

open Aeneas Aeneas.Std Result ControlFlow Error
open Std.Do
set_option linter.unusedVariables false
set_option maxHeartbeats 1000000
set_option maxRecDepth 2048

noncomputable section

namespace ark_ip_proofs
def tipa.synthetic_division_coefficients_loop.body
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithAddInst :
  core.ops.arith.Add F F F) (coreopsarithMulInst : core.ops.arith.Mul F F F)
  (coefficients : Slice F) (point : F) (descending : alloc.vec.Vec F)
  (index : Std.Usize) (accumulator : F) :
  Result (ControlFlow ((alloc.vec.Vec F) × Std.Usize × F) (alloc.vec.Vec F))
  := do
  if index > 1#usize
  then
    let index1 ← index - 1#usize
    let t ← Slice.index_usize coefficients index1
    let t1 ← corecloneCloneInst.clone t
    let t2 ← corecloneCloneInst.clone point
    let t3 ← coreopsarithMulInst.mul accumulator t2
    let accumulator1 ← coreopsarithAddInst.add t1 t3
    let t4 ← corecloneCloneInst.clone accumulator1
    let descending1 ← alloc.vec.Vec.push descending t4
    ok (cont (descending1, index1, accumulator1))
  else ok (done descending)
def tipa.synthetic_division_coefficients_loop
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithAddInst :
  core.ops.arith.Add F F F) (coreopsarithMulInst : core.ops.arith.Mul F F F)
  (coefficients : Slice F) (point : F) (descending : alloc.vec.Vec F)
  (index : Std.Usize) (accumulator : F) :
  Result (alloc.vec.Vec F)
  := do
  loop
    (fun (descending1, index1, accumulator1) =>
      tipa.synthetic_division_coefficients_loop.body corecloneCloneInst
      coreopsarithAddInst coreopsarithMulInst coefficients point descending1
      index1 accumulator1)
    (descending, index, accumulator)
def tipa.synthetic_division_coefficients
  {F : Type} (corecloneCloneInst : core.clone.Clone F) (coreopsarithAddInst :
  core.ops.arith.Add F F F) (coreopsarithMulInst : core.ops.arith.Mul F F F)
  (coefficients : Slice F) (point : F) :
  Result (alloc.vec.Vec F)
  := do
  let i := Slice.len coefficients
  if i <= 1#usize
  then ok (alloc.vec.Vec.new F)
  else
    let i1 := Slice.len coefficients
    let i2 ← i1 - 1#usize
    let descending := alloc.vec.Vec.with_capacity F i2
    let i3 := Slice.len coefficients
    let index ← i3 - 1#usize
    let t ← Slice.index_usize coefficients index
    let accumulator ← corecloneCloneInst.clone t
    let t1 ← corecloneCloneInst.clone accumulator
    let descending1 ← alloc.vec.Vec.push descending t1
    let descending2 ←
      tipa.synthetic_division_coefficients_loop corecloneCloneInst
        coreopsarithAddInst coreopsarithMulInst coefficients point descending1
        index accumulator
    let (s, deref_mut_back) ← lift (alloc.vec.Vec.deref_mut descending2)
    let s1 ← lift (core.slice.Slice.reverse s)
    ok (deref_mut_back s1)

end ark_ip_proofs
