# SnarkPack Operation-Count Reduction Register

This file is generated from `operation-reduction-register.json`.

## Symbolic v1 Baseline

- Domain: `n = 2^mu; successful shape-valid non-bench-baseline release core; Miller/MSM counts are logical submitted terms`.
- Prover: `hashes = open total; challenge-core Blake2b subtotal is between mu+4 and (mu+4)*2^64 queries on an accepted path`, `decoded_elements = open at the shipping construction boundary`, `g1_subgroup_validations = open at the shipping construction boundary`, `g2_subgroup_validations = open at the shipping construction boundary`, `gt_subgroup_validations = open at the shipping construction boundary`, `g1_scalar_multiplications = open total; the recurrence-proved primary-fold subset is 3*(n-1)`, `g2_scalar_multiplications = open total; the recurrence-proved primary-fold subset is 2*(n-1)`, `g1_msm_terms = open total; the KZG-opening subset is 2*n-1`, `g2_msm_terms = open total; the KZG-opening subset is 2*n-1`, `gt_exponentiations = open total`, `miller_terms = 12*n-8 in the successful release aggregate-construction core`, `final_exponentiations = 4+8*mu in the successful release aggregate-construction core`.
- Verifier: `hashes = open total; challenge-core Blake2b subtotal is between mu+4 and (mu+4)*2^64 queries on an accepted path`, `decoded_elements = 12+10*mu accepted-wire group elements, conditional on singleton identity-output shape`, `g1_subgroup_validations = 5+2*mu accepted-wire decode/validation sites; the Arkworks subgroup postcondition is an external conformance obligation`, `g2_subgroup_validations = 3 accepted-wire decode/validation sites; the Arkworks subgroup postcondition is an external conformance obligation`, `gt_subgroup_validations = 4+8*mu accepted-wire decode/validation sites; the Arkworks subgroup postcondition is an external conformance obligation`, `g1_scalar_multiplications = open total: depends on public-input arity and includes 2*mu GIPA scalar actions`, `g2_scalar_multiplications = open total`, `g1_msm_terms = open total`, `g2_msm_terms = open total`, `gt_exponentiations = open total; the recurrence-proved GIPA subset is 8*mu and the PPE adds a fixed GT scalar action`, `miller_terms = 10 logical input terms on the accepted verifier core path`, `final_exponentiations = 7 on the accepted verifier core path`.
- Backend: Every prover, verifier, and setup backend cost dimension is explicitly open. Semantic field, extension-field, reduction, and curve-operation correctness is governed separately by the verification manifest; absent cost formulas are unknown, not zero. Status: `open`.
- Implementation conformance: Lean proves only the named logical-term structural slice from abstract recurrences. Exact production cost conformance still requires source-to-recurrence evidence, accepted-wire counters, feature-boundary accounting, public-input-arity parameterization, retry-aware hash counts, and full shipping-vector accounting. This is an optimization-measurement boundary, not a semantic FV gap. Status: `open`.

## Formal Pareto Frontier

The listed exact-v1 candidates have audited value equivalence and a strict decrease in their registered operation vector with no positive registered delta. E2, E7, and E10 are retained measured Pareto trades and are deliberately not mislabeled as componentwise dominance.

## Ranked Research Order

1. `V2-0-TORUS-COMPRESSED-GT-WIRE` (versioned): default every protocol dimension = same as baseline after decoding; overrides: wrapped proof bytes = 1761+2432*mu, including 4+8*mu Fq6 torus coordinates; prover and verifier transport-codec backend arithmetic is an open added cost
2. `E10-ENDOMORPHISM-G1-G2-SUBGROUP` (exact-v1): default every protocol dimension = same as baseline; overrides at 5+2*mu G1 and 3 G2 sites: first test the proved-sound BLS12-377 relations phi(P)=[-x^2]P and psi(P)=[x]P, then run the original scalar check only when the relation fails. The accepted fast path records the exact double-and-add curve savings together with 11+2*mu field multiplications, 8+2*mu endomorphism maps, and 6 coordinate Frobenius maps; the all-fallback bound is baseline plus that filter cost
3. `E5-SHARED-PAIRING-PREPARATION` (exact-v1): default every dimension = same as baseline; overrides over mu GIPA rounds: prover.backend g1_normalization_batches and g1_prepared_elements decrease by mu and (n-1); g2_normalization_batches and g2_prepared_elements decrease by 2*mu and 2*(n-1); g1_normalized_elements decrease by (n-1); g2_normalized_elements decrease by 2*(n-1); every pairing output, Miller term, and final exponentiation remains independent and unchanged
4. `E1-SHARED-GT-MULTIEXP` (exact-v1): default every dimension = same as baseline; overrides: the four verifier GIPA GT recurrences are flattened into four (1+2*mu)-term variable-base GT MSMs sharing one scalar schedule; verifier.backend extension_field_multiplications/extension_field_squarings are benchmark-dependent open decreases; every protocol dimension remains the same
5. `E9-STREAM-PUBLIC-INPUT-FOLD` (exact-v1): default every dimension = same as baseline; overrides for k rows: when r != 1, use one streamed power recurrence for both the folded rows and the final r^k geometric-sum expression; when r = 1, fold rows directly without a power recurrence; both paths eliminate k-1 Fr multiplications
6. `E7-CYCLOTOMIC-FP12` (exact-v1): default every protocol dimension = same as baseline; overrides at every shipping BLS12-377 accepted-wire GT site: replace generic 253-bit x^r validation with nonzero, x^(q^4)*x=x^(q^2), and x^q=x^BLS_X; extension-field multiplication/squaring/Frobenius counts require a concrete backend vector
7. `E2-REGROUP-PAIRING-TERMS` (exact-v1): default every dimension = same as baseline; overrides for padded n=2^mu and authenticated real count m: prover.protocol miller_terms = 9*n+3*m-8, an exact decrease of 3*(n-m); final_exponentiations unchanged; prover.backend curve_additions add (n-m) G1 and (n-m) G2 key additions because the shared G2 suffix sum is reused by comA/comC; induced pairing backend dimensions remain open
8. `E3-REGROUP-MSM-COEFFICIENTS` (exact-v1): default every dimension = same as baseline; overrides: prover KZG-opening subset g1_msm_terms = 2*n-2 and g2_msm_terms = 2*n-2, exactly one fewer submitted term in each lane; every other protocol dimension remains the same

## Active Candidates

| ID | Class | Proposed vector | Prover delta | Verifier delta | Setup delta | Required theorem | Wire impact | Error | Boundary | Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `E1-SHARED-GT-MULTIEXP` | exact-v1 | default every dimension = same as baseline; overrides: the four verifier GIPA GT recurrences are flattened into four (1+2*mu)-term variable-base GT MSMs sharing one scalar schedule; verifier.backend extension_field_multiplications/extension_field_squarings are benchmark-dependent open decreases; every protocol dimension remains the same | default every protocol/backend dimension = 0; overrides: none | default every protocol/backend dimension = 0; overrides: extension_field_multiplications and extension_field_squarings = open negative | default every protocol/backend dimension = 0; overrides: none | Ipp.Optimization.shared_gt_fold_exact and shared_four_lane_fold_exact prove equality of all four flattened recurrences; Ipp.Cost.verifierSharedGtScheduled_exact_saving and verifierSharedGtScheduled_strictlyImproves prove exactly 3*(1+2*mu) fewer scalar decompositions with identical lane work | none; v1 proof bytes and challenge trace must be identical | 0 | Implemented in verify_tipp_mipp_challenge_prefix_core via fold_arkworks_gt_commitments_shared_schedule; the test oracle retains four independent Arkworks MSM calls | claims `OPTIMIZED-GT-FOLD-REFINEMENT`; Lean roots `Ipp.Optimization.shared_gt_fold_exact`, `Ipp.Optimization.shared_four_lane_fold_exact`, `Ipp.OptimizedGtFoldRefinement.optimizedLanes_eq_terminalFold`, `Ipp.OptimizedGtFoldRefinement.optimizedTuple_eq_terminalFold`, `Ipp.Extracted.AggregateVerifier.ArkworksTippPrimitiveExecutionContract.toKernel`, `Ipp.Cost.verifierSharedGtScheduled_exact_saving`, `Ipp.Cost.verifierSharedGtScheduled_strictlyImproves`; source digests pinned | `verified` |
| `E2-REGROUP-PAIRING-TERMS` | exact-v1 | default every dimension = same as baseline; overrides for padded n=2^mu and authenticated real count m: prover.protocol miller_terms = 9*n+3*m-8, an exact decrease of 3*(n-m); final_exponentiations unchanged; prover.backend curve_additions add (n-m) G1 and (n-m) G2 key additions because the shared G2 suffix sum is reused by comA/comC; induced pairing backend dimensions remain open | default every protocol/backend dimension = 0; overrides: miller_terms = -3*(n-m); curve_additions = +(n-m) G1 plus +(n-m) G2 at the typed group boundary; induced extension-field dimensions = open; final_exponentiations = 0 | default every protocol/backend dimension = 0; overrides: none unless a verifier site is separately specified and proved | default every protocol/backend dimension = 0; overrides: none | Ipp.Optimization.repeated_left_pairing_exact and repeated_right_pairing_exact prove value equality; Ipp.Cost.prover_padding_pairing_exact_saving and prover_padding_pairing_strictly_fewer prove the exact Miller delta | none | 0 | Implemented for comA/comB/comC in initial_commitments_profiled using authenticated real_count; malformed internal suffixes fall back to the historical full pairing path | claims `OPT-PADDING-PAIRING-COALESCING`; Lean roots `Ipp.Optimization.repeated_left_pairing_exact`, `Ipp.Optimization.repeated_right_pairing_exact`, `Ipp.Cost.prover_padding_pairing_exact_saving`, `Ipp.Cost.prover_padding_pairing_strictly_fewer`; source digests pinned | `verified` |
| `E3-REGROUP-MSM-COEFFICIENTS` | exact-v1 | default every dimension = same as baseline; overrides: prover KZG-opening subset g1_msm_terms = 2*n-2 and g2_msm_terms = 2*n-2, exactly one fewer submitted term in each lane; every other protocol dimension remains the same | default every protocol/backend dimension = 0; overrides: g1_msm_terms = -1 and g2_msm_terms = -1; induced curve_additions/curve_doublings are nonpositive but remain backend-open | default every protocol/backend dimension = 0; overrides: none | default every protocol/backend dimension = 0; overrides: none | Ipp.Optimization.omit_trailing_zero_msm_exact proves value equality; Ipp.Cost.prover_unpadded_kzg_exact_saving proves one fewer logical term per lane | none | 0 | Implemented in prove_commitment_key_kzg_opening_adapter_core; the full zero-padded coefficient vector remains in the extraction witness while the primitive receives only the nonzero-length prefix | claims `OPT-KZG-MSM-PREFIX`; Lean roots `Ipp.Optimization.omit_trailing_zero_msm_exact`, `Ipp.Cost.prover_exact_optimized_g1_kzg_msm_terms`, `Ipp.Cost.prover_exact_optimized_g2_kzg_msm_terms`, `Ipp.Cost.prover_unpadded_kzg_exact_saving`; source digests pinned | `verified` |
| `E5-SHARED-PAIRING-PREPARATION` | exact-v1 | default every dimension = same as baseline; overrides over mu GIPA rounds: prover.backend g1_normalization_batches and g1_prepared_elements decrease by mu and (n-1); g2_normalization_batches and g2_prepared_elements decrease by 2*mu and 2*(n-1); g1_normalized_elements decrease by (n-1); g2_normalized_elements decrease by 2*(n-1); every pairing output, Miller term, and final exponentiation remains independent and unchanged | default every protocol/backend dimension = 0; overrides: g1_normalization_batches = -mu, g2_normalization_batches = -2*mu, g1_normalized_elements = -(n-1), g2_normalized_elements = -2*(n-1), g1_prepared_elements = -(n-1), g2_prepared_elements = -2*(n-1); induced field and curve dimensions are open nonpositive decreases | default every protocol/backend dimension = 0; overrides: none | default every protocol/backend dimension = 0; overrides: none | Ipp.Optimization.shared_pairing_preparation_exact proves ordered com_a, com_b, ip_ab, and com_c equality under prepared-kernel conformance; Ipp.Cost.proverRoundPairingPreparationShared_savings and proverRoundPairingPreparationShared_strictlyImproves prove the exact normalization/preparation recurrence and strict vector decrease | none; all four pairing outputs and the derived round challenge must be byte-identical | 0 | Implemented in the Arkworks ProverRoundCommitmentPrimitive through typed PreparedG1/PreparedG2 values. Initial commitments remain excluded because their unequal padded operand lengths are modeled by E2 instead | claims `OPT-SHARED-PAIRING-PREPARATION`; Lean roots `Ipp.Optimization.shared_pairing_preparation_exact`, `Ipp.Cost.proverRoundPairingPreparationShared_savings`, `Ipp.Cost.proverRoundPairingPreparationShared_strictlyImproves`; source digests pinned | `verified` |
| `E7-CYCLOTOMIC-FP12` | exact-v1 | default every protocol dimension = same as baseline; overrides at every shipping BLS12-377 accepted-wire GT site: replace generic 253-bit x^r validation with nonzero, x^(q^4)*x=x^(q^2), and x^q=x^BLS_X; extension-field multiplication/squaring/Frobenius counts require a concrete backend vector | default every protocol/backend dimension = 0; overrides: extension_field_multiplications and extension_field_squarings = open at registered cyclotomic sites | default every protocol/backend dimension = 0; overrides: extension_field_multiplications, extension_field_squarings, and extension-field Frobenius work = open strict trade against each generic 253-bit validation exponentiation | default every protocol/backend dimension = 0; overrides: none | Ipp.Bls12377.fastGtUnitMembership_iff proves the concrete cyclotomic-plus-seed predicate iff x^r=1 using the exact BLS12-377 exponent gcd | none | 0 | Implemented by validate_bls12_377_gt_fast and validate_decoded_aggregate_proof; canonical bytes and strict failure behavior are unchanged. E10 now independently replaces the G1/G2 predicates | claims `OPT-FAST-GT-VALIDATION`; Lean roots `Ipp.Bls12377.fastGtUnitMembership_iff`; source digests pinned | `verified` |
| `E9-STREAM-PUBLIC-INPUT-FOLD` | exact-v1 | default every dimension = same as baseline; overrides for k rows: when r != 1, use one streamed power recurrence for both the folded rows and the final r^k geometric-sum expression; when r = 1, fold rows directly without a power recurrence; both paths eliminate k-1 Fr multiplications | default every protocol/backend dimension = 0; overrides: none | default every protocol/backend dimension = 0; overrides: field_multiplications = -(k-1) for both r = 1 and r != 1; removal of the temporary power vector is not counted | default every protocol/backend dimension = 0; overrides: none | Ipp.Optimization.streamed_weighted_sum_exact and streamed_terminal_power_exact prove the row and terminal-power equalities; Ipp.Cost.publicInputCoefficientStreamed_exact_saving proves exactly k-1 fewer field multiplications, with Rust parity covering both r branches and malformed shapes | none | 0 | fold_public_inputs_core, already included in the hax/Aeneas production extraction graph | claims `OPT-STREAM-PUBLIC-INPUT-FOLD`; Lean roots `Ipp.Optimization.streamed_weighted_sum_exact`, `Ipp.Optimization.streamed_terminal_power_exact`, `Ipp.Cost.publicInputCoefficientStreamed_exact_saving`; source digests pinned | `verified` |
| `E10-ENDOMORPHISM-G1-G2-SUBGROUP` | exact-v1 | default every protocol dimension = same as baseline; overrides at 5+2*mu G1 and 3 G2 sites: first test the proved-sound BLS12-377 relations phi(P)=[-x^2]P and psi(P)=[x]P, then run the original scalar check only when the relation fails. The accepted fast path records the exact double-and-add curve savings together with 11+2*mu field multiplications, 8+2*mu endomorphism maps, and 6 coordinate Frobenius maps; the all-fallback bound is baseline plus that filter cost | default every protocol/backend dimension = 0; overrides: none | accepted fast path: backend curve_additions = -(573+132*mu), curve_doublings = -(1197+252*mu), field_multiplications = +(11+2*mu), endomorphism_maps = +(8+2*mu), and frobenius_maps = +6; all-fallback bound: the baseline scalar work plus the full fast-filter vector | default every protocol/backend dimension = 0; overrides: none | Lean constructs the concrete G1 phi and G2 psi additive maps, proves each fast relation sound for Arkworks [r]P=0 membership, and proves (fast relation OR scalar fallback) iff [r]P=0; whole-list acceptance and first-failure position follow. Ipp.Cost proves both the accepted fast-path vector and the all-fallback bound | none | 0 | validate_bls12_377_g1_fast, validate_bls12_377_g2_fast, validate_decoded_aggregate_proof, and deserialize_compressed_strict_with; canonical reserialization and full consumption remain unchanged | claims `OPT-FAST-G1-G2-VALIDATION`; Lean roots `Ipp.FastSubgroupValidation.blsSeed_squared_exact`, `Ipp.Bls12377G1Endomorphism.g1_fast_sound`, `Ipp.Bls12377G2SubgroupSoundness.g2_fast_sound`, `Ipp.FastSubgroupValidation.g1VerifiedMembership_iff_arkworks`, `Ipp.FastSubgroupValidation.g2VerifiedMembership_iff_arkworks`, `Ipp.FastSubgroupValidation.fastCheck_eq_arkworksCheck`, `Ipp.FastSubgroupValidation.strictDecoder_failure_order_preserved`, `Ipp.FastSubgroupValidation.strictDecoder_acceptance_preserved`, `Ipp.Cost.verifierSubgroupValidationFast_exact_curve_savings`, `Ipp.Cost.verifierSubgroupValidationFast_exact_added_maps`, `Ipp.Cost.verifierSubgroupValidationFallbackWorstCase_exact`; source digests pinned | `verified` |
| `V2-0-TORUS-COMPRESSED-GT-WIRE` | versioned | default every protocol dimension = same as baseline after decoding; overrides: wrapped proof bytes = 1761+2432*mu, including 4+8*mu Fq6 torus coordinates; prover and verifier transport-codec backend arithmetic is an open added cost | default every protocol dimension = 0; overrides: wrapped proof bytes = -(1152+2304*mu); transport encoding adds one batched Fq6 inversion plus extension-field additions/multiplications whose complete backend vector remains open | default every protocol dimension = 0 after reconstruction and validation; overrides: wrapped proof bytes = -(1152+2304*mu); transport decoding adds one batched Fq6 inversion plus extension-field additions/multiplications whose complete backend vector remains open | default every protocol/backend dimension = 0; overrides: none | Ipp.TorusCompression proves the BLS12-377 torus chart, both inverse directions, injectivity, zero-sentinel semantics, and exclusion of a zero encoding for nonexceptional odd-order points; Ipp.TorusV2Optimization proves total cached decoding plus exact ordered all-target round trips for certified points and arbitrary coordinates; Ipp.TorusCoordinateSerialization proves strict canonical 288-byte Fq6 decoding; Ipp.TorusAggregateSerialization proves the exact v2 byte formula and strict saving | explicit disjoint v2 wrapper and half-size GT representation; the reconstructed v1 proof and transcript inputs are unchanged | 0; malformed coordinates, omitted -1, non-subgroup reconstructions, and invalid proof shapes fail closed | aggregate_family_torus_v2 validates the untyped AggregateProof before entering the torus chart; verify_family_aggregate_torus_v2 strictly decodes and validates the reconstructed regular proof; v1 entry points and bytes remain unchanged | claims `TORUS-V2-CODEC-REFINEMENT`, `TORUS-V2-STRICT-COORDINATE-WIRE`; Lean roots `Ipp.TorusCompression.decompress_isNormOne`, `Ipp.TorusCompression.compress_decompress`, `Ipp.TorusCompression.decompress_compress`, `Ipp.TorusCompression.compress_ne_zero_of_odd_torsion`, `Ipp.TorusCompression.decompress_injective_on_nonzero`, `Ipp.TorusCompression.bls12377_fq6V_nonsquare`, `Ipp.TorusCompression.bls12377_zero_sentinel_sound`, `Ipp.TorusAggregateSerialization.exact_saving`, `Ipp.TorusAggregateSerialization.wrappedLength_count48`, `Ipp.TorusAggregateSerialization.strictly_smaller`, `Ipp.TorusV2Optimization.decompressCached_eq_decompress`, `Ipp.TorusV2Optimization.decodeCoordinateCached_eq_decodeCoordinate`, `Ipp.TorusV2Optimization.decodeCoordinate_encodeCoordinate`, `Ipp.TorusV2Optimization.encode_decodeCoordinate`, `Ipp.TorusV2Optimization.decodeCoordinates_encodeCoordinates`, `Ipp.TorusCoordinateSerialization.decode_exact_consumption`, `Ipp.TorusCoordinateSerialization.decode_injective`, `Ipp.TorusCoordinateSerialization.decode_rejects_wrong_length`, `Ipp.TorusCoordinateSerialization.decode_rejects_noncanonical_field`; source digests pinned | `verified` |

## Rejected Paths

| ID | Path | Reason |
| --- | --- | --- |
| `E4-STREAMING-KZG-COEFFICIENTS` | default every dimension = same as baseline; overrides: prover.backend field_multiplications/field_squarings/field_inversions/field_reductions = candidate-specific and at least one must be strictly smaller under an exact fused streaming coefficient-to-quotient construction; allocations are excluded | The even-polynomial construction is formally exact and reduces the modeled field vector, but the affected stage is below 0.01% of count-48 aggregation and both allocation-free production variants regressed wall-clock time (Transfer +0.34%, NoteReshape +0.83%, ICS20 statistically flat). The production experiment was deleted; the algebraic theorem remains research evidence. |
| `E6-S3-STRICT-KERNEL-DOMINANCE` | default every protocol dimension = same as baseline; default every unaffected backend dimension = same; overrides: every backend dimension of the replaced kernel = an exact candidate formula, componentwise no larger with at least one strict decrease | This is an umbrella search category, not a concrete candidate: it names no replaced expression, exact kernel vector, or source boundary. Future S3 opportunities must be registered independently with their own equivalence and cost theorem instead of claiming progress under a non-falsifiable catch-all. |
| `E8-BATCH-CHALLENGE-INVERSION` | default every dimension = same as baseline; overrides: after checking each derived challenge for zero in historical order, batch-invert the mu round challenges and r using one Fr inversion and 3*mu Fr multiplications | The standalone batch-inversion algebra and cost vector are exact, and the isolated count-48 verifier experiment improved all three families by 0.27% to 0.56%. However, moving inversions across the verifier stages changed the extracted TippMippEffect from ordered per-stage inversion effects into prechecks plus a later batch effect, invalidating the load-bearing VerifyTippMipp refinement and downstream accepted-run projections. Repair requires a proof-stack redesign rather than a local kernel proof. The production helper, interface changes, and test were deleted; the standalone algebraic theorem remains research evidence. |
| `E11-SHARED-GIPA-FOLD-SCHEDULE` | default every protocol dimension = same as baseline; overrides: flatten com_z into a (1+2*mu)-term G1 MSM and expose one scalar-digit schedule to all four GT lanes and the G1 lane; concrete scalar-decomposition, field, and curve deltas remain open until the cross-group kernel is fixed | A five-lane G1/GT digit schedule is value-exact, but it changes com_z from Arkworks' GLV scalar multiplication to projective bucket accumulation and therefore introduces a curve-add/double trade that cannot satisfy this candidate's componentwise kernel criterion. The profitable four-GT shared decomposition was retained and closed under E1; the cross-group production experiment was deleted. |
| `P1-RANDOMIZED-GT-VALIDATION` | default every dimension = same as baseline; overrides: verifier.protocol gt_subgroup_validations = k; verifier hashes, GT scalar actions, and all induced backend dimensions = open added costs of randomized aggregation | E7 supplies an exact deterministic fast predicate at every GT site with zero added soundness error. Randomized aggregation would weaken malformed-element rejection, require fresh verifier randomness at a deterministic consensus boundary, add a new error term, and has no demonstrated operation or wall-clock advantage over the shipping E7 kernel. |
| `P2-RANDOMIZED-EQUATION-BATCH` | default every dimension = same as baseline; overrides: verifier.protocol final_exponentiations = 1 target; hashes, scalar multiplications, Miller terms, and all induced backend dimensions = open added or regrouped costs | The shipping verifier is deterministic and consensus-facing; it has no admissible fresh private randomness source. Fiat-Shamir-derived batching coefficients would change the acceptance proof and add a correlated random-linear-combination error across KZG, base-commitment, and PPE equations. That is a new protocol/security reduction, not an exact optimization for this branch. |
| `V2-1-COMBINE-GIPA-GT-OBJECTS` | default every declared role/layer/dimension = open for the new protocol; target overrides: verifier decoded_elements/gt_subgroup_validations/gt_exponentiations and prover GT commitment work strictly decrease after combination work is included | Combining transcript-visible GT commitments changes the proof shape and requires a new binding or random-combination argument. E1 and E7 already reduce the v1 fold and validation work without a wire or soundness change; a combined-object protocol belongs in a separately scoped v2 design. |
| `V2-2-BATCHED-KZG-OPENING` | default every declared role/layer/dimension = open for the new protocol; target overrides: one batched or multi-opening with exact prover MSM/field work, verifier Miller/final-exponentiation work, and setup additions | The two openings use distinct G1/G2 commitment-key lanes. A combined opening needs a concrete two-key relation, augmented setup, transcript, binding reduction, and new proof encoding. No such construction is present, so it is outside the exact-v1 optimization branch. |
| `V2-3-RAGGED-FOLDING` | default every declared role/layer/dimension = open as a function of real count m and ragged shape; target overrides: padded-only prover folds/MSM/pairing terms are absent and every ragged-boundary operation is counted | Ragged folding changes the statement cardinality, round schedule, transcript, and proof encoding. E2 already removes the dominant pairing work caused by repeat-final padding while preserving v1. A ragged protocol requires an independent completeness and soundness development. |
| `V2-4-HETEROGENEOUS-MULTI-VK` | default every declared role/layer/dimension = open as a function of proofs, keys, and families; target overrides: one protocol-level aggregate pays fixed commitment/opening/PPE work once and counts multi-key binding/routing work | A heterogeneous aggregate changes the authenticated statement and needs canonical multi-key routing plus a cross-key substitution proof. The current goal is one v1 verifying-key family per aggregate, so this is a separate product/protocol project rather than an implementation optimization. |
| `V2-5-ALTERNATIVE-ACCUMULATION` | default every declared role/layer/dimension = open for the replacement construction; target override: prover.protocol miller_terms is asymptotically or concretely smaller after all replacement commitments/openings and setup are counted | No concrete replacement construction, operation vector, setup, or security reduction is specified. An asymptotic target alone is not an implementable or formally falsifiable candidate; any future accumulator must enter as its own protocol with representative-corpus evidence. |
| `V2-6-NATIVE-GROTH16-BATCH-LOWER-BOUND` | default every declared role/layer/dimension = open for randomized native Groth16 batching; target overrides: aggregation prover commitments/GIPA/KZG are absent, while verifier decoding, randomized scalar work, Miller terms, and errors scale with all n proofs | Native randomized Groth16 batching transports all n proofs and removes SnarkPack's compression objective. It is a comparison protocol with verifier work linear in n, not an optimization of the aggregate proof path, and it inherits the same unavailable fresh-randomness issue as P2. |
| `V2-7-RECURSIVE-OR-BLOCK-ACCUMULATION` | default every declared role/layer/dimension = open for the recursive or accumulated protocol; target overrides: at least one external-verifier dimension strictly decreases while recursive prover, circuit, accumulator, and setup work are all explicit | Recursive or block accumulation introduces a new circuit, prover, setup, accumulator state machine, proof format, and security stack. No concrete BLS12-377 implementation or same-corpus operation vector is available, so it cannot be evaluated as an in-place SnarkPack optimization. |
| `R1-RUNTIME-TUNING` | default every declared role/layer/dimension = same as baseline; overrides: none; threading, scheduling, caches, tables, bucket tiling, native fallback, and deployment changes are outside the operation vector | This campaign reduces operation counts, not runtime settings or orchestration. |
| `R2-SINGLE-HARDWARE-SCORE` | default every declared dimension = discarded into one hardware-dependent scalar; no componentwise vector remains | A scalar score hides role and primitive tradeoffs. |
| `R3-REFERENCE-AS-SPECIFICATION` | default every declared role/layer/dimension = not-applicable; treat the Rust reference implementation as normative | The reference remains an independent falsification oracle only. |
| `R4-UNJUSTIFIED-RANDOM-BATCHING` | default every dimension = same as baseline; claimed verifier reductions are unspecified and unsupported because fresh independence, cofactor analysis, and full malformed-position coverage are absent | The required BLS12-377 cofactor, independence, and malformed-element arguments are absent. |
| `R5-SILENT-V1-BYTE-CHANGE` | default every declared role/layer/dimension = open because the changed protocol was not specified; proof or transcript bytes change while retaining the v1 label | Any byte or transcript change is a separately specified protocol version. |
| `R6-WALL-CLOCK-ONLY-SELECTION` | default every declared role/layer/dimension = open; select a v2 candidate only from benchmark time without a formal vector | Wall-clock measurements neither prove soundness nor operation-vector improvement. |
| `R7-POST-HOC-RUNTIME-BATCHING` | default every declared role/layer/dimension = same as baseline per aggregate; overrides: none; aggregates are combined only after protocol verification | The protocol itself must pay fixed commitment, opening, and PPE work once. |
