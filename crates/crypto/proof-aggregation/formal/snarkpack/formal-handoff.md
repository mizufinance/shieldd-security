# SnarkPack Formal Verification Evidence

This file is generated from `verification-manifest.json`. Do not edit claim or assumption tables by hand.

## Scope

`SHIPPING-TO-GOAL` is `proved` at `Ipp.ShippingConcreteToGoal.output_derived_shipping_call_to_goal_quantitative`. `FULL-ADAPTIVE-END-TO-END-FV` is a kernel-proved conditional theorem at `Ipp.ShippingFullAdaptiveEndToEnd.shipping_full_adaptive_end_to_end_fv`. Its prover result is accepted-execution partial correctness, and its security result bounds an iterated fork transform rather than invalid-acceptance probability directly. It is conditional on the named cryptographic, implementation, translator, and query-bound assumptions below. `DEPLOYED-SRS-SOUNDNESS` remains explicitly open because no production ceremony is registered; it blocks any instantiated deployment security level.

## Claims

| ID | Claim | Root | Depends on | Assumes | Evidence | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `GOAL-ORDER` | The independent goal exposes exact positional equality, so omission or reordering changes the represented ordered rows. | `Ipp.Goal.no_omission_or_reordering` | none | none | Kernel-audited theorem in the import-isolated Goal module. | `proved` |
| `V1-ABSTRACT-ACCEPTS` | SnarkPackV1.Accepts is exactly the existing abstract Fiat-Shamir acceptance relation. | `Ipp.SnarkPackV1.accepts_eq_fsAccepts` | none | none | Definitional equality in the import-isolated v1 module. | `proved` |
| `OPTIMIZED-GT-FOLD-REFINEMENT` | The concrete four-lane shared-GT primitive equation on the exact roots, reversed wire rounds, and chronological inverse/raw challenges entails the complete terminal TIPP fold contract used by the extracted verifier. | `Ipp.Extracted.AggregateVerifier.ArkworksTippPrimitiveExecutionContract.toKernel` | none | none | Kernel-audited refinement. OptimizedGtFoldRefinement identifies each signed-window lane with its chronological sequential fold and proves the exact four-tuple equals terminalFold. ArkworksTippPrimitiveExecutionContract.toKernel uses that theorem to construct all four ArkworksTippKernelContract fields. This proves the adapter algebra, but deliberately does not assert that the concrete Rust primitive satisfies its execution equation; that remaining source-to-model boundary is ARKWORKS-TIPP-PRIMITIVE-SEMANTICS. | `proved` |
| `TORUS-V2-CODEC-REFINEMENT` | For every ordered list of certified norm-one points in an odd-order subgroup, torus-v2 encoding followed by total zero-sentinel/cached-chart decoding preserves the exact order, multiplicity, and point values; every raw coordinate re-encodes exactly. | `Ipp.TorusV2Optimization.decodeCoordinates_encodeCoordinates` | none | none | Kernel-audited algebra covers both chart directions, the zero identity sentinel, exclusion of the exceptional -1 point by odd-order torsion, cached decoder equality, all-target traversal and length preservation. TorusCoordinateSerialization separately audits exact 288-byte consumption, byte injectivity, wrong-length rejection, and noncanonical rejection for all six Fq limbs. TorusAggregateSerialization audits the complete 4+8μ target inventory and exact wrapper size. The Rust encoder now validates the untyped AggregateProof before entering the chart; no trusted-check-erasure premise remains. | `proved` |
| `TORUS-V2-STRICT-COORDINATE-WIRE` | A successfully decoded torus-v2 coordinate consumes exactly 288 bytes, is byte-injective at a fixed Fq6 value, and rejects wrong lengths or a noncanonical value in any of its six base-field limbs. | `Ipp.TorusCoordinateSerialization.decode_injective` | none | none | Kernel-audited strict Fq6 decoder reuses the established canonical 48-byte BLS12-377 field decoder componentwise. The companion audited roots establish exact consumption, wrong-length rejection, and all-limb noncanonical rejection. | `proved` |
| `OPT-PADDING-PAIRING-COALESCING` | Coalescing equal left or right pairing operands is value-exact, and authenticated padding removes exactly 3·(n-m) logical prover Miller terms. | `Ipp.Optimization.repeated_left_pairing_exact` | none | none | Kernel-audited algebraic equality; the companion audited repeated_right_pairing_exact and prover_padding_pairing exact-saving/strictly-fewer roots cover both operand orientations and the registered count delta. | `proved` |
| `OPT-KZG-MSM-PREFIX` | Omitting the known trailing zero coefficient preserves the KZG MSM value and removes exactly one submitted term in each G1 and G2 lane. | `Ipp.Optimization.omit_trailing_zero_msm_exact` | none | none | Kernel-audited equality plus exact G1/G2 optimized-term and saving roots in ProofAuditOptimization. | `proved` |
| `OPT-SHARED-PAIRING-PREPARATION` | Sharing normalization and preparation of repeated GIPA round operands preserves all four ordered pairing outputs and strictly reduces the registered preparation vector. | `Ipp.Optimization.shared_pairing_preparation_exact` | none | none | Kernel-audited ordered output equality under the explicit prepared-kernel premise, with companion exact savings and strict-improvement roots. | `proved` |
| `OPT-FAST-GT-VALIDATION` | The BLS12-377 fast GT unit-membership predicate is equivalent to the established target-group membership condition. | `Ipp.Bls12377.fastGtUnitMembership_iff` | none | none | Kernel-audited concrete Fq12 unit/cyclotomic membership equivalence; strict Rust boundary parity and failure order remain source-pinned executable evidence. | `proved` |
| `OPT-STREAM-PUBLIC-INPUT-FOLD` | Streaming the weighted public-input sum and terminal power preserves the exact verifier fold values while removing the materialized coefficient vector. | `Ipp.Optimization.streamed_weighted_sum_exact` | none | none | Kernel-audited weighted-sum equality with companion terminal-power equality and an exact field-multiplication saving root. | `proved` |
| `OPT-FAST-G1-G2-VALIDATION` | The concrete fast BLS12-377 G1 and G2 subgroup predicates agree with Arkworks membership and preserve strict-decoder acceptance and failure order. | `Ipp.FastSubgroupValidation.fastCheck_eq_arkworksCheck` | none | none | Kernel-audited concrete endomorphism soundness and verified-membership equivalences, together with strict-decoder acceptance/failure-order roots and exact cost-vector accounting. | `proved` |
| `V1-ORDERED-PROJECTION` | A v1 proof represents an ordered hidden witness exactly when its proof-owned root commitments open to that witness; the public claim is definitionally the statement's ordered rows. | `Ipp.SnarkPackV1.Refinement.represents_iff_rootOpens` | GOAL-ORDER | none | Kernel-audited opening equivalence together with the import-isolated toGoal/publicRowAt definitions. | `proved` |
| `V1-GOAL-PPE` | Validity of the projected independent goal is equivalent to one proof-root-bound ordered witness satisfying every Groth16 PPE. | `Ipp.SnarkPackV1.Refinement.hasValidRepresentation_iff` | V1-ORDERED-PROJECTION | none | Kernel-audited existential root-opening/PPE equivalence; validWithWitness_iff_all_ppe separately audits the exact Fin-indexed row relation. | `proved` |
| `REAL-PREFIX-GOAL` | Validity of exactly the first realCount ordered statements retains the proof's full padded root opening, and invalidity of that real prefix implies invalidity of the padded v1 representation. | `Ipp.SnarkPackV1.Refinement.invalid_realPrefix_implies_invalid_padded` | V1-GOAL-PPE | none | Kernel-audited Goal prefix construction and v1 refinement. Ipp.SnarkPackV1.Refinement.hasValidRealPrefixRepresentation_iff characterizes the target as one full root-bound padded witness plus PPE validity at exactly the first realCount positions; invalid_realPrefix_implies_invalid_padded supplies the soundness premise consumed by S1. | `proved` |
| `S1-QUANTITATIVE-SOUNDNESS` | In the exact full-depth fork experiment, invalidity of the ordered protocol-independent goal bounds the S1 fork expression by the sum of the explicit leaf-local KZG false-opening and GIPA fork-knowledge bad-event probabilities. | `Ipp.S1.invalid_goal_fork_bound_le_extraction_advantage` | V1-GOAL-PPE, REAL-PREFIX-GOAL | ADVERSARY-QUERY-BOUND, KZG-FALSE-OPENING-SECURITY, GIPA-FORK-KNOWLEDGE-SOUNDNESS | Kernel-audited S1Computational composition. s1Fork_success_lower_bound exposes the exact full-depth fork probability; s1_computational_soundness extracts a root-bound PPE-valid witness outside S1KzgBad and S1PairingBad; invalid_goal_fork_bound_le_extraction_advantage gives the ordered-goal contrapositive, and invalid_proof_fork_bound_le_extraction_advantage gives the fixed-proof form. No globally injective finite-group commitment map or universally binding KZG equation is used by this claim. | `proved` |
| `DEPLOYED-SRS-SOUNDNESS` | The exact SRS selected by every shipping v1 call is sampled from a setup distribution whose KZG trapdoors are not known or reproducible by the adversary, and its authenticated identifier binds the complete proving and verifier material used by that call. | `UNPROVED.deployed_srs_unknown_trapdoor_and_registry_binding` | none | none | Open because no production ceremony artifact is registered. The shipping application calls the fail-closed registered-SRS loaders in ordinary and release production builds; deterministic DevSrs remains only behind test, fuzzing, or the explicitly insecure Orbis integration feature. The Orbis feature is forwarded only by pd and enabled only by the integration build recipe, so it does not weaken the production loader boundary. The machine-readable deployed_srs_evidence object records the unregistered state, exact production source owners, required postcondition, and closure route. Ipp.S1.publicTrapdoors_forge_both_lanes proves why the public deterministic fixture cannot be promoted: knowledge of its shifted-base scalars gives deterministic accepted false openings in both concrete KZG lanes. Closure requires an identified ceremony owner, independently verified transcript, canonical BLS12-377 artifact, raw artifact and transcript digests, registered SRS identifier, and an audited deployed setup-binding theorem. Rotating the SRS necessarily changes the identifier and aggregate proof/trace fixtures; existing proof bytes cannot be preserved across that setup change. | `open` |
| `PUBLIC-TRAPDOOR-KZG-COUNTEREXAMPLE` | For concrete BLS12-377 KZG parameters satisfying gBeta = beta*g and hAlpha = alpha*h with nonzero public bases, knowledge of beta and alpha deterministically constructs accepted false openings in both V and W lanes. | `Ipp.S1.publicTrapdoors_forge_both_lanes` | none | none | DeployedSrsCounterexample constructs zero-coefficient forgeries at beta-1 and alpha-1, proves both concrete pairing equations, and proves each claimed key differs from the honest zero MSM. This is a mathematical counterexample to public-trapdoor setup soundness; the separate open DEPLOYED-SRS-SOUNDNESS row records that the deterministic test, fuzzing, and Orbis integration DevSrs exposes exactly these scalars through a public reproducible seed and therefore cannot be promoted to production evidence. | `proved` |
| `KZG-LEAF-REDUCTION` | For one fixed BLS12-377 reduction statement, the exact S1 V- and W-lane false-opening events reduce without a semantic leaf classifier to two fork-preserving concrete target games under exact statement/setup parameter binding. | `Ipp.S1.kzg_false_opening_to_fixed_statement_bls12377_fork_games` | S1-QUANTITATIVE-SOUNDNESS, DEPLOYED-SRS-SOUNDNESS | ARKWORKS-KZG-VERIFIER-EQUATIONS, BLS12-377-PAIRING-BILINEARITY | Proved reduction theorem, audited at the exact ProofAuditReductions root. S1Bls12377FixedStatementKzg retains the complete S1 fork, maps the exact V/W false-opening events structurally to separate concrete BLS12-377 fork-target games, binds the formal statement to fixed public parameters through universal Arkworks equations, and proves that both game wrappers add zero oracle queries. No semantic leaf classifier, single-leaf selection, shipping acceptance conclusion, or numerical KZG bound is hidden in the theorem. KZG-FALSE-OPENING-SECURITY is therefore not an assumption of this reduction claim: it remains a separate downstream assumption that must bound these whole-fork targets or expose every batch-to-single/fork-selection loss. The required production instantiation remains open through DEPLOYED-SRS-SOUNDNESS; the public-trapdoor DevSrs cannot instantiate a negligible target-game bound. | `proved` |
| `GIPA-FORK-KNOWLEDGE-REDUCTION` | For one fixed BLS12-377 reduction statement, the exact S1 root-opening and product-lane failures reduce to two full-FsAccepts accepted-fork knowledge games using an extractor selected from the original adversary before the fork is constructed, with no added oracle queries. | `Ipp.S1.gipa_fork_knowledge_to_prefork_accepted_bls12377_games` | S1-QUANTITATIVE-SOUNDNESS | GIPA-FORK-KNOWLEDGE-SOUNDNESS | Proved conditional reduction theorem, audited at the exact ProofAuditReductions root. Bls12377GipaForkAccepts requires full FsAccepts at every projected fork leaf, and support of the actual S1 fork experiment derives that condition independently of either knowledge failure. Bls12377GipaPreForkKnowledgeSecurity selects extractorFor from the original fixed-statement adversary before either fork game is formed; its two assumption fields bound only the separately defined accepted root-opening and product-lane games. gipa_fork_knowledge_to_prefork_accepted_bls12377_games then bounds the exact S1 pairing bad event by those two advantages and transports the same total oracle-query budget through witness-only maps. The theorem does not prove local polynomial running time, instantiate either game advantage, bind a deployed SRS, or absorb heterogeneous adaptive statement-selection/replay loss. The acceptsFork := True fixed-setup route remains exploratory and is not audited as closure evidence. | `proved` |
| `APP-STATE-MACHINE` | The extracted application verifier plans exact full call identifiers, accepts exactly one successful result for every expected identifier, is invariant under result permutation, and has identical normal/profiled acceptance. | `Ipp.Extracted.AppVerifierStateMachine.app_reduce_accepts_iff_all_expected_accept` | none | AENEAS-TRANSLATOR | Kernel-audited state-machine roots cover the generated family/call comparators, plan and segment projection, family counts, identity and padding checks, reducer model, exact nonduplicating plan, permutation invariance, and normal/profiled parity. | `proved` |
| `APP-SHIPPING-BINDING` | For fixed formal call data, an accepted extracted application execution plus exact production scalar-projection construction facts derives one AcceptedShippingCallFacts witness with exact identity, count, family, and padding facts; no free ShippingApplicationProjectionContract is required. | `Ipp.Extracted.AppVerifierStateMachine.app_acceptance_binds_shipping_input` | APP-STATE-MACHINE, CANONICAL-STATEMENT-BINDING | AENEAS-TRANSLATOR | Kernel-audited conditional composition of the extracted planner, identity and padding checks, fail-closed result reducer, canonical family-code witness, and exact StatementBindingContract count and repeat-final-padding postconditions. The production-used app_verify_shipping_projection_core equation derives ShippingApplicationProjectionContract, removing that free semantic premise. Complete asynchronous call construction is deliberately downstream and is discharged by RUST-CALL-CONSTRUCTION. | `proved` |
| `SCALAR-MOD-REDUCTION` | A uniform 64-byte digest interpreted as one little-endian integer and reduced modulo BLS12-377 Fr is within q/(2·2^512) total variation distance of uniform Fr. | `Ipp.ShippingScalarReduction.uniformDigest_reduceFr_tvDist_le` | none | ARKWORKS-FR-DIGEST-REDUCTION | Kernel-audited exact residue-fiber counts prove the point mass and total-variation bound. The modeled conversion is total: there is no canonical-digest rejection or conditioning step. Correspondence of Arkworks Fr::from_random_bytes to that exact 512-bit little-endian reduction is the separately named external postcondition. | `proved` |
| `SCALAR-CACHE-AWARE-REPLACEMENT` | For every structurally query-bounded program using the shared lazy cache, replacing reduced 64-byte responses by uniform Fr responses changes total variation by at most queryBudget times modReductionBias, including repeated queries. | `Ipp.ShippingScalarReduction.simulate_reducedCaching_uniformCaching_tvDist_le` | SCALAR-MOD-REDUCTION | none | Kernel-audited cache-aware adaptive replacement theorem; cache hits preserve the same value and only fresh misses incur the explicit modular-reduction slack. | `proved` |
| `SHIPPING-HASH-MOD-REDUCTION` | For one fixed formal statement and proof, the ideal-byte-oracle attempt-indexed verifier with its shared cache is acceptance-bounded by the uniform-field FsGame plus modReductionBudget ((μ + 4) · 2^64). | `Ipp.ShippingHashGame.shippingIdeal_acceptance_le_fsProbComp_add_modReduction` | V1-ABSTRACT-ACCEPTS, SCALAR-CACHE-AWARE-REPLACEMENT | none | Kernel-audited fixed-input ideal-byte-oracle coupling. Injective bounded framing, exact u64 rejection fuel, shared-cache semantics, and transcript erasure reduce the formal shipping verifier to fsProbComp while retaining modReductionBudget ((μ + 4) · 2^64) as a separate statistical term. This root neither constructs the deployed Rust trace nor quantifies over an adaptive proof-producing adversary. | `proved` |
| `SHIPPING-FIXED-INPUT-COMPUTATIONAL-BOUND` | For one fixed formal statement and decoded proof, an explicit real-to-ideal coupling bounds real acceptance by FsGame acceptance plus modular-reduction and hash-replacement losses, while proof invalidity bounds the S1 fork expression by the exact experiment-relative extraction losses. | `Ipp.ShippingHashGame.shipping_invalid_goal_quantitative_bound_computational` | S1-QUANTITATIVE-SOUNDNESS, SHIPPING-HASH-MOD-REDUCTION | ADVERSARY-QUERY-BOUND, KZG-FALSE-OPENING-SECURITY, GIPA-FORK-KNOWLEDGE-SOUNDNESS, SHA256-SECURITY, BLAKE2B-ROM-SECURITY | Kernel-audited fixed-formal-input theorem using S1ExtractionSecurity and the exact shipping Coupling structure. It does not construct production CallData, identify the formal statement with authenticated shipping rows/VK/SRS, prove an adaptive SHA-256 reduction, or lift the bound to a heterogeneous bundle. | `proved` |
| `SHIPPING-TO-V1` | For fixed formal ShippingCallData, a raw accepted application/adapter equation implies SnarkPack v1 acceptance when exact construction, statement projection, decode, prepared-VK, challenge-trace, TIPP-kernel, KZG, and pairing postconditions are supplied; the application family/count projection is derived from the production scalar-projection core. | `Ipp.Extracted.ShippingVerifierComposition.ShippingCallData.acceptedAt_refines_v1` | V1-ABSTRACT-ACCEPTS, APP-SHIPPING-BINDING, CANONICAL-STATEMENT-BINDING, OPTIMIZED-GT-FOLD-REFINEMENT | AENEAS-TRANSLATOR, BLS12-377-PAIRING-BILINEARITY, ARKWORKS-TIPP-PRIMITIVE-SEMANTICS, ARKWORKS-PREPARED-VK-AIC-PROJECTION, ARKWORKS-KZG-VERIFIER-EQUATIONS, ARKWORKS-AGGREGATE-PROOF-DECODE, ARKWORKS-CHALLENGE-SERIALIZATION | Kernel-audited conditional composition keeps raw adapter acceptance independent of semantic conclusions and derives v1 acceptance from ShippingVerifierRefinementContracts plus ArkworksChallengeOracleAnswers. Sampler admissibility is an explicit argument of this abstract non-hash root rather than a field hidden in ShippingVerifierExternalContracts; the deployed caller derives it from the exact executed sampler trace. The generated app_verify_shipping_projection_core equation derives the application family/count projection. Its inputs retain exact external postconditions for BLS12-377 bilinearity, inverse/singleton pairing/singleton MSM, prepared-VK Aic projection, both KZG equations, decoded proof fields, statement rows/SRS, and canonical component serializers. Concrete call construction is deliberately supplied by the downstream RUST-CALL-CONSTRUCTION claim. | `proved` |
| `SHIPPING-TO-GOAL` | For one concrete shipping call, production acceptance is bound to exactly the authenticated family, VK, SRS, decoded proof, and first realCount ordered caller rows, and invalidity of that goal yields the computational S1 bound plus explicit modular-reduction, SHA-256, and Blake2b losses. | `Ipp.ShippingConcreteToGoal.output_derived_shipping_call_to_goal_quantitative` | SHIPPING-TO-V1, CANONICAL-STATEMENT-BINDING, STATEMENT-PROJECTION-CONSTRUCTION, RUST-CALL-CONSTRUCTION, DEPLOYED-HASH-TRACE-CONSTRUCTION, S1-QUANTITATIVE-SOUNDNESS, SHIPPING-FIXED-INPUT-COMPUTATIONAL-BOUND, SHIPPING-HASH-MOD-REDUCTION, REAL-PREFIX-GOAL, KZG-LEAF-REDUCTION, GIPA-FORK-KNOWLEDGE-REDUCTION | ADVERSARY-QUERY-BOUND, KZG-FALSE-OPENING-SECURITY, GIPA-FORK-KNOWLEDGE-SOUNDNESS, SHA256-SECURITY, BLAKE2B-ROM-SECURITY | Kernel-audited conditional concrete composition. output_derived_shipping_call_to_goal_quantitative consumes one ConcreteOutputDerivedCall, derives the exact application/statement/proof/projection/prepared-VK/verifier-SRS refinement bundle from that retained production construction, and constructs DeployedChallengePrimitiveContract from the accepted extracted run plus the exact randomizer and TIPP Blake2b postconditions. It then invokes shipping_call_to_goal_quantitative to prove the ordered Ipp.Goal refinement and the fixed-call computational acceptance and S1 fork bounds. The independently authenticated intended input, invalid-goal premise, SHA-256 collision security, Blake2b ROM security, wrapped-oracle query bound, modular-reduction bad set, KZG/GIPA extraction security, strict decoder, Arkworks serialization/effect semantics, and deployment SRS boundary remain explicit parameters or named assumptions. No numerical security level, ceremony soundness, adaptive adversary coupling, or bundle-level result is claimed here. | `proved` |
| `STATEMENT-PROJECTION-CONSTRUCTION` | The concrete accepted shipping input constructs one exact formal statement whose family route, complete prepared VK, SRS keys, row arity, and padded rows are the authenticated production values, including the caller-order real prefix. | `Ipp.ShippingPreflightProjection.accepted_shipping_preflight_constructs_statement_projection` | V1-ORDERED-PROJECTION | AENEAS-TRANSLATOR, HAX-FSTAR-TRANSLATOR, FSTAR-CHECKER, ARKWORKS-VK-SERIALIZATION, ARKWORKS-FIELD-SERIALIZATION, ARKWORKS-PREPARED-VK-ROUTE-DECODE, ARKWORKS-VERIFIER-SRS-ID-LOAD, ARKWORKS-PREPARED-VK-AIC-PROJECTION, RUST-SHIPPING-STATEMENT-PREFLIGHT-DELEGATION | Kernel-audited concrete capstone. AcceptedShippingPreflightExecution is indexed by the generated app_verify_shipping_preflight_core success equation and fixes the backend call, rows, authenticated input, counts, and serialized matrix. Its constructor-indexed ConcreteOutputDerivedCallRemainder exposes only the named decoder, row, plan, verifier-SRS-load, and concrete-effect boundaries. The root constructs that call and derives StatementProjectionContract, exact family/VK/SRS routing, prepared Aic projection for every padded row, ValidCounts, caller-order RealPrefixExact, and RepeatFinalPadding. The theorem assumes no statement contract or selected VK/SRS witness; those are constructed from ExactSemanticBoundary. The listed translator, serialization, Arkworks, SRS-loader, and outer-delegation assumptions remain explicit and supply no cryptographic soundness conclusion. | `proved` |
| `CANONICAL-STATEMENT-BINDING` | The concrete shipping construction instantiates StatementBindingContract and StatementProjectionContract so successful authenticated wrapper/digest inputs determine one exact ordered family, VK, SRS, statement, decoded proof, caller-order real prefix, and repeat-final padding interpretation. | `Ipp.ShippingPreflightProjection.accepted_shipping_preflights_bind_unique_shipping_input` | V1-ORDERED-PROJECTION, STATEMENT-PROJECTION-CONSTRUCTION | SHA256-SECURITY, HAX-FSTAR-TRANSLATOR, FSTAR-CHECKER, ARKWORKS-VK-SERIALIZATION, ARKWORKS-FIELD-SERIALIZATION, ARKWORKS-AGGREGATE-PROOF-DECODE, ARKWORKS-PREPARED-VK-ROUTE-DECODE, ARKWORKS-VERIFIER-SRS-ID-LOAD | Kernel-audited concrete uniqueness capstone. Two generated accepted-preflight executions are converted through their constructor-indexed remainders to concrete calls under one ExactSemanticBoundary. Equal retained statement digests and wrapped proof bytes then imply one serialized VK, public claim, formal statement, decoded proof, caller-order prefix, and repeat-final padding interpretation unless the theorem returns an explicit collision in the VK, canonical-statement, or challenge-context deployed SHA-256 call. Wrapper exposure, strict proof decoding, canonical framing, SHA execution, VK routing, Aic projection, and SRS loading remain exactly the listed assumptions; SHA256-SECURITY discharges only the explicit collision event, not construction facts. | `proved` |
| `V1-COMPLETENESS` | Given an exact ordered valid witness, a fixed admissible Fiat-Shamir transcript, and KZG openings satisfying their exact verifier postconditions, the Lean honest construction yields a proof that opens to that witness and is accepted by SnarkPack v1. | `Ipp.SnarkPackV1.HonestProver.honest_complete` | V1-GOAL-PPE | none | Kernel-audited honest construction. KzgOpenings and AdmissibleTranscript are explicit theorem inputs with exact postconditions; this root does not claim that the shipping prover constructs them. | `proved` |
| `SHIPPING-PROVER-REFINEMENT` | Given exact successful equations for the four production prover semantic roots, their retained granular GIPA schedule, exact challenge and opening projections, a well-formed proving SRS boundary, and a valid ordered witness, the exact Rust-returned proof is the canonical wire image of one honest SnarkPack v1 proof, opens to the witness, and is accepted. | `Ipp.Extracted.ShippingProverProductionExecution.accepted_shipping_prover_refines_honest_complete` | V1-COMPLETENESS | AENEAS-TRANSLATOR, ARKWORKS-PROVER-CURVE-OPERATIONS, ARKWORKS-CHALLENGE-SERIALIZATION, ARKWORKS-KZG-VERIFIER-EQUATIONS, WELL-FORMED-PROVING-SRS | Kernel-audited production composition. AcceptedShippingProverExecution carries the exact generated equations for prover_gipa_semantic_execution_core, shipping_prover_execution_from_parts, shipping_aggregate_prover_semantic_execution_from_parts, and shipping_aggregate_prover_semantic_returned_proof. The first equation exposes the underlying successful retained-effect execution without a second whole-loop equality. ContinuingRoundEvidence and ExternalRoundSchedule construct every generated loop step from deterministic splits and exact X0, commitment, chronological challenge, and inversion effects; MatchesRetainedRounds indexes that schedule by the exact retained chronological evidence vector. ProductionRemainder binds the concrete input, semantic output, randomizer, bridge, KZG challenge, openings, admissibility, full-power proving-SRS postcondition, and V/W verifier equations. The observation deliberately omits a caller-supplied emission equation: canonicalObservation installs AggregateProver.wireProofOf, AggregateProver.assemble_exact derives emission, and returnedProof_eq_wireProof derives the exact returned Rust proof from the fourth generated root. The capstone constructs ShippingProverContract and invokes the independent honest-prover completeness theorem, with no accepted-proof premise, free wire equality, numerical security claim, or deployed-ceremony claim. Deployment identity remains the separate DEPLOYED-SRS-SOUNDNESS gap. | `proved` |
| `RUST-CALL-CONSTRUCTION` | Every supported accepted production Rust verifier call constructs the exact formal ShippingCallData and ShippingApplicationConstruction, from which the application projection contract is derived, plus the intended statement digest, decoder projection, prepared-VK projection, and adapter input used by the fixed-call Lean theorems. | `Ipp.ShippingPreflightProjection.accepted_shipping_preflight_constructs_concrete_rust_call` | APP-STATE-MACHINE, APP-SHIPPING-BINDING, CANONICAL-STATEMENT-BINDING, DECODER-LAYOUT-CONFORMANCE | AENEAS-TRANSLATOR, HAX-FSTAR-TRANSLATOR, FSTAR-CHECKER, ARKWORKS-VK-SERIALIZATION, ARKWORKS-FIELD-SERIALIZATION, ARKWORKS-AGGREGATE-PROOF-DECODE, ARKWORKS-PREPARED-VK-ROUTE-DECODE, ARKWORKS-VERIFIER-SRS-ID-LOAD, ARKWORKS-PREPARED-VK-AIC-PROJECTION, ARKWORKS-SHIPPING-EFFECT-INSTALLATION, RUST-SHIPPING-STATEMENT-PREFLIGHT-DELEGATION, RUST-IMMUTABLE-OBSERVED-RESULT-TRANSPORT | Kernel-audited accepted-preflight capstone. The generated app_verify_shipping_preflight_core equation fixes one BuiltConstructorExecution; the constructor-indexed remainder may supply only the named decoder, row, plan, verifier-SRS-load, and concrete-effect postconditions. The root constructs ConcreteOutputDerivedCall and proves exact ShippingCallData/call/output equalities, verifier SRS selection, ShippingApplicationConstruction, the derived application projection, canonical statement/VK/context SHA executions, wrapper and strict proof decoding, StatementProjectionContract, formal statement/proof representation, prepared VK/Aic alignment, and ordered prefix/padding facts. RUST-SHIPPING-STATEMENT-PREFLIGHT-DELEGATION and immutable transport remain narrow production-boundary assumptions; Arkworks and translator assumptions retain their stated byte/effect meanings and do not assert verifier acceptance or soundness. | `proved` |
| `DEPLOYED-HASH-TRACE-CONSTRUCTION` | Every accepted production challenge execution constructs the exact randomizer, X0, chronological round, bridge, and KZG sampler trace consumed by DeployedChallengePrimitiveContract, with identical framing, Blake2b bytes, Fr reduction, nonce bounds, cache behavior, and fail-closed exhaustion. | `Ipp.ShippingDeployedHashTraceConstruction.deployed_hash_trace_construction` | BOUNDED-CHALLENGE-SAMPLER, SHIPPING-HASH-MOD-REDUCTION | AENEAS-TRANSLATOR, ARKWORKS-AGGREGATE-PROOF-DECODE, ARKWORKS-CHALLENGE-SERIALIZATION, ARKWORKS-FR-DIGEST-REDUCTION, DEPLOYED-BLAKE2B-EXECUTION, ARKWORKS-SHIPPING-EFFECT-INSTALLATION, RUST-IMMUTABLE-OBSERVED-RESULT-TRANSPORT | Kernel-audited conditional closure. runTipp_success_has_effect_trace derives the state-threaded X0, chronological round, bridge, and KZG calls directly from the accepted extracted runTipp execution, and acceptedRunCallProjection installs that result in AcceptedRunCallProjection. acceptedExecution_challengePrimitiveContract_of_decoded combines those calls with the accepted adapter run's exact randomizer call and the deterministic shipping transcript execution. deployed_hash_trace_construction therefore constructs DeployedChallengePrimitiveContract for every supported accepted shipping execution without any caller-supplied trace, transcript equation, admissibility fact, or other operational premise. The only remaining boundaries are the named strict aggregate-proof decoder correspondence, challenge serialization, Arkworks digest-to-Fr reduction, deployed Blake2b execution, concrete shipping effect installation, immutable observed-result transport, and Aeneas translator correctness. Those postconditions supply no cache, scheduling, distribution, acceptance, or soundness conclusion. | `proved` |
| `ADAPTIVE-SHARED-ORACLE-SKELETON` | An adversary may query one global oracle before selecting a heterogeneous statement and proof; selection and verification remain one shared program with an additive adversary-plus-verifier query bound, and every selected-verifier query has the exact registered family and retained context framing. | `Ipp.ShippingMultiStatement.multiStatementFsGame_selectedFraming_queryBounded` | none | none | The audited theorem constructs one dependent per-mu selection package and one global oracle program spanning pre-selection queries and selected verification. SelectionAt structurally ties every selected-verifier query to the registered family transcript domain and retained logical challenge context, and the theorem proves the additive adversary-plus-verifier query bound without assumptions. This closes only the abstract shared-oracle skeleton. Concrete production call identity, unrestricted raw-byte prequeries, byte/field cache coherence, deployed-hash replacement, and the heterogeneous shipping-outcome coupling remain explicit downstream obligations. | `proved` |
| `ADAPTIVE-SHA256-COUPLING` | In one adaptive production experiment, any accepted substitution of the authenticated VK, statement rows, family context, or challenge context yields two distinct concrete SHA-256 preimages with the same digest, and the probability of that event is bounded by epsilonSha256 under the declared query budget. | `Ipp.ShippingProductionOriginSha.adaptive_production_shipping_sha256_collision_reduction` | CANONICAL-STATEMENT-BINDING, STATEMENT-PROJECTION-CONSTRUCTION | SHA256-SECURITY, ADAPTIVE-SHA-QUERY-BOUND | ShippingAdaptiveOriginSha places a heterogeneous request, production-call materializer, and concrete shipping verifier in one Type-1 GlobalByteSourceSpec program while retaining the complete production CallData. Acceptance is the real verifier result, not an adversary label. MaterializedAliasCollisionSecurity carries one structural SHA query-bound witness over preselection, materialization, and verification plus the explicit epsilonSha256 collision-game bound. ShippingProductionOrigin constructs OriginSelectedCall and MaterializedAliasCall from one ConcreteOutputDerivedCall and its derived refinement contracts. ShippingProductionOriginSha instantiates the materializer with six explicit tagged SHA-256 queries for the intended and retained VK, canonical-statement, and challenge-context preimages. bindingExecutions ties all six answers to the exact deployed SHA function retained by the production contract; materializeRetainedAlias_matches_deployed proves that charging those queries returns the identical retained call; and adaptive_production_shipping_sha256_collision_reduction bounds every accepted binding substitution by the declared SHA-256 collision advantage. No numerical security level is claimed without instantiating the query budget and collision bound. | `proved` |
| `ADAPTIVE-ADVERSARY-COUPLING` | One adaptive adversary experiment chooses heterogeneous statements and aggregate proofs after querying a coherent global oracle, binds verification to the selected call, charges each SHA alias and Blake2b replacement once, and supplies the raw multi-statement fork and S1 query-bound interfaces directly. | `Ipp.ShippingBundleProductionAdaptiveComposition.productionInvalidAcceptedAt_adjustedForkTransform_le_acceptedReplaySecurity` | ADAPTIVE-SHARED-ORACLE-SKELETON, ADAPTIVE-SHA256-COUPLING, SCALAR-CACHE-AWARE-REPLACEMENT, KZG-LEAF-REDUCTION, GIPA-FORK-KNOWLEDGE-REDUCTION, SHIPPING-TO-GOAL, RUST-CALL-CONSTRUCTION, DEPLOYED-HASH-TRACE-CONSTRUCTION | AENEAS-TRANSLATOR, RUST-TOKIO-JOIN-SEMANTICS, RUST-IMMUTABLE-OBSERVED-RESULT-TRANSPORT, ADVERSARY-QUERY-BOUND, ADAPTIVE-SHA-QUERY-BOUND, SHA256-SECURITY, BLAKE2B-ROM-SECURITY, KZG-FALSE-OPENING-SECURITY, GIPA-FORK-KNOWLEDGE-SOUNDNESS | Kernel-audited production adaptive composition. ProductionInvalidAcceptedAt fixes the real event to one retained production verifier call that accepts an independently intended invalid ordered prefix under the authenticated statement digest. good_production_invalid_projects_to_canonical proves that outside the exact retained SHA substitution event this projects to the canonical selected-size InvalidV1Selection. ProductionAdaptiveBundleBridge contains the extracted output-derived planner/cache/reindexing equations plus one narrow translator/runtime equality between the complete adversary-materializer-verifier source, after a pure output projection, and that raw bundle program; it contains no event or probability conclusion. ProductionBlake2bRomSecurity is explicitly an event-specific ROM replacement premise over the identical raw program, and productionRomSecurity derives the precise deployed composition interface without hiding its epsilon. The capstone then composes the concrete production event with the accepted-replay reductions. Its inequality keeps the whole-program SHA query budget, Blake2b-ROM loss, modular-reduction budget, adaptive randomizer loss, replay-query budget, separate KZG-V/KZG-W and GIPA-root/GIPA-product bounds, and iterated multi-statement fork loss visible. The classifier, adversary/extractor polynomial-time, setup-binding, and numerical security instantiations remain explicit theorem inputs or named assumptions. Deployed ceremony soundness remains separately open through DEPLOYED-SRS-SOUNDNESS; no numerical level is claimed. | `proved` |
| `EXTRACTED-BUNDLE-COMPOSITION` | Given an exact materialization witness for each uniquely accepted planned full call identifier, the extracted bundle state machine yields one accepted canonical SnarkPack-v1 call for every planned identifier, with exact plan order and result cardinality. | `Ipp.Extracted.ShippingBundleComposition.accepted_shipping_bundle_refines_all_v1` | APP-STATE-MACHINE, SHIPPING-TO-V1 | AENEAS-TRANSLATOR | Kernel-audited conditional composition. ShippingCallExecutionBoundary.toAccepted, ShippingBundleCallWitness.refinesV1, accepted_bundle_lifts_per_call_claim, and accepted_shipping_bundle_refines_all_v1 contain no semantic acceptance premise. The sole residual materialize field asks the unextracted async production caller to construct the exact ShippingCallData, raw execution, refinement contracts, and deployed challenge answers for each accepted full call identifier. | `proved` |
| `BUNDLE-LEVEL-COMPOSITION` | Acceptance of one production application bundle implies that every exactly planned family subcall is bound to its intended ordered statements and satisfies the per-call goal theorem, with no omitted, duplicated, substituted, or silently unverified call. | `Ipp.Extracted.ShippingBundleGoalComposition.accepted_output_derived_bundle_refines_all_goals` | EXTRACTED-BUNDLE-COMPOSITION, RUST-CALL-CONSTRUCTION, SHIPPING-TO-GOAL, DEPLOYED-HASH-TRACE-CONSTRUCTION | AENEAS-TRANSLATOR, RUST-TOKIO-JOIN-SEMANTICS, RUST-IMMUTABLE-OBSERVED-RESULT-TRANSPORT | Kernel-audited deterministic production composition. accepted_output_derived_bundle_refines_all_goals consumes one OutputDerivedShippingBundle, the exact extracted planner and reducer success equations, and OutputDerivedBundleVerificationBridge. The bridge contains only per-call serialization, kernel/prepared-VK/decoder/KZG postconditions, deployed Blake2b effect postconditions, and transport of an exact raw accepted execution into the deployed verifier experiment after the reducer-derived Rust result bit is true. It contains no formal acceptance, invalidity, goal-row, or probability conclusion. OutputDerivedVerifierRemainder.toExternalContracts derives supported input, statement projection, and exact statement/proof representation from the constructor output and semantic alignment. The deployed challenge contract is constructed from retained effect executions. acceptedCallOutput_refines_shipping_v1 then derives formal v1 acceptance, valid counts, real-prefix preservation, and repeat-final padding; realGoal_rows derives every independent ordered public row. The extracted plan and reducer prove exact planner order, no duplicate order tags, exact result cardinality, and one accepted result for every full identifier, so no call can be omitted, duplicated, substituted, or silently unverified. The RUST-TOKIO-JOIN-SEMANTICS and immutable-transport assumptions are used only to justify the raw emitted-execution bridge at the outer async boundary. Cache equivalence, whole-program distributions, and adaptive security are intentionally excluded here and remain obligations of ADAPTIVE-ADVERSARY-COUPLING. | `proved` |
| `FULL-ADAPTIVE-END-TO-END-FV` | Given an already successful retained production prover execution, its exact external postconditions, and a valid ordered witness, the returned wire proof refines the honest SnarkPack construction; every accepted output-derived bundle row refines Ipp.Goal; and the adjusted iterated-fork expression for adaptive invalid acceptance is bounded by the explicit security terms. | `Ipp.ShippingFullAdaptiveEndToEnd.shipping_full_adaptive_end_to_end_fv` | SHIPPING-PROVER-REFINEMENT, ADAPTIVE-ADVERSARY-COUPLING, BUNDLE-LEVEL-COMPOSITION, KZG-LEAF-REDUCTION, GIPA-FORK-KNOWLEDGE-REDUCTION, ADAPTIVE-SHA256-COUPLING | AENEAS-TRANSLATOR, ADVERSARY-QUERY-BOUND, ADAPTIVE-SHA-QUERY-BOUND, KZG-FALSE-OPENING-SECURITY, GIPA-FORK-KNOWLEDGE-SOUNDNESS, SHA256-SECURITY, BLAKE2B-ROM-SECURITY, RUST-TOKIO-JOIN-SEMANTICS, RUST-IMMUTABLE-OBSERVED-RESULT-TRANSPORT | Kernel-audited conditional composition capstone. shipping_full_adaptive_end_to_end_fv constructs three conclusions: exact returned-wire-proof and honest-construction refinement from AcceptedShippingProverExecution; every ordered accepted bundle row's independent Ipp.Goal refinement from the extracted planner/reducer and OutputDerivedBundleVerificationBridge; and the adjusted production adaptive invalid-acceptance fork bound from ProductionAdaptiveBundleBridge and the concrete security interfaces. The first conclusion is accepted-execution partial correctness: it does not prove that every valid input makes the production prover terminate successfully. The final inequality bounds the iterated forkTreeStep expression, not invalid-acceptance probability directly, and keeps SHA-256, event-specific Blake2b-ROM, modular-reduction, adaptive-randomizer, replay-query, KZG-V, KZG-W, GIPA-root, and GIPA-product losses separate. It publishes neither an inverted acceptance bound nor a numerical security level. AENEAS and the narrow Rust async/immutable transport boundaries remain explicit assumptions; exact Arkworks, encoding, and hash boundaries are inherited through audited dependencies. Deployed ceremony soundness remains the open DEPLOYED-SRS-SOUNDNESS dependency of KZG-LEAF-REDUCTION and must be verified before claiming instantiated production security. | `proved` |
| `DECODER-LAYOUT-CONFORMANCE` | The exact aggregate serialization layout round-trips, has the declared component inventory, and every packaged component satisfies its strict acceptance predicate. | `Ipp.AggregateSerialization.aggregate_layout_component_acceptance_conformance` | none | ARKWORKS-AGGREGATE-PROOF-DECODE | Kernel-audited exact-layout and component-acceptance theorem. It does not model a parser cursor or decoder call order; Rust-to-Lean decoder correspondence remains the explicit ARKWORKS-AGGREGATE-PROOF-DECODE boundary. | `proved` |
| `BOUNDED-CHALLENGE-SAMPLER` | Every production Fiat-Shamir stage-predicate retry loop either uses the shared bounded sampler or the formally extracted Arkworks adapter specialization; both query within the u64 nonce domain, accept success at u64::MAX, and fail closed rather than wrap after rejection there. | `bounded_challenge_sampler_boundary_suite` | V1-ABSTRACT-ACCEPTS | none | Digest-to-field conversion itself is total for the deployed decaf377::Fq implementation. Seven exact ordinary Rust tests exercise the shared helper's immediate success, ordered retries, success and rejection at u64::MAX, query-before-increment ordering, attempt-error precedence, and nonce boundaries. The static gate separately inventories every generic sampler call and the sole first-order loop in verify_aggregate_adapter_execution_core_from_nonce, pins that execution function's query, admissibility, installation, checked-successor, and fail-closed branches, forbids direct nonce arithmetic, requires its four ordinary retry/MAX/error-order tests and both public error mappings, and requires Ipp.Extracted.AggregateAdapter.accepted_path as exactly one ProofAudit root. The core-only wrapper merely projects the retained execution result. That Lean root binds accepted adapter execution to the exact retained randomizer call; MAX rejection and exhaustion remain Rust-tested against the shared checked successor. | `tested` |
| `V1-BYTE-LOCK` | For the committed deterministic vectors covering every registered family at counts 1, 2, 4, and 8, v1 proof bytes and exact prover/verifier challenge-trace bytes match version-tagged fixtures. | `v1_bytes_and_transcript_match_committed_baselines` | CANONICAL-STATEMENT-BINDING | none | The ordinary production-crate umbrella test checks aggregate_bytes_baseline.txt and shieldd_byte_trace_baseline.txt. Trace generation first proves the traced core emits the production-wrapped inner proof and that prover and verifier record identical ordered stage labels, nonces, preimages, and digests. Independent two-way reference interop remains a separate acceptance oracle. | `tested` |
| `COST-PROVER-MILLER` | For n equal to 2^mu, the declared symbolic prover recurrence normalizes to 12n minus 8 Miller terms. | `Ipp.Cost.prover_miller_terms` | none | none | Kernel-audited arithmetic normalization of the declared recurrence; this root does not derive the recurrence from SnarkPack v1 semantics or production code. | `proved` |
| `COST-PROVER-FINAL-EXP` | The declared symbolic prover recurrence normalizes to 4 plus 8mu final exponentiations. | `Ipp.Cost.prover_final_exponentiations` | none | none | Kernel-audited arithmetic normalization of the declared recurrence; protocol and implementation counting conformance remains separate. | `proved` |
| `COST-PROVER-FOLDS` | The declared symbolic prover recurrence normalizes to 5 times n minus 1 primary fold scalar multiplications. | `Ipp.Cost.prover_primary_fold_scalar_muls` | none | none | Kernel-audited arithmetic normalization of the declared recurrence; protocol and implementation counting conformance remains separate. | `proved` |
| `COST-PROVER-KZG` | The declared symbolic prover recurrence normalizes to 4n minus 2 KZG MSM terms. | `Ipp.Cost.prover_kzg_msm_terms` | none | none | Kernel-audited arithmetic normalization of the declared recurrence; protocol and implementation counting conformance remains separate. | `proved` |
| `COST-VERIFIER-GT-VALIDATION` | The declared accepted-wire verifier recurrence normalizes to 4 plus 8mu GT decode and validation sites. | `Ipp.Cost.verifier_gt_subgroup_validations` | none | none | Kernel-audited arithmetic normalization of a declared wire-site recurrence. Concrete Arkworks decode conformance remains separately open in operation-reduction-register.json and is not part of the FV publication gate. | `proved` |
| `COST-VERIFIER-GT-EXP` | The declared GIPA-only verifier subtotal normalizes to 8mu GT exponentiations. | `Ipp.Cost.verifier_gt_exponentiations` | none | none | Kernel-audited arithmetic normalization of the declared GIPA subtotal. The accepted verifier core also computes the fixed alpha_beta exponentiation; full implementation counting conformance remains separately open in operation-reduction-register.json. | `proved` |
| `COST-VERIFIER-FINAL-EXP` | The declared symbolic verifier baseline is the constant seven independent final exponentiations. | `Ipp.Cost.verifier_final_exponentiations` | none | none | Kernel-audited normalization of the declared constant; protocol and implementation counting conformance remains separate. | `proved` |

## Statement-Binding Contract Evidence

| Field | Kind | Theorem roots | Toolchain role | Last checker result |
| --- | --- | --- | --- | --- |
| `familyRegistered` | `fstar` | `FamilyRoutingProofs.lemma_family_proto_fields_transfer_inverse`, `FamilyRoutingProofs.lemma_family_proto_fields_note_reshape_inverse`, `FamilyRoutingProofs.lemma_family_proto_fields_shielded_ics20_withdrawal_inverse`, `FamilyRoutingProofs.lemma_family_route_transfer_total`, `FamilyRoutingProofs.lemma_family_route_note_reshape_total`, `FamilyRoutingProofs.lemma_family_route_shielded_ics20_withdrawal_total`, `FamilyRoutingProofs.lemma_family_route_rejects_unknown_family`, `FamilyRoutingProofs.lemma_family_route_note_reshape_rejects_missing`, `FamilyRoutingProofs.lemma_family_route_shielded_rejects_missing`, `FamilyRoutingProofs.lemma_family_route_transfer_rejects_cross_subids`, `FamilyRoutingProofs.lemma_family_route_note_reshape_rejects_cross_subids`, `FamilyRoutingProofs.lemma_family_route_shielded_rejects_cross_subids`, `FamilyRoutingProofs.lemma_family_route_note_reshape_rejects_unknown`, `FamilyRoutingProofs.lemma_family_route_shielded_rejects_unknown` | `hax-fstar` | `pass` |
| `canonicalStatementExact` | `fstar` | `DigestBindingProofs.lemma_statement_digest_preimage_content`, `StatementEncodingProofs.lemma_statement_encoding_input_core_projection`, `StatementEncodingProofs.lemma_encode_statement_content` | `hax-fstar` | `pass` |
| `canonicalStatementInjective` | `fstar` | `StatementEncodingProofs.lemma_encode_statement_injective` | `hax-fstar` | `pass` |
| `vkDigestPreimageInjective` | `lean` | `Ipp.ShippingV1.vkDigestPreimage_injective` | `hax-aeneas` | `pass` |
| `vkDigestExact` | `external` | `Rust.proof_aggregation.statement.AggregateStatement.new` | `external` | `assumed` |
| `statementDigestExact` | `external` | `Rust.proof_aggregation.statement.AggregateStatement.new` | `external` | `assumed` |
| `challengeContextExact` | `external` | `Rust.proof_aggregation.statement.AggregateStatement.new` | `external` | `assumed` |
| `wrapperExact` | `external` | `Rust.proof_aggregation.preflight.preflight_shipping_aggregate_verify` | `external` | `assumed` |
| `proofDecodeExact` | `external` | `Rust.proof_aggregation.backend.deserialize_aggregate_proof` | `external` | `assumed` |
| `validCounts` | `lean` | `Ipp.Extracted.ShippingStatementConstruction.ExecutionCoupledRowConstruction.toExactRowConstruction`, `Ipp.Extracted.ShippingRowConstruction.validCounts`, `Ipp.Extracted.ShippingStatementConstruction.ExactSemanticBoundary.bindingContract` | `hax-aeneas` | `pass` |
| `realPrefixExact` | `lean` | `Ipp.Extracted.ShippingStatementConstruction.ExecutionCoupledRowConstruction.toExactRowConstruction`, `Ipp.Extracted.ShippingRowConstruction.realPrefixExact`, `Ipp.Extracted.ShippingStatementConstruction.ExactSemanticBoundary.bindingContract` | `hax-aeneas` | `pass` |
| `repeatFinalPadding` | `lean` | `Ipp.Extracted.ShippingStatementConstruction.ExecutionCoupledRowConstruction.toExactRowConstruction`, `Ipp.Extracted.ShippingRowConstruction.repeatFinalPadding`, `Ipp.Extracted.ShippingStatementConstruction.ExactSemanticBoundary.bindingContract` | `hax-aeneas` | `pass` |

## Assumptions

| ID | Owner | Postcondition | Evidence | Closure | Status |
| --- | --- | --- | --- | --- | --- |
| `SHA256-SECURITY` | Cryptography platform | For the declared adaptive shipping experiment and its Q_sha budget, the probability that an efficient adversary produces two distinct concrete VK, canonical-statement, or challenge-context preimages with the same SHA-256 digest is at most epsilonSha256. | Production uses SHA-256 at the VK, canonical-statement, and challenge-context binding sites. The Lean fixed-input ShippingShaCollision disjunction identifies the required collision shapes, but its badMass encoding is only a deterministic 0-or-1 case split and is not itself this computational assumption. | Define the adaptive SHA-256 experiment over adversary-selected production bytes, reduce every accepted binding alias to a pair of distinct equal-digest preimages, and instantiate epsilonSha256 and the query budget. | `assumed` |
| `ADVERSARY-QUERY-BOUND` | SnarkPack security analysis | The adversary makes at most Q_fs according to qb at every wrapped field-oracle domain used by the fork theorem. | Ipp.S1.s1_computational_soundness and both computational contrapositive roots require IsTotalQueryBound explicitly. | Instantiate qb from a declared adversary budget and maximum aggregate size before publishing a numerical security level. | `assumed` |
| `ADAPTIVE-SHA-QUERY-BOUND` | SnarkPack security analysis | At the fixed-call layer, IsTotalQueryBound holds with Q_sha for the exact Ipp.ShippingAdaptiveSha.AdaptiveShaSourceSpec adversary; final heterogeneous composition extends Q_sha to every SHA-256 query made by adversarial preselection and production VK, canonical-statement, and challenge-context construction without resetting the budget between calls. | Ipp.ShippingAdaptiveSha.CollisionSecurity and Ipp.ShippingAdaptiveCall.CollisionSecurity retain an IsTotalQueryBound witness for the exact SHA-querying OracleComp source. This budget is distinct from the wrapped field-oracle qb in ADVERSARY-QUERY-BOUND. The heterogeneous whole-program extension is deliberately the external postcondition of this assumption, not a conclusion hidden in the fixed-call model. | Derive one Q_sha bound for the complete heterogeneous byte program, including every production binding-site SHA-256 execution, and instantiate epsilonSha256 at that exact budget before publishing a numerical security level. | `assumed` |
| `KZG-FALSE-OPENING-SECURITY` | SnarkPack protocol and setup | For every polynomial-time proof-free adversary in acceptedReplayBls12377KzgVGame and acceptedReplayBls12377KzgWGame, where the statement may be selected after shared-oracle queries and the challenger requires AcceptedReplayBls12377StatementBinding to the sampled BLS12-377 setup, the two target-winning probabilities are bounded by epsilonV(Q_setup,Q_adv) and epsilonW(Q_setup,Q_adv), including every replay, classifier-selection, batch-to-single, and fork-selection loss. | ShippingAdaptiveAcceptedReplayGames defines the setup-first proof-free target games, keeps binding evidence out of adversary outputs, checks exact Bls12377KzgStatementBinding and executable-pairing identity in the challenger win predicate, records explicit classifier-family and adversary polynomial-time predicates, and proves the audited win normal forms, structural selector soundness, and exact query bounds. ForkTreeGateDominance, ShippingAdaptiveAcceptedReplayDominance, and ShippingAdaptiveRoundReachability prove the qualified strong-gate probability transport for every selected global-FS round slot. ShippingAdaptiveAcceptedReplayKzgSelectorTransport proves V/W selector completeness and source-event preservation. ShippingAdaptiveAcceptedReplayKzgProbabilityTransport proves the exact fixed-setup V/W game maps and bounds both cache-preserving source events under one explicit support-indexed sampled-setup binding invariant. ShippingAdaptiveAcceptedReplaySecurityComposition applies those transports to epsilonV(0,Q_replay) and epsilonW(0,Q_replay) using the exact query-free setup bound, one complete accepted-replay query bound, and explicit classifier/adversary polynomial-time certificates, then includes both terms in the visible four-event adaptive crypto sum. S1Bls12377FixedStatementKzg remains the audited fixed-statement structural precursor. Production setup binding, the concrete polynomial-time certificates, and the computational advantage instantiation remain explicit obligations. This assumption is not currently instantiable for shipping v1 because the production registry is empty and no ceremony evidence exists. The deterministic test, fuzzing, and Orbis integration DevSrs is regenerated from a public fixed ChaCha seed, revealing the alpha and beta sampled by TIPA::setup and permitting false openings in both lanes, so it cannot fill that gap. | Replace the reproducible DevSrs with an authenticated BLS12-377 setup whose trapdoors are unknown, bind its ceremony transcript and artifact digest to the shipping SRS registry, prove production statement/setup binding, certify the concrete classifier family and accepted-replay adversaries as polynomial time, derive the explicit cryptographic reduction and every batch-to-single or fork loss, then instantiate epsilonV and epsilonW at the exact setup and adversary query budgets. Changing the SRS necessarily changes the SRS identifier and aggregate proof fixtures; current v1 proof-byte preservation is incompatible with this closure. | `assumed` |
| `GIPA-FORK-KNOWLEDGE-SOUNDNESS` | SnarkPack protocol | For every polynomial-time proof-free adversary in acceptedReplayBls12377GipaGame, whose statement is selected after shared-oracle queries and is checked by the challenger against the sampled BLS12-377 setup, one extractor selected from that already-fixed adversary before setup sampling and game execution may inspect the public sampled setup, selected statement, and pre-randomizer commitment payload; the full-FsAccepts root-opening and product-lane winning probabilities are bounded separately by epsilonRoot(Q_setup,Q_adv) and epsilonProduct(Q_setup,Q_adv). | ShippingAdaptiveAcceptedReplayGames defines the extractor-independent accepted-replay GIPA adversary, the setup-first game, challenger-side exact KZG/pairing binding, and full accepted-fork conjuncts in both wins. Its GIPA security interface chooses an extractor only after receiving the already-fixed adversary and records extractor/adversary polynomial-time predicates plus separate query-indexed advantages. ForkTreeGateDominance, ShippingAdaptiveAcceptedReplayDominance, and ShippingAdaptiveRoundReachability establish the qualified probability transport for the complete selected global-FS replay. ShippingAdaptiveAcceptedReplayExtractorTransport derives full accepted-fork coverage from accepted-replay support and adapts the security-selected extractor to the source root/product relations at one explicit setup. ShippingAdaptiveAcceptedReplayGipaProbabilityTransport then proves the exact fixed-setup game map and bounds both cache-preserving source events under one explicit support-indexed sampled-setup binding invariant. ShippingAdaptiveAcceptedReplaySecurityComposition applies those transports to epsilonRoot(0,Q_replay) and epsilonProduct(0,Q_replay) for the extractor selected from the already-fixed unfiltered adversary, using one complete accepted-replay query bound plus explicit adversary/extractor polynomial-time evidence, and retains both terms separately in the adaptive crypto sum. These roots do not instantiate production setup binding, polynomial time, or a shipping security conclusion. The earlier fixed-statement theorem remains only a structural precursor. | Prove deployed setup binding, certify the accepted-replay adversary and security-selected extractor as polynomial time, instantiate both accepted-fork game bounds over a secure authenticated setup, and retain every replay, query-overhead, and selection loss in the two explicit advantage arguments. | `assumed` |
| `AENEAS-TRANSLATOR` | Formal methods toolchain | Pinned hax and Aeneas translation preserves the semantics of every declared Rust extraction root and its checked failure behavior. | The extraction manifest fixes each graph's source roots, exact start-from commands, dedicated hax-aeneas toolchain role, normalizer revision, and normalized generated-output digest. CI additionally fingerprints the complete current graph recipe and rejects stale generated output; semantic preservation of the translator itself remains this explicit assumption. | Validate the translator or replace the assumption with proof-producing translation certificates. | `assumed` |
| `RUST-TOKIO-JOIN-SEMANTICS` | Rust runtime platform | On every accepted execution of verify_aggregate_bundle_for_artifacts_raw_profiled, each planned spawn_blocking closure returns the exact execute_aggregate_verify_call result for its owned call, awaiting the stored handles yields those semantic results in plan insertion order, and any task panic, call error, identity mismatch, missing result, or extra result reaches the fail-closed error branch before bundle acceptance. Scheduler-dependent profile, timing, and debug observations are excluded from the security projection. | The production function constructs the complete plan before spawning, stores handles in plan order, awaits that vector in the same order, rechecks every returned full call identity, and reduces only after all successful joins. The synchronous planner, call core, identity projection, and reducer are extraction targets; Tokio spawn/join behavior itself is outside Aeneas and must remain an exact runtime boundary rather than an inferred sequential oracle trace. | Verify the relevant Tokio/Rust async lowering or replace the outer scheduler with a proof-producing runtime boundary. Until then, retain AcceptedTokioIndexedPostconditions as the exact accepted-branch postcondition, derive AcceptedConcurrentRunBridge only from that boundary plus extracted synchronous projections, and keep all scheduler-dependent observations outside the theorem. | `assumed` |
| `RUST-IMMUTABLE-OBSERVED-RESULT-TRANSPORT` | Rust language and proof-aggregation implementation | In a normal shipping build without bench-baseline, after shipping_verifier_observed_backend_result_core consumes the exact verifier semantic execution, correlates its result identifier and acceptance bit, and represents the final effect state as the same challenge context plus one chronological trace and its exact randomizer-prefix length, every successful production transport through shipping_verifier_shared_observed_execution_core, ShippingVerifierExecutionCarrier and Arc, ShippingAggregateVerification, its shipping_result and shipping_observation accessors, and AppVerifyPlannerIndexedExecutedRecord preserves that complete immutable shared payload extensionally. In particular it preserves the adapter input, typed accepted randomizer and TIPP/MIPP calls, checks, TIPP output, initial effect state, result identifier and acceptance bit, observation context and bytes, and phase split. The backend shipping_observation clone is element-for-element equal to the stored observation. An allocation panic, carrier/conversion failure, backend identity mismatch, or application projection error yields no accepted application result. This postcondition excludes Tokio task scheduling and join order, oracle-cache behavior, query accounting, probability distributions, and the observation-only bench-baseline feature. | shipping_verifier_observed_backend_result_core is the pending extraction root that correlates the result with the exact semantic execution and moves the final context and two trace buffers into the stored observation plus split before Arc ownership; its formal reconstruction obligation is take split equals the original randomizer trace, drop split equals the original TIPP/MIPP trace, and split is in bounds. shipping_verifier_shared_observed_execution_core only wraps that already-correlated record. The production share helper performs only Arc::new on the shared value; app_verify_shipping_result_from_backend_result checks authenticated and executed call identities; ShippingAggregateVerification retains the checked result and exact carrier; its shipping_observation accessor clones the stored observation with extensional equality; and the application places both that equal observation and the opaque executed payload in the record preserved by the pending AppVerifier projection. The Arc/backend/app segment lies between pure extraction roots and is intentionally narrower than RUST-TOKIO-JOIN-SEMANTICS. | Verify Rust Arc/value transport and the backend/application lowering for this exact immutable shared record, or keep the full observed execution outside shared ownership until the extracted application projection consumes it. Do not use this assumption to justify semantic execution correctness, scheduler behavior, coherent-cache semantics, oracle replacement, or any query bound. | `assumed` |
| `ARKWORKS-SHIPPING-EFFECT-INSTALLATION` | Proof aggregation implementation | In a normal shipping build without bench-baseline, run_arkworks_adapter_semantic constructs exactly ArkworksAggregateRandomizerEffect with the supplied challenge context, retained randomizer sink, and timing effect; ArkworksTippMippEffect with the same context, supplied TIPP/MIPP sink, and an empty accepted trace; ArkworksPairingEffect; and ArkworksPreparedPairingEffect, then returns verify_shipping_adapter_semantic_execution_core on the supplied adapter input and those effects unchanged. run_shipping_verifier_semantic_core passes its exact retained randomizer and TIPP/MIPP sinks to that call and returns shipping_verifier_semantic_execution_with_traces_core of the resulting semantic execution, context, and those final sinks unchanged. Every adapter error propagates before either successful record is returned. | The production wrappers are deliberately thin around two production-used AggregateAdapter extraction roots. The semantic core invokes the extracted installed-adapter core exactly once and derives every retained acceptance field from its output; the trace-envelope core constructs both effect states without Arkworks operations. Focused source and parity tests pin exact effect types, context/sink identity, trace chronology, accepted randomizer fields, and error propagation. This boundary states no verifier-acceptance, transcript-distribution, cache, query-bound, pairing, KZG, or soundness conclusion. | Refactor the concrete Arkworks effect installation and retained-sink wrapper into an extractable first-order effect interpreter, or verify the remaining generic Rust/trait lowering and remove this assumption. | `assumed` |
| `RUST-SHIPPING-STATEMENT-PREFLIGHT-DELEGATION` | Proof aggregation implementation | On every successful normal-build AggregateStatement::new call, the returned serialized VK, VK digest, canonical statement, statement digest, and challenge context are the values retained by one successful statement_hash_effect_core execution using ShippingStatementHashEffect on the same validated constructor input. On every successful preflight_shipping_aggregate_verify call, production retains that exact hash execution, performs one wrapper_decode_effect_core execution over the original bounded wrapper and expected statement digest, joins the two records through statement_binding_execution_core without replacement, and passes the retained values, exact source rows, backend call, application call, protocol/family route, and authenticated SRS identifier unchanged to app_verify_shipping_preflight_core. Any constructor, effect, digest-join, inner-preflight, projection, or identity error returns before an accepted carrier exists. | The production statement and shipping preflight now retain explicit StatementHashExecution, WrapperDecodeExecution, and StatementBindingExecution values. The extracted StatementBinding graph proves the exact seven-effect order, retained intermediate values, one wrapper decode, digest-equality join, and fail-closed branches; extracted application roots prove the preflight carrier and into-parts projections. Source-pinned parity tests cover every successful value and every failing stage. This assumption is limited to installing those proved first-order cores in the two unextracted outer Rust functions and transporting their outputs unchanged. It does not assert SHA-256 correctness or security, canonical encoding semantics, aggregate-proof decode correctness, verifier acceptance, cache semantics, or any probability bound. | Extract AggregateStatement::new and preflight_shipping_aggregate_verify through their existing first-order statement/wrapper effects, or verify the remaining Rust trait and outer-function lowering, then replace the four source-pinned assumed contract rows with proof-producing artifacts. | `assumed` |
| `HAX-FSTAR-TRANSLATOR` | Formal methods toolchain | Pinned hax v0.3.7 preserves the semantics of every Rust boundary consumed by the F* proofs. | The F* lane machine-checks a separate named hax-fstar role and its exact target list. | Validate the extraction or adopt proof-producing translation. | `assumed` |
| `FSTAR-CHECKER` | Formal methods toolchain | Acceptance by the pinned F* 2026.05.24 checker establishes every source-digest-pinned F* theorem root listed in the statement-binding evidence. | scripts/snarkpack-formal.sh checks the exact F*, hax, and Z3 pins and emits a source-bound checker artifact for the selected dependency closure. Its environment and cache identity project only F* modules, F*-kind contract rows, their toolchain role, and exact F* source/global inputs; Lean audits, Aeneas evidence, external postconditions, claim metadata, and deployed-SRS registration cannot invalidate or reuse an unrelated F* result. Changes to the checker or fingerprint implementation are explicit F* control changes: the impact planner schedules a one-time full refresh and the cache key changes. Only a CI-produced exact artifact may promote stale F* rows to pass. | Rerun the pinned hax v0.3.7, F* 2026.05.24, and Z3 4.14.1 lane on the current digests, then separately retain or discharge checker trust. | `assumed` |
| `ARKWORKS-VK-SERIALIZATION` | Proof aggregation implementation | Compressed Arkworks BLS12-377 verification-key serialization produces exactly the byte string used by the VK-digest preimage and reports serialization failure before digest construction. | Production construction uses one compressed VK serializer and the canonical digest preimage; targeted vectors cover the boundary. | Extract the serializer or connect Arkworks serialization to the Lean canonical component model. | `assumed` |
| `ARKWORKS-FIELD-SERIALIZATION` | Proof aggregation implementation | Compressed BLS12-377 scalar serialization of every ordered padded public-input field equals the bytes consumed by canonical statement encoding. | Statement construction centralizes field-row serialization and F* proves the typed statement encoder over those bytes. | Extract field serialization and prove correspondence to Ipp.CanonicalSerializers.serializeFr. | `assumed` |
| `ARKWORKS-AGGREGATE-PROOF-DECODE` | Proof aggregation implementation | Successful strict decoding consumes exactly one complete inner byte string and satisfies ArkworksDecodedProofContract fieldwise: com_a, com_b, com_c, ip_ab, agg_c, and the complete tipp_mipp_proof equal the formal proof projection; malformed, trailing, or off-subgroup components reject. | Lean proves the formal aggregate layout, round-trip injectivity, component inventory, and component-acceptance predicate. It does not prove the shipping Rust decoder equation. The Rust-to-Lean correspondence that constructs ArkworksDecodedProofContract for the shipping decoder remains external. | Extract the complete aggregate-proof decoder and prove both strictAggregateDecode correspondence and the six ArkworksDecodedProofContract equalities. | `assumed` |
| `ARKWORKS-CHALLENGE-SERIALIZATION` | Proof aggregation implementation | The production serializer constructs ShippingArkworksHash.SerializationContract: encodeF, encodeG1, encodeG2, and encodeGT equal the canonical Lean encoders, canonicalGt is injective and value-preserving, and serialization errors preserve shipping failure order. | Lean derives every stage payload, family/domain/context/nonce frame, and preimage-injectivity theorem from this fieldwise contract; Rust vectors pin commitment order and transcript bytes. | Extract each Arkworks component serializer and prove every SerializationContract field plus exact error ordering. | `assumed` |
| `BLAKE2B-ROM-SECURITY` | Cryptography platform | For every polynomial-time whole-program distinguisher program : OracleComp GlobalByteSourceSpec Output with IsQueryBoundP program IsFsQuery Q_fs, and every fail-closed output event E, replacing the deployed family-domain-separated Blake2b implementation by one coherent uniform lazy byte oracle increases Pr[E] by at most epsilonBlake2bRom(Q_fs, program, E). The same oracle persists across adversarial preselection and every selected or bundled verifier call; neither its state nor Q_fs is reset at a call boundary. | The existing adaptive byte/field/global-FS chain already proves the whole-program query accounting: DistinctQueryBudgets.fs bounds globalOriginByteProgram; projectedOriginByteProgram_fsBound, OriginByteReindexing.hybridQueryBound, projectedOriginQueryBound, fiberLiftedHybridOutput_queryBound, and fiberLiftedGlobalFsProgram_queryBound preserve the same Q_fs through projection, annotation, digest-fiber coupling, and the global FS source. BundleByteOriginReindexing.rawProgram_fsQueryBound and fiberLiftedHybridOutput_queryBound provide the bundle-generic analog. These theorems retain arbitrary preselection queries and do not establish the computational replacement of deterministic Blake2b by a uniform oracle. | Give the complete adaptive deployed-versus-lazy-oracle security reduction for the stated GlobalByteSourceSpec experiment, certify the program/distinguisher as polynomial time, and instantiate epsilonBlake2bRom at the theorem-derived Q_fs before publishing a numerical security level. | `assumed` |
| `BLS12-377-PAIRING-BILINEARITY` | Pairing mathematics | The concrete executable BLS12-377 pairing on the two prime-order input subgroups satisfies both additive bilinearity equations in Ipp.Bls12377.PublishedPairingBilinear. | The verifier takes a PublishedPairingBilinear proof explicitly; extracted Miller/final-exponentiation execution does not prove the cited pairing law. | Formalize the optimal-ate pairing mathematics or retain this exact published-curve assumption with reviewed evidence. | `assumed` |
| `ARKWORKS-TIPP-PRIMITIVE-SEMANTICS` | Curve and pairing implementation | The production primitive constructs ArkworksTippPrimitiveExecutionContract: nonzero scalar inversion returns the mathematical inverse; fold_gt_commitments returns Ipp.OptimizedGtFoldRefinement.optimizedLanes for the exact four roots, reversed wire rounds, and chronological inverse/raw challenge vectors; singleton pairing_inner_product equals executablePairingLinear; and singleton msm_inner_product equals scalar multiplication. | ArkworksTippPrimitiveExecutionContract.toKernel proves the full ArkworksTippKernelContract. In particular, optimizedLanes_eq_terminalFold kernel-proves that the exact shared four-lane flat MSM operands equal Ipp.terminalFold, so terminal-fold algebra is no longer assumed. The remaining boundary is execution correspondence of the concrete signed-window Arkworks primitive plus inverse/singleton calls to the exact formal operations. | Extract the shared signed-window schedule, inverse, singleton pairing, and singleton MSM implementations or connect their exact execution equations to audited concrete kernels. No terminal-fold algebra premise remains to be supplied. | `assumed` |
| `ARKWORKS-PREPARED-VK-ROUTE-DECODE` | Proof aggregation implementation | For each accepted registered-family shipping input, verification-key routing by the authenticated VK digest and strict decoding of the retained serialized VK return the same complete prepared key: alpha, beta, gamma, delta, and the full gammaABC vector used by the verifier statement; unknown routes, malformed keys, trailing bytes, and subgroup-invalid components reject before statement installation. | ExactSemanticBoundary names vkDigestRouteExecution and serializedVkDecodeExecution separately and requires both to return one call-scoped PreparedVkMaterial. Existing VK serialization evidence binds digest bytes but does not prove either prepared-key execution. | Extract the registered-family resolver and prepared verification-key decoder, then prove both execution equations and exact failure ordering for the retained shipping call. | `assumed` |
| `ARKWORKS-VERIFIER-SRS-ID-LOAD` | Proof aggregation implementation | For each accepted shipping input, the authenticated SRS identifier loads exactly the complete V and W verifier-key arrays installed in the formal statement at every Fin (2^mu) index; the successful path never reaches a default SRS, and unknown identifiers, wrong lengths, or malformed and subgroup-invalid points reject before verification. | ExactSemanticBoundary.srsLoadExecution eliminates statementOf's formal default only under this exact call-scoped equation. WELL-FORMED-PROVING-SRS concerns the prover's power sequence and does not establish verifier SRS identity loading. | Pin or extract the production verifier SRS registry/load path and prove the retained srsId selects the exact V/W arrays with fail-closed error ordering. | `assumed` |
| `ARKWORKS-PREPARED-VK-AIC-PROJECTION` | Proof aggregation implementation | For every padded row i, the prepared verification-key projection satisfies ArkworksPreparedVkAicContract.aic: Aic i equals gammaABC[0] plus the ordered scalar-weighted gammaABC tail, with no omitted or reordered input. | AggregateAdapterProjection proves all later vector folding from this pointwise formula; construction of the formula from the concrete PreparedVerifyingKey remains external. | Extract prepared-VK construction and prove the pointwise Aic equation for every row and arity. | `assumed` |
| `ARKWORKS-KZG-VERIFIER-EQUATIONS` | Proof aggregation implementation | The shipping statement's acceptV and acceptW predicates satisfy the published KZG opening equations for every challenge, coefficient vector, claimed key, and opening, and each exact concrete pairing-effect state used by the two verifier calls reports success precisely when its installed pairing computation succeeds. | ArkworksTippKzgBoundary proves the extracted G1/G2 KZG opening checkers equivalent to concrete pairing success conjoined with those universal equations, then derives the legacy per-execution ArkworksTippKzgContract. Challenge order and coefficient construction outside the pairing effect remain Lean-proved. | Instantiate ArkworksTippKzgStatementEquations from the concrete statement predicates and prove ArkworksTippKzgPairingSuccess from the installed BLS12-377 pairing effects and audited S3 kernels. | `assumed` |
| `ARKWORKS-FR-DIGEST-REDUCTION` | Field implementation | For every 64-byte Blake2b digest, the deployed `<decaf377::Bls12_377 as Pairing>::ScalarField` (`decaf377::Fq`) conversion equals the Lean model that interprets all bytes as one little-endian integer and reduces modulo the BLS12-377 scalar modulus, returning a value for every digest. | ShippingScalarReduction proves the exact distribution and modular-reduction budget for that total model; correspondence of Fr::from_random_bytes to the model is not kernel-proved. | Extract Fr::from_random_bytes or prove byte-for-byte equivalence to ShippingHashGame.reduceFr. | `assumed` |
| `DEPLOYED-BLAKE2B-EXECUTION` | Challenge implementation | The production randomizer and TIPP challenge effects satisfy Blake2bRandomizerEffectPostcondition and Blake2bTippEffectPostcondition for the exact family, context, fuel, preimage bytes, returned Fr value, state transition, and successful nonce. | The extracted stage wrappers prove stage and message selection. These two postconditions isolate only Digest::digest, Fr conversion, the internal rejection loop, and recovery of the successful nonce. | Extract the deployed Blake2b/Fr/retry implementation with a trace-carrying result and prove both postcondition structures. | `assumed` |
| `ARKWORKS-PROVER-CURVE-OPERATIONS` | Curve and pairing implementation | Each unextracted prover scalar multiplication, MSM, pairing, and KZG-opening kernel returns the corresponding mathematical group or pairing operation on the exact supplied operands; this postcondition contains no transcript, proof-assembly, or acceptance conclusion. | Audited S3 roots cover many field/group/MSM/pairing kernels, while production construction of ShippingProverContract remains an open proof obligation. | Connect every remaining prover call site to an audited S3 root or a separately reviewed exact kernel postcondition. | `assumed` |
| `WELL-FORMED-PROVING-SRS` | Proof aggregation setup | For the exact production SRS identity selected by the call, the retained 2n-1 V/W proving powers and verifier opening keys come from one nonzero BLS12-377 secret-power sequence, their even entries are exactly the n logical GIPA keys, and this structured relation supplies precisely the WellFormedProvingSrs premise used to derive both ExecutedKzgOpeningAcceptance fields; it contains no transcript, witness, proof, or acceptance outcome. | Production pins and preflights the SRS identity and the extracted prover projection retains the full arrays, even keys, and verifier keys. The secret-power/setup relation for the selected artifact is not kernel-proved. | Verify the selected SRS artifact against a proof-producing setup transcript or retain this exact setup postcondition with an identified ceremony owner and independently reviewed artifact digest. | `assumed` |

## Audit Contract

- Required capstones: 801.
- Open claims: 1.
- Contract evidence awaiting its required checker result: 0.
- Allowed kernel axioms: `propext`, `Classical.choice`, `Quot.sound`.
- An `open` row is a recorded gap, not a passing proof result. Only the exact unregistered `DEPLOYED-SRS-SOUNDNESS` row may coexist with conditional publication closure; it still prevents an instantiated deployment security claim. Every other open row blocks publication.
